---
phase: 26-mvi-architecture
plan: 01
type: execute
wave: 1
depends_on: [23-01, 25-01]
files_modified:
  - android/app/src/main/java/com/drama/app/ui/mvi/MviIntent.kt
  - android/app/src/main/java/com/drama/app/ui/mvi/MviState.kt
  - android/app/src/main/java/com/drama/app/ui/mvi/MviEffect.kt
  - android/app/src/main/java/com/drama/app/ui/mvi/BaseMviViewModel.kt
  - android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListIntent.kt
  - android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListEffect.kt
  - android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListViewModel.kt
  - android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListScreen.kt
autonomous: true
requirements: [MVI-01, MVI-05, MVI-06, MVI-07]

must_haves:
  truths:
    - "所有 ViewModel 只有一个 public 方法：processIntent()"
    - "Composable 只发送 Intent，不直接调用 ViewModel 方法"
    - "Reducer 是纯函数：(State, Intent) -> State，无副作用"
    - "Effect 通过 SharedFlow 发射，一次性消费"
  artifacts:
    - path: "android/app/src/main/java/com/drama/app/ui/mvi/BaseMviViewModel.kt"
      provides: "MVI 泛型基类，封装 stateFlow/effectFlow/intentQueue"
      min_lines: 60
      contains: "abstract class BaseMviViewModel"
    - path: "android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListIntent.kt"
      provides: "DramaList 所有用户操作密封类"
      min_lines: 30
      contains: "sealed class DramaListIntent"
    - path: "android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListViewModel.kt"
      provides: "MVI 化后的 DramaListViewModel"
      min_lines: 80
      contains: "processIntent"
  key_links:
    - from: "DramaListScreen.kt"
      to: "DramaListViewModel.kt"
      via: "onAction = { viewModel.processIntent(it) }"
      pattern: "processIntent"
    - from: "DramaListViewModel.kt"
      to: "BaseMviViewModel.kt"
      via: "继承基类"
      pattern: "BaseMviViewModel"
---

<objective>
MVI 基座搭建 + DramaListScreen 低风险验证。创建 `BaseMviViewModel<S, I, E>` 泛型基类，定义 `MviIntent` / `MviState` / `MviEffect` 标记接口；将最简单的 DramaListViewModel 迁移为 MVI 模式，验证 Intent → Reducer → State → Effect 单向数据流可行。

Purpose: 当前 DramaListViewModel 暴露 loadDramas()/deleteDrama()/onSearchQueryChanged() 等 10+ public 方法，Composable 直接调用。MVI 化后，所有操作通过 `DramaListIntent` 密封类 + `processIntent()` 单一入口处理，Reducer 为纯函数。

Output: mvi/ 基座包（4 文件），DramaListIntent/DramaListEffect 密封类，MVI 化 DramaListViewModel + Screen。
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-mvi-architecture/26-CONTEXT.md

<!-- 当前 DramaListViewModel 公共接口（将被迁移） -->
<!-- public 方法: loadDramas(), deleteDrama(), batchDelete(), batchUpdateStatus(), loadDrama(), onSearchQueryChanged(), onStatusFilterChanged(), enterSelectionMode(), exitSelectionMode(), toggleSelection(), selectAll(), clearSelection() -->
<!-- 全部转为 DramaListIntent 密封类成员 -->

<!-- 当前 DramaListScreen 调用方式（将被改造） -->
<!-- viewModel.deleteDrama(folder) -> onAction(DramaListIntent.DeleteDrama(folder)) -->
<!-- viewModel.onSearchQueryChanged(query) -> onAction(DramaListIntent.OnSearchQueryChanged(query)) -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: MVI 基座包创建 — MviIntent/MviState/MviEffect + BaseMviViewModel</name>
  <files>
    android/app/src/main/java/com/drama/app/ui/mvi/MviIntent.kt
    android/app/src/main/java/com/drama/app/ui/mvi/MviState.kt
    android/app/src/main/java/com/drama/app/ui/mvi/MviEffect.kt
    android/app/src/main/java/com/drama/app/ui/mvi/BaseMviViewModel.kt
  </files>
  <action>
    **1. MviIntent.kt — 标记接口**
    ```kotlin
    package com.drama.app.ui.mvi

    /** MVI Intent 标记接口 — 所有用户操作/系统事件的密封类必须实现 */
    interface MviIntent
    ```

    **2. MviState.kt — 标记接口**
    ```kotlin
    package com.drama.app.ui.mvi

    /** MVI State 标记接口 — 所有 UI 状态 data class 必须实现 */
    interface MviState
    ```

    **3. MviEffect.kt — 标记接口**
    ```kotlin
    package com.drama.app.ui.mvi

    /** MVI Effect 标记接口 — 所有一次性副作用密封类必须实现 */
    interface MviEffect
    ```

    **4. BaseMviViewModel.kt — 泛型基类（核心）**
    ```kotlin
    package com.drama.app.ui.mvi

    import androidx.lifecycle.ViewModel
    import androidx.lifecycle.viewModelScope
    import kotlinx.coroutines.channels.Channel
    import kotlinx.coroutines.flow.MutableSharedFlow
    import kotlinx.coroutines.flow.MutableStateFlow
    import kotlinx.coroutines.flow.SharedFlow
    import kotlinx.coroutines.flow.StateFlow
    import kotlinx.coroutines.flow.asSharedFlow
    import kotlinx.coroutines.flow.asStateFlow
    import kotlinx.coroutines.flow.consumeEach
    import kotlinx.coroutines.launch

    /**
     * MVI 泛型基类（per D-26-01, D-26-02）。
     *
     * S = State（不可变 UI 状态）
     * I = Intent（用户操作/系统事件密封类）
     * E = Effect（一次性副作用，如导航/Toast/Haptic）
     *
     * 核心机制：
     * 1. [intentQueue] Channel 顺序消费，保证所有 Intent 串行处理，消除并发竞态
     * 2. [reduce] 纯函数，(State, Intent) -> State，无副作用，可独立测试
     * 3. [state] StateFlow 驱动 Compose 重组
     * 4. [effect] SharedFlow 发射一次性事件，UI 层 collect 消费
     */
    abstract class BaseMviViewModel<
        S : MviState,
        I : MviIntent,
        E : MviEffect
    >(
        initialState: S
    ) : ViewModel() {

        private val _state = MutableStateFlow(initialState)
        val state: StateFlow<S> = _state.asStateFlow()

        private val _effect = MutableSharedFlow<E>(extraBufferCapacity = 64)
        val effect: SharedFlow<E> = _effect.asSharedFlow()

        /** Intent 顺序队列 — D-26-01: UNLIMITED 防止 WS 消息突发阻塞 */
        private val intentQueue = Channel<I>(Channel.UNLIMITED)

        init {
            viewModelScope.launch {
                intentQueue.consumeEach { intent ->
                    // ★ 串行处理，绝无并发竞态！
                    val currentState = _state.value
                    val newState = reduce(currentState, intent)
                    _state.value = newState
                }
            }
        }

        /** 唯一公共入口 — 所有操作必须经此入队 */
        fun processIntent(intent: I) {
            intentQueue.trySend(intent)
        }

        /** Reducer 纯函数 — 子类实现 (State, Intent) -> State */
        protected abstract fun reduce(state: S, intent: I): S

        /** 发射一次性 Effect */
        protected fun emitEffect(effect: E) {
            _effect.tryEmit(effect)
        }

        override fun onCleared() {
            super.onCleared()
            intentQueue.close()
        }
    }
    ```
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama/android && grep 'interface MviIntent' app/src/main/java/com/drama/app/ui/mvi/MviIntent.kt && grep 'interface MviState' app/src/main/java/com/drama/app/ui/mvi/MviState.kt && grep 'interface MviEffect' app/src/main/java/com/drama/app/ui/mvi/MviEffect.kt && grep 'abstract class BaseMviViewModel' app/src/main/java/com/drama/app/ui/mvi/BaseMviViewModel.kt && grep 'consumeEach' app/src/main/java/com/drama/app/ui/mvi/BaseMviViewModel.kt && grep 'Channel.UNLIMITED' app/src/main/java/com/drama/app/ui/mvi/BaseMviViewModel.kt && grep 'processIntent' app/src/main/java/com/drama/app/ui/mvi/BaseMviViewModel.kt</automated>
  </verify>
  <done>
    - mvi/ 包包含 MviIntent / MviState / MviEffect 标记接口
    - BaseMviViewModel 抽象基类包含 state/effect/intentQueue
    - intentQueue 使用 Channel.UNLIMITED 顺序消费
    - processIntent() 是唯一公共入口
    - reduce() 是纯函数抽象方法
  </done>
</task>

<task type="auto">
  <name>Task 2: DramaListIntent / DramaListEffect 密封类创建</name>
  <files>
    android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListIntent.kt
    android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListEffect.kt
  </files>
  <action>
    **1. DramaListIntent.kt — 所有操作密封类**
    ```kotlin
    package com.drama.app.ui.screens.dramalist

    import com.drama.app.ui.mvi.MviIntent

    /** DramaListScreen 所有用户操作 / 系统事件（per MVI-01） */
    sealed class DramaListIntent : MviIntent {
        // === 数据操作 ===
        data object LoadDramas : DramaListIntent()
        data class DeleteDrama(val folder: String) : DramaListIntent()
        data class BatchDelete(val folders: Set<String>) : DramaListIntent()
        data class BatchUpdateStatus(val folders: Set<String>, val newStatus: String) : DramaListIntent()
        data class LoadDrama(val folder: String) : DramaListIntent()

        // === 搜索过滤 ===
        data class OnSearchQueryChanged(val query: String) : DramaListIntent()
        data class OnStatusFilterChanged(val filter: String?) : DramaListIntent()

        // === 选择模式 ===
        data object EnterSelectionMode : DramaListIntent()
        data object ExitSelectionMode : DramaListIntent()
        data class ToggleSelection(val folder: String) : DramaListIntent()
        data class SelectAll(val folders: List<String>) : DramaListIntent()
        data object ClearSelection : DramaListIntent()
    }
    ```

    **2. DramaListEffect.kt — 一次性副作用**
    ```kotlin
    package com.drama.app.ui.screens.dramalist

    import com.drama.app.ui.mvi.MviEffect

    /** DramaListScreen 一次性副作用（per MVI-06） */
    sealed class DramaListEffect : MviEffect {
        data class ShowSnackbar(val message: String) : DramaListEffect()
        data class NavigateToDetail(val dramaId: String) : DramaListEffect()
    }
    ```
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama/android && grep 'sealed class DramaListIntent' app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListIntent.kt && grep 'sealed class DramaListEffect' app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListEffect.kt && grep 'MviIntent' app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListIntent.kt && grep 'MviEffect' app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListEffect.kt</automated>
  </verify>
  <done>
    - DramaListIntent 密封类覆盖所有现有 public 方法对应的操作
    - DramaListEffect 密封类覆盖 Snackbar / 导航等一次性事件
    - 两者分别实现 MviIntent / MviEffect 标记接口
  </done>
</task>

<task type="auto">
  <name>Task 3: DramaListViewModel MVI 化</name>
  <files>
    android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListViewModel.kt
  </files>
  <read_first>
    android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListViewModel.kt
  </read_first>
  <action>
    将 DramaListViewModel 改造为继承 BaseMviViewModel，删除所有 public 方法，只保留 `processIntent()`。

    **关键改造点：**
    1. `class DramaListViewModel ... : BaseMviViewModel<DramaListUiState, DramaListIntent, DramaListEffect>(DramaListUiState())`
    2. 删除所有现有的 public fun（loadDramas, deleteDrama, onSearchQueryChanged...）
    3. 实现 `override fun reduce(state: DramaListUiState, intent: DramaListIntent): DramaListUiState`
    4. 数据操作（需挂起函数）在 reduce 中启动 viewModelScope.launch，并通过 _state.update 间接更新

    **注意：** 由于 reduce 必须是纯函数但部分操作需要挂起（如网络请求），采用**异步 Intent 模式**：
    - reduce 中识别需要异步操作的 Intent，返回 `state.copy(isLoading = true)`
    - 同时启动协程执行请求，完成后发送新的 Intent 回队列

    ```kotlin
    override fun reduce(state: DramaListUiState, intent: DramaListIntent): DramaListUiState {
        return when (intent) {
            is DramaListIntent.LoadDramas -> {
                viewModelScope.launch { loadDramasAsync() }
                state.copy(isLoading = true, error = null)
            }
            is DramaListIntent.OnSearchQueryChanged -> state.copy(searchQuery = intent.query)
            is DramaListIntent.EnterSelectionMode -> state.copy(isSelectionMode = true, selectedFolders = emptySet())
            // ... 其余分支
        }
    }

    /** 异步加载完成后回传结果 */
    private fun loadDramasAsync() {
        viewModelScope.launch {
            dramaRepository.listDramas()
                .onSuccess { dramas ->
                    processIntent(DramaListIntent.Internal.DramasLoaded(dramas))
                }
                .onFailure { e ->
                    processIntent(DramaListIntent.Internal.LoadError(e.message))
                }
        }
    }
    ```

    **内部 Intent（异步结果回传）：**
    ```kotlin
    sealed class DramaListIntent : MviIntent {
        // ... 外部 Intent ...

        /** 内部异步结果 — 不暴露给 UI */
        sealed class Internal : DramaListIntent() {
            data class DramasLoaded(val dramas: List<Drama>) : Internal()
            data class LoadError(val message: String?) : Internal()
        }
    }
    ```
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama/android && grep 'BaseMviViewModel' app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListViewModel.kt && grep 'override fun reduce' app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListViewModel.kt && grep 'fun processIntent' app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListViewModel.kt | wc -l | xargs test {} -eq 1 && echo "PASS: only one processIntent" || echo "FAIL"</automated>
  </verify>
  <done>
    - DramaListViewModel 继承 BaseMviViewModel
    - 所有现有 public 方法删除，转为 reduce 中的 when 分支
    - 异步操作通过 Internal 子密封类回传结果
    - 只有一个 public 方法 processIntent()
  </done>
</task>

<task type="auto">
  <name>Task 4: DramaListScreen 改造 — 只发送 Intent</name>
  <files>
    android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListScreen.kt
  </files>
  <read_first>
    android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListScreen.kt
  </read_first>
  <action>
    改造 DramaListScreen，所有事件回调改为发送 Intent。

    **典型替换：**
    ```kotlin
    // Before:
    onDelete = { folder -> viewModel.deleteDrama(folder) }
    onSearch = { query -> viewModel.onSearchQueryChanged(query) }

    // After:
    onDelete = { folder -> viewModel.processIntent(DramaListIntent.DeleteDrama(folder)) }
    onSearch = { query -> viewModel.processIntent(DramaListIntent.OnSearchQueryChanged(query)) }
    ```

    **Effect 收集：**
    ```kotlin
    val context = LocalContext.current
    LaunchedEffect(Unit) {
        viewModel.effect.collect { effect ->
            when (effect) {
                is DramaListEffect.ShowSnackbar -> scaffoldState.showSnackbar(effect.message)
                is DramaListEffect.NavigateToDetail -> navController.navigate(Route.DramaDetail(effect.dramaId))
            }
        }
    }
    ```
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama/android && grep 'processIntent' app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListScreen.kt | wc -l | xargs -I {} sh -c 'if [ {} -ge 5 ]; then echo "PASS: multiple processIntent calls"; else echo "FAIL"; fi'</automated>
  </verify>
  <done>
    - DramaListScreen 所有事件通过 processIntent 发送
    - 无直接 ViewModel 方法调用
    - Effect 通过 LaunchedEffect 收集
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IntentQueue → Reducer | 内部信任边界，同进程内串行处理 |
| Reducer → State | 纯函数边界，无副作用 |
| Effect → UI | 一次性事件，消费后失效 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | D (DoS) | Channel.UNLIMITED 无界缓冲 | accept | Intent 对象极小（密封类实例），64MB 内存可容纳数百万个；极端情况 OOM 前已有其他问题 |
| T-26-02 | T (Tampering) | Internal Intent 被外部伪造 | mitigate | Internal 子类 sealed，外部无法实例化跨包子类（Kotlin sealed 限制） |
</threat_model>

<verification>
1. 编译检查：`cd /workspace/director-actor-drama/android && ./gradlew :app:compileDebugKotlin` 无错误
2. grep 验证：DramaListViewModel 中无 `fun loadDramas` / `fun deleteDrama` 等旧 public 方法
3. grep 验证：DramaListScreen 中所有 viewModel. 调用均为 processIntent
4. 结构完整性：mvi/ 包 4 文件存在，DramaListIntent / DramaListEffect 密封类存在
5. 单元测试：Reducer 纯函数可独立测试（不需 Android 环境）
</verification>

<success_criteria>
- BaseMviViewModel 泛型基类可用，含 state/effect/intentQueue/reduce/processIntent
- DramaListIntent 密封类覆盖所有用户操作
- DramaListEffect 密封类覆盖所有一次性副作用
- DramaListViewModel 只有一个 public 方法 processIntent()
- DramaListScreen 只发送 Intent，不直接调用 VM 方法
- 编译通过，运行时列表加载/删除/搜索/筛选功能正常
</success_criteria>

<output>
After completion, create `.planning/phases/26-mvi-architecture/26-01-SUMMARY.md`
</output>
