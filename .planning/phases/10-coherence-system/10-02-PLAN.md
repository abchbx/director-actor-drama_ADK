---
phase: 10-coherence-system
plan: 02
type: tdd
wave: 2
depends_on:
  - 10-01
files_modified:
  - app/tools.py
  - app/context_builder.py
  - app/agent.py
  - tests/unit/test_tools_phase10.py
autonomous: true
requirements:
  - COHERENCE-01
  - COHERENCE-02
  - COHERENCE-03
  - COHERENCE-04

must_haves:
  truths:
    - "导演可通过 add_fact Tool 添加事实到 established_facts"
    - "导演可通过 validate_consistency Tool 触发 LLM 一致性检查"
    - "导演可通过 repair_contradiction Tool 标记矛盾已修复"
    - "导演上下文【已确立事实】段落展示 high/medium 事实 + 检查提醒"
    - "演员上下文【角色锚点】段落包含性格核心+关键记忆+已确立事实"
    - "演员上下文锚点段落优先级 7 且不可截断"
    - "导演 prompt §11 引导一致性保障行为"
    - "_improv_director tools 列表包含 3 个新工具"
  artifacts:
    - path: "app/tools.py"
      provides: "3 个 Tool 薄代理函数"
      exports: ["add_fact", "validate_consistency", "repair_contradiction"]
    - path: "app/context_builder.py"
      provides: "_build_facts_section 完整实现 + 角色锚点段落"
      contains: "actor_dna"
    - path: "app/agent.py"
      provides: "导演 prompt §11 + tools 列表更新"
      contains: "§11 一致性保障"
    - path: "tests/unit/test_tools_phase10.py"
      provides: "Tool 函数集成测试"
      contains: "test_add_fact"
  key_links:
    - from: "app/tools.py::add_fact"
      to: "app/coherence_checker.py::add_fact_logic"
      via: "纯函数调用 + 状态读写"
      pattern: "add_fact_logic"
    - from: "app/tools.py::validate_consistency"
      to: "app/coherence_checker.py::validate_consistency_logic"
      via: "纯函数 + LLM 调用"
      pattern: "validate_consistency_logic"
    - from: "app/context_builder.py::_build_facts_section"
      to: "app/coherence_checker.py"
      via: "COHERENCE_CHECK_INTERVAL 常量"
      pattern: "COHERENCE_CHECK_INTERVAL"
    - from: "app/agent.py::_improv_director"
      to: "app/tools.py"
      via: "tools 列表注册"
      pattern: "add_fact.*validate_consistency.*repair_contradiction"
---

<objective>
集成 coherence_checker.py 到系统——3 个 Tool 函数薄代理、context_builder 升级（_build_facts_section 完整实现 + 角色锚点段落）、agent.py §11 prompt 和 tools 注册。

Purpose: 让导演和演员能通过 Tool 调用使用一致性系统，让上下文自动展示事实和检查提醒，让角色锚点保障角色一致性（COHERENCE-03）。
Output: 完整集成——Tools + context_builder + agent.py
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-coherence-system/10-CONTEXT.md
@.planning/phases/10-coherence-system/10-RESEARCH.md
@.planning/phases/10-coherence-system/10-01-SUMMARY.md

<interfaces>
<!-- Plan 01 产出接口 — executor 必须使用这些 -->

From app/coherence_checker.py (Plan 01 产出):
```python
# 常量
FACT_CATEGORIES: set[str]  # {"event", "identity", "location", "relationship", "rule"}
COHERENCE_CHECK_INTERVAL: int  # 5
MAX_FACTS: int  # 50
MAX_CHECK_HISTORY: int  # 10

# 纯函数 — Tool 薄代理调用这些
def add_fact_logic(fact: str, category: str, importance: str, state: dict) -> dict:
    # 返回 {"status": "success"/"info"/"error", ...}

def validate_consistency_logic(state: dict) -> dict:
    # 返回 {"status": "success", "relevant_facts": [...], "recent_scenes": [...], ...}

def validate_consistency_prompt(facts: list[dict], recent_scenes: list[dict]) -> str:
    # 返回完整 LLM prompt 字符串

def parse_contradictions(response_text: str, relevant_facts: list[dict]) -> list[dict]:
    # 返回矛盾列表，每条含 severity

def generate_repair_narration_prompt(contradiction: dict, repair_type: str) -> str:
    # 返回修复旁白 prompt 字符串

def repair_contradiction_logic(fact_id: str, repair_type: str, repair_note: str, state: dict) -> dict:
    # 返回 {"status": "success"/"error", ...}

def _filter_relevant_facts(state: dict) -> list[dict]:
    # 返回筛选后的事实列表

def _extract_actor_names(fact_text: str, known_actors: list[str]) -> list[str]:
    # 返回匹配的角色名列表
```

From app/tools.py — 现有薄代理模式:
```python
def create_thread(description: str, involved_actors: str, tool_context: ToolContext) -> dict:
    state = _get_state(tool_context)
    state.setdefault("plot_threads", [])
    actor_list = [name.strip() for name in involved_actors.split(",") if name.strip()]
    result = create_thread_logic(description, actor_list, state)
    if result["status"] == "success":
        _set_state(state, tool_context)
        return {"status": "success", "thread_id": result["thread_id"], "message": f"📌 线索已创建：..."}
    return result
```

From app/context_builder.py — 段落组装模式:
```python
# _DIRECTOR_SECTION_PRIORITIES 已有 "facts": 5
# _ACTOR_SECTION_PRIORITIES 已有 "anchor": 6
# _assemble_actor_sections 已有 anchor 段落 (priority 6)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — 3 个 Tool 函数薄代理 + 测试</name>
  <files>app/tools.py, tests/unit/test_tools_phase10.py</files>
  <read_first>
    app/tools.py
    app/coherence_checker.py
    app/arc_tracker.py
    tests/unit/test_tools_phase7.py
  </read_first>
  <behavior>
    - Test 1: add_fact Tool 调用 add_fact_logic 并写入 state
    - Test 2: add_fact Tool 默认 category="event"，importance="medium"
    - Test 3: validate_consistency Tool 调用 validate_consistency_logic + LLM 并更新 coherence_checks
    - Test 4: validate_consistency Tool 无相关事实时返回无需检查
    - Test 5: repair_contradiction Tool 调用 repair_contradiction_logic 并更新 state
    - Test 6: repair_contradiction Tool 验证 repair_type 为 supplement/correction
  </behavior>
  <action>
    **RED 阶段**：创建 tests/unit/test_tools_phase10.py，编写上述 6 个测试用例。使用 conftest.py 的 mock_tool_context fixture（需添加 established_facts 和 coherence_checks 字段到 fixture）。运行确认失败。

    **GREEN 阶段**：在 app/tools.py 中实现 3 个 Tool 函数：

    1. **`add_fact(fact: str, category: str = "event", importance: str = "medium", tool_context: ToolContext) -> dict`**（per D-38/D-09）：
       - 读取 state：`state = _get_state(tool_context)`
       - 确保 state 有 established_facts：`state.setdefault("established_facts", [])`
       - 调用纯函数：`result = add_fact_logic(fact, category, importance, state)`
       - 成功时写回 state：`_set_state(state, tool_context)`
       - 返回格式：`{"status": "success", "fact_id": result["fact_id"], "message": "📌 事实已记录：..."}`

    2. **`async def validate_consistency(tool_context: ToolContext) -> dict`**（per D-37）：
       - 读取 state
       - 调用 `validate_consistency_logic(state)` 获取相关事实
       - 无相关事实时返回 `{"status": "success", "message": "✅ 一致性检查通过，无需检查", "contradictions": [], ...}`
       - 构建 prompt：`prompt = validate_consistency_prompt(relevant_facts, recent_scenes)`
       - 调用 LLM：`from .memory_manager import _call_llm; response_text = await _call_llm(prompt)`
       - LLM 失败时返回 error
       - 解析响应：`contradictions = parse_contradictions(response_text, relevant_facts)`
       - 更新 `state["coherence_checks"]`：
         - `last_check_scene = state.get("current_scene", 0)`
         - `last_result = len(contradictions)` 或 "clean"
         - 追加到 `check_history`（保留最近 MAX_CHECK_HISTORY 条）
         - `total_contradictions += len(contradictions)`
       - 写回 state：`_set_state(state, tool_context)`
       - 返回格式（per D-15）：
         ```python
         {
             "status": "success",
             "message": "✅ 一致性检查通过" 或 "⚠️ 发现 N 处潜在矛盾",
             "contradictions": [...],
             "facts_checked": len(relevant_facts),
             "scenes_analyzed": len(recent_scenes),
         }
         ```

    3. **`repair_contradiction(fact_id: str, repair_type: str, tool_context: ToolContext) -> dict`**（per D-39）：
       - 读取 state
       - 调用 `repair_contradiction_logic(fact_id, repair_type, repair_note="", state=state)`
       - 注意：repair_note 由导演通过旁白自然修复，Tool 只标记状态，repair_note 留空或可选
       - 成功时写回 state
       - 返回格式：`{"status": "success", "fact_id": ..., "repair_type": ..., "message": "✅ 矛盾已标记修复"}`

    4. **tools.py import 更新**：
       - 在文件顶部 import 区添加：`from .coherence_checker import (add_fact_logic, validate_consistency_logic, validate_consistency_prompt, parse_contradictions, repair_contradiction_logic, COHERENCE_CHECK_INTERVAL, MAX_CHECK_HISTORY)`

    5. **conftest.py fixture 更新**：
       - 在 mock_tool_context fixture 的 drama state 中添加：
         ```python
         "established_facts": [],
         "coherence_checks": {
             "last_check_scene": 0,
             "last_result": None,
             "check_history": [],
             "total_contradictions": 0,
         },
         ```

    运行 `uv run pytest tests/unit/test_tools_phase10.py -x -q` 确认通过。
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_tools_phase10.py -x -q</automated>
  </verify>
  <done>
    - tools.py 包含 add_fact、validate_consistency、repair_contradiction 3 个 Tool 函数
    - add_fact 默认 category="event", importance="medium"
    - validate_consistency 是 async 函数，调用 _call_llm
    - repair_contradiction 调用 repair_contradiction_logic
    - 所有 6 个测试通过
    - conftest.py fixture 包含 established_facts 和 coherence_checks
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: context_builder 升级 + agent.py §11 prompt + tools 注册</name>
  <files>app/context_builder.py, app/agent.py, tests/unit/test_context_builder.py</files>
  <read_first>
    app/context_builder.py
    app/agent.py
    app/coherence_checker.py
    .planning/phases/10-coherence-system/10-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: _build_facts_section 展示 high importance 事实标记为 [核心]
    - Test 2: _build_facts_section 展示 medium importance 事实标记为 category 中文名
    - Test 3: _build_facts_section 不展示 low importance 事实
    - Test 4: _build_facts_section 每 5 场显示检查提醒
    - Test 5: _build_facts_section 无事实时返回空文本（或有检查提醒）
    - Test 6: 角色锚点段落包含性格核心+关键记忆+已确立事实
    - Test 7: 角色锚点段落优先级 7 且 truncatable=False
  </behavior>
  <action>
    **RED 阶段**：在 tests/unit/test_context_builder.py 中新增上述 7 个测试函数。运行确认失败。

    **GREEN 阶段**：

    1. **context_builder.py — `_build_facts_section(state: dict)` 完整实现**（per D-05/D-28）：
       - 替换现有空壳（L824-845），实现完整逻辑：
       - 从 `state.get("established_facts", [])` 获取事实列表
       - 从 `state.get("coherence_checks", {})` 获取检查状态
       - 筛选仅 high/medium importance 事实（D-05/D-28）
       - 展示格式：
         - `CATEGORY_LABELS = {"event": "事件", "identity": "身份", "location": "地点", "relationship": "关系", "rule": "规则"}`
         - `IMPORTANCE_LABELS = {"high": "核心", "medium": ""}`
         - high: `[核心] 事实文本（第N场确立，涉及：A、B）`
         - medium: `[事件] 事实文本（第N场确立，涉及：A）`
       - Header：`事实总数：N 条 | 上次检查：第N场（无矛盾/发现N处矛盾）`
       - 检查提醒：距上次检查 ≥ COHERENCE_CHECK_INTERVAL 场时显示 `💡 距上次检查已 N 场，建议调用 validate_consistency() 检查一致性`
       - 无事实但有检查提醒时仅显示提醒行
       - 无事实无提醒时返回空文本
       - 需要 import COHERENCE_CHECK_INTERVAL：`from .coherence_checker import COHERENCE_CHECK_INTERVAL`

    2. **context_builder.py — `_assemble_actor_sections` 新增角色锚点段落**（per D-23~D-27）：
       - 在现有 anchor 段落（L221-230）之后，新增 `actor_dna` section
       - 注意：现有的 `anchor` section（priority 6）是基础锚点"你是X，Y。Z"，新增的 `actor_dna` 是一致性约束锚点
       - 在 `_ACTOR_SECTION_PRIORITIES` 中添加：`"actor_dna": 7`（D-26: 最高优先级）
       - 在 `_assemble_actor_sections` 中，anchor 段落之后添加：
         ```python
         # Phase 10: Actor DNA anchor — character consistency constraint (D-23~D-27, priority 7)
         dna_lines = _build_actor_dna_text(actor_name, actor_data, tool_context)
         if dna_lines:
             sections.append({
                 "key": "actor_dna",
                 "text": dna_lines,
                 "priority": 7,  # D-26: 最高优先级，不可截断
                 "truncatable": False,
             })
         ```
       - 新增 `_build_actor_dna_text(actor_name: str, actor_data: dict, tool_context: ToolContext) -> str` 辅助函数：
         - 性格核心：`actor_data.get("personality", "")`
         - 关键记忆：`working_memory` 中 `is_critical=True` 的条目（取最重要的 1 条）
           - 实际从 `actor_data.get("critical_memories", [])` 获取（项目使用 critical_memories 列表）
         - 已确立事实：`state["established_facts"]` 中 actors 包含该演员的 high importance 事实（取最重要的 1 条）
         - 格式：
           ```
           【角色锚点】（你必须遵守的约束）
           性格核心：果断、野心勃勃、多疑
           关键记忆：[第3场] 你发现了密信的内容
           已确立事实：你已起兵（第5场）
           ```
         - 三项内容均有则全部展示，缺少某项则省略该行
         - 三项都无则返回空字符串

    3. **agent.py — 导演 prompt 新增 §11**（per D-30）：
       - 在 §10 之后添加：
         ```
         ## §11 一致性保障
         - 每场 write_scene 后，考虑用 add_fact 记录关键事实（角色生死、地点切换、关系变化、世界规则）
         - 【已确立事实】段落显示提醒时，调用 validate_consistency() 检查一致性
         - 发现矛盾时，用修复性旁白（"其实..."、"之前未曾提及的是..."）自然圆回，不要报错中断
         - 高严重度矛盾必须修复，中严重度建议修复，低严重度可忽略
         ```

    4. **agent.py — _improv_director tools 列表更新**（per D-37~D-39）：
       - 在 tools 列表末尾（`resolve_conflict_tool` 之后）添加：`add_fact, validate_consistency, repair_contradiction`
       - 确保 import 存在：`from .tools import add_fact, validate_consistency, repair_contradiction`

    运行 `uv run pytest tests/unit/test_context_builder.py -x -q -k "facts or actor_dna or Facts or ActorDna"` 确认新增测试通过。
    运行 `uv run pytest tests/unit/test_context_builder.py -x -q` 确认全部通过。
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_context_builder.py -x -q && uv run pytest tests/unit/test_tools_phase10.py -x -q</automated>
  </verify>
  <done>
    - _build_facts_section 展示 high/medium 事实 + 检查提醒
    - _build_facts_section 不展示 low importance 事实
    - 每 5 场显示检查提醒
    - 角色锚点段落包含性格核心+关键记忆+已确立事实
    - 角色锚点段落 priority=7 且 truncatable=False
    - 导演 prompt 包含 §11 一致性保障段落
    - _improv_director tools 列表包含 add_fact, validate_consistency, repair_contradiction
    - 所有测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 导演 Tool 调用 → validate_consistency LLM | LLM 调用可能超时或返回非法格式 |
| 上下文构建 → state 读取 | established_facts 可能缺失（旧存档） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Denial of Service | validate_consistency LLM 超时 | mitigate | try/except 包裹 _call_llm 调用，超时返回 error dict |
| T-10-06 | Tampering | _build_facts_section 读取旧格式 facts | mitigate | 兼容旧格式（isinstance 检查 list 中元素是 str 还是 dict） |
| T-10-07 | Information Disclosure | 角色锚点泄露其他演员事实 | mitigate | 仅包含涉及当前演员的 high importance 事实 |
</threat_model>

<verification>
1. `uv run pytest tests/unit/test_tools_phase10.py -x -q` — Tool 函数测试通过
2. `uv run pytest tests/unit/test_context_builder.py -x -q` — 上下文构建测试通过
3. `grep "§11 一致性保障" app/agent.py` — §11 存在
4. `grep "add_fact, validate_consistency, repair_contradiction" app/agent.py` — tools 注册
5. `grep "actor_dna" app/context_builder.py` — 角色锚点段落
6. `grep "COHERENCE_CHECK_INTERVAL" app/context_builder.py` — 检查间隔常量使用
7. `uv run ruff check app/tools.py app/context_builder.py app/agent.py` — 无 lint 错误
</verification>

<success_criteria>
1. 3 个 Tool 函数（add_fact, validate_consistency, repair_contradiction）在 tools.py 中实现
2. _build_facts_section 完整实现——展示 high/medium 事实、检查提醒、上次检查结果
3. 角色锚点段落（actor_dna）在 _assemble_actor_sections 中实现，priority=7, truncatable=False
4. 导演 prompt §11 一致性保障段落存在
5. _improv_director tools 列表包含 3 个新工具
6. 所有测试通过
7. ruff check 无错误
</success_criteria>

<output>
After completion, create `.planning/phases/10-coherence-system/10-02-SUMMARY.md`
</output>
