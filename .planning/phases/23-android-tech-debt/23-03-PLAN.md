---
phase: 23-android-tech-debt
plan: 03
type: execute
wave: 3
depends_on: [23-02]
priority: P2/P3 (optional)
status: deferred_per_D-23-18
files_modified:
  - android/app/src/test/java/com/drama/app/ui/screens/dramadetail/orchestrator/ConnectionOrchestratorTest.kt
  - android/app/src/test/java/com/drama/app/data/remote/interceptor/BaseUrlInterceptorTest.kt
  - android/app/src/test/java/com/drama/app/MainDispatcherRule.kt
  - android/app/build.gradle.kts

autonomous: true
requirements: [ARCH-02, ARCH-04, ARCH-05]

must_haves:
  truths:
    - "ConnectionOrchestrator 的 WS 连接/降级/重连行为可被单元测试验证"
    - "BaseUrlInterceptor 的动态 URL 替换逻辑可被单元测试验证"
  artifacts:
    - path: "android/app/src/test/java/com/drama/app/ui/screens/dramadetail/orchestrator/ConnectionOrchestratorTest.kt"
      provides: "ConnectionOrchestrator 单元测试"
      min_lines: 60
      contains: "@Test"
    - path: "android/app/src/test/java/com/drama/app/data/remote/interceptor/BaseUrlInterceptorTest.kt"
      provides: "BaseUrlInterceptor 单元测试"
      min_lines: 40
      contains: "@Test"
    - path: "android/app/src/test/java/com/drama/app/MainDispatcherRule.kt"
      provides: "共享协程测试 Rule"
      min_lines: 20
      contains: "TestWatcher"
---

<objective>
P2/P3 扫荡战 — 补全关键子组件测试覆盖 + 可选增强。

Purpose: 23-01 和 23-02 已解决全部 17 个 ARCH 问题（ARCH-01~ARCH-17）。本 Plan 补全验证矩阵中缺失的 ConnectionOrchestrator 和 BaseUrlInterceptor 单元测试，以及可选的 P2/P3 增强（Room 缓存、RetryPolicy、图标库优化、CI/CD）。

Per D-23-18: P2/P3 标记为 deferred，不影响核心功能和稳定性。本 Plan 的必做项仅限测试补全，P2/P3 增强为可选。
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-android-tech-debt/23-01-PLAN.md
@.planning/phases/23-android-tech-debt/23-02-PLAN.md

<!-- 依赖 23-01 创建的 ConnectionOrchestrator（含 _isWsConnected, SharedFlow 事件） -->
<!-- 依赖 23-02 创建的 BaseUrlInterceptor + ServerPreferences.currentApiBaseUrl() -->

<interfaces>
<!-- ConnectionOrchestrator.kt 关键接口（23-02 修复后） -->
```kotlin
class ConnectionOrchestrator @Inject constructor(
    private val webSocketManager: WebSocketManager,
    private val serverPreferences: ServerPreferences,
) {
    sealed class ConnectionEvent { ... }
    val events: SharedFlow<ConnectionEvent>
    val isWsConnected: StateFlow<Boolean>     // 23-02 修复: 独立 MutableStateFlow
    val connectionState: StateFlow<ConnectionState>
    fun connect(dramaId: String, token: String, scope: CoroutineScope)
    fun disconnect()
    fun cleanup()
}
```

<!-- BaseUrlInterceptor.kt 接口（23-02 新建） -->
```kotlin
class BaseUrlInterceptor @Inject constructor(
    private val serverPreferences: ServerPreferences,
) : Interceptor {
    override fun intercept(chain: Interceptor.Chain): Response
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MainDispatcherRule + ConnectionOrchestrator/BaseUrlInterceptor 单元测试</name>
  <files>
    android/app/src/test/java/com/drama/app/MainDispatcherRule.kt
    android/app/src/test/java/com/drama/app/ui/screens/dramadetail/orchestrator/ConnectionOrchestratorTest.kt
    android/app/src/test/java/com/drama/app/data/remote/interceptor/BaseUrlInterceptorTest.kt
  </files>
  <read_first>
    android/app/src/main/java/com/drama/app/ui/screens/dramadetail/orchestrator/ConnectionOrchestrator.kt
    android/app/src/main/java/com/drama/app/data/remote/interceptor/BaseUrlInterceptor.kt
    android/app/src/test/java/com/drama/app/ui/screens/dramadetail/orchestrator/BubbleMergerTest.kt
  </read_first>
  <action>
    **1. MainDispatcherRule.kt — 共享协程测试 Rule**

    创建 `src/test/java/com/drama/app/MainDispatcherRule.kt`：
    ```kotlin
    package com.drama.app

    import kotlinx.coroutines.Dispatchers
    import kotlinx.coroutines.test.TestDispatcher
    import kotlinx.coroutines.test.UnconfinedTestDispatcher
    import kotlinx.coroutines.test.resetMain
    import kotlinx.coroutines.test.setMain
    import org.junit.rules.TestWatcher
    import org.junit.runner.Description

    /**
     * 共享的协程测试 Rule — 替换 Main dispatcher 为 UnconfinedTestDispatcher。
     * 在 ViewModel 和包含 viewModelScope.launch 的组件测试中使用。
     */
    class MainDispatcherRule(
        private val testDispatcher: TestDispatcher = UnconfinedTestDispatcher()
    ) : TestWatcher() {
        override fun starting(description: Description) {
            Dispatchers.setMain(testDispatcher)
        }
        override fun finished(description: Description) {
            Dispatchers.resetMain()
        }
    }
    ```

    **2. ConnectionOrchestratorTest.kt — WS 连接编排测试**

    创建 `src/test/java/.../orchestrator/ConnectionOrchestratorTest.kt`：
    ```kotlin
    package com.drama.app.ui.screens.dramadetail.orchestrator

    import com.drama.app.MainDispatcherRule
    import com.drama.app.data.remote.ws.ConnectionState
    import com.drama.app.data.remote.ws.WebSocketManager
    import com.drama.app.data.local.ServerPreferences
    import com.drama.app.domain.model.ServerConfig
    import kotlinx.coroutines.flow.MutableStateFlow
    import kotlinx.coroutines.flow.flowOf
    import kotlinx.coroutines.test.runTest
    import org.junit.Assert.*
    import org.junit.Before
    import org.junit.Rule
    import org.junit.Test
    import org.mockito.kotlin.mock
    import org.mockito.kotlin.whenever

    class ConnectionOrchestratorTest {

        @get:Rule
        val dispatcherRule = MainDispatcherRule()

        private lateinit var orchestrator: ConnectionOrchestrator
        private val mockWebSocketManager: WebSocketManager = mock()
        private val mockServerPreferences: ServerPreferences = mock()

        @Before
        fun setup() {
            whenever(mockWebSocketManager.connectionState)
                .thenReturn(MutableStateFlow(ConnectionState.Disconnected))
            whenever(mockWebSocketManager.events).thenReturn(flowOf())
            whenever(mockServerPreferences.serverConfig).thenReturn(
                flowOf(ServerConfig(ip = "192.168.1.100", port = "8000", token = "test"))
            )
            orchestrator = ConnectionOrchestrator(mockWebSocketManager, mockServerPreferences)
        }

        @Test
        fun `initial state is disconnected`() {
            assertFalse(orchestrator.isWsConnected.value)
            assertFalse(orchestrator.hasConnected())
        }

        @Test
        fun `isWsConnected reflects WebSocketManager state`() = runTest {
            val connectionState = MutableStateFlow<ConnectionState>(ConnectionState.Disconnected)
            whenever(mockWebSocketManager.connectionState).thenReturn(connectionState)

            // Re-create to pick up mock
            orchestrator = ConnectionOrchestrator(mockWebSocketManager, mockServerPreferences)

            // Initially disconnected
            assertFalse(orchestrator.isWsConnected.value)

            // Simulate connection
            connectionState.value = ConnectionState.Connected
            orchestrator.connect("test-drama", "token", this)
            // Wait for collection
            advanceUntilIdle()
            assertTrue(orchestrator.isWsConnected.value)
        }

        @Test
        fun `connect sets hasConnected flag`() = runTest {
            orchestrator.connect("test-drama", "token", this)
            assertTrue(orchestrator.hasConnected())
        }

        @Test
        fun `disconnect resets hasConnected and isWsConnected`() = runTest {
            orchestrator.connect("test-drama", "token", this)
            orchestrator.disconnect()
            assertFalse(orchestrator.hasConnected())
            assertFalse(orchestrator.isWsConnected.value)
        }

        @Test
        fun `cleanup calls disconnect and releases`() {
            orchestrator.cleanup()
            assertFalse(orchestrator.hasConnected())
            assertFalse(orchestrator.isWsConnected.value)
        }
    }
    ```

    **3. BaseUrlInterceptorTest.kt — 动态 URL 替换测试**

    创建 `src/test/java/.../interceptor/BaseUrlInterceptorTest.kt`：
    ```kotlin
    package com.drama.app.data.remote.interceptor

    import com.drama.app.data.local.ServerPreferences
    import okhttp3.OkHttpClient
    import okhttp3.Request
    okhttp3.mockwebserver.MockResponse
    import okhttp3.mockwebserver.MockWebServer
    import org.junit.Assert.*
    import org.junit.Before
    import org.junit.Test
    import org.mockito.kotlin.mock
    import org.mockito.kotlin.whenever

    class BaseUrlInterceptorTest {

        private lateinit var interceptor: BaseUrlInterceptor
        private val mockServerPreferences: ServerPreferences = mock()

        @Before
        fun setup() {
            interceptor = BaseUrlInterceptor(mockServerPreferences)
        }

        @Test
        fun `replaces scheme host and port with current BaseUrl`() {
            whenever(mockServerPreferences.currentApiBaseUrl())
                .thenReturn("http://192.168.1.100:8000/api/v1/")

            val server = MockWebServer()
            server.start()
            server.enqueue(MockResponse().setBody("ok"))

            val client = OkHttpClient.Builder()
                .addInterceptor(interceptor)
                .build()

            val request = Request.Builder()
                .url("http://localhost:9999/api/v1/drama/status")
                .build()

            client.newCall(request).execute()

            val recorded = server.takeRequest()
            assertEquals("192.168.1.100", recorded.requestUrl?.host)
            assertEquals(8000, recorded.requestUrl?.port)
            assertEquals("/api/v1/drama/status", recorded.path)
            server.shutdown()
        }

        @Test
        fun `preserves path and query parameters`() {
            whenever(mockServerPreferences.currentApiBaseUrl())
                .thenReturn("http://10.0.2.2:8000/api/v1/")

            val server = MockWebServer()
            server.start()
            server.enqueue(MockResponse().setBody("ok"))

            val client = OkHttpClient.Builder()
                .addInterceptor(interceptor)
                .build()

            val request = Request.Builder()
                .url("http://placeholder/api/v1/drama/start?theme=horror")
                .build()

            client.newCall(request).execute()

            val recorded = server.takeRequest()
            assertEquals("/api/v1/drama/start", recorded.path?.substringBefore("?"))
            assertEquals("horror", recorded.requestUrl?.queryParameter("theme"))
            server.shutdown()
        }

        @Test
        fun `proceeds with original request when BaseUrl is invalid`() {
            whenever(mockServerPreferences.currentApiBaseUrl())
                .thenReturn("not-a-valid-url")

            val server = MockWebServer()
            server.start()
            server.enqueue(MockResponse().setBody("ok"))

            val client = OkHttpClient.Builder()
                .addInterceptor(interceptor)
                .build()

            // When BaseUrl is invalid, the interceptor falls through to original URL
            val request = Request.Builder()
                .url(server.url("/api/v1/test"))
                .build()

            val response = client.newCall(request).execute()
            assertTrue(response.isSuccessful)
            server.shutdown()
        }
    }
    ```
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama/android && grep '@Test' app/src/test/java/com/drama/app/ui/screens/dramadetail/orchestrator/ConnectionOrchestratorTest.kt | wc -l && grep '@Test' app/src/test/java/com/drama/app/data/remote/interceptor/BaseUrlInterceptorTest.kt | wc -l && grep 'TestWatcher' app/src/test/java/com/drama/app/MainDispatcherRule.kt</automated>
  </verify>
  <done>
    - MainDispatcherRule 共享协程测试 Rule（per D-23-12）
    - ConnectionOrchestratorTest 覆盖：初始状态、isWsConnected 同步、connect/disconnect/cleanup（per ARCH-02）
    - BaseUrlInterceptorTest 覆盖：URL 替换、路径保留、无效 URL 降级（per ARCH-04）
  </done>
</task>

<task type="manual" optional="true">
  <name>Task 2 (OPTIONAL P2): 可配置 RetryPolicy + Room 离线缓存</name>
  <files>
    android/app/src/main/java/com/drama/app/data/remote/retry/RetryPolicy.kt
    android/app/src/main/java/com/drama/app/data/local/database/DramaDatabase.kt
    android/app/src/main/java/com/drama/app/data/local/database/SceneBubbleDao.kt
  </files>
  <action>
    **P2-3: 可配置 RetryPolicy**

    创建 `data/remote/retry/RetryPolicy.kt`：
    - 定义 `RetryPolicy` 接口：`maxRetries`, `initialDelayMs`, `maxDelayMs`, `backoffMultiplier`
    - 提供 `DefaultRetryPolicy` 和 `ExponentialBackoffRetryPolicy` 实现
    - 在 `NetworkModule` 中注入，供 WebSocketManager 重连使用

    **P2-5: Room 离线缓存**

    创建 `data/local/database/DramaDatabase.kt`：
    - 定义 `SceneBubbleEntity` + `SceneBubbleDao`（insert/query:byDramaId/clear）
    - `DramaDatabase` @Database 注解，version 1
    - Repository 装饰器模式：在线时先写缓存再返回，离线时从缓存读取
    - DataStore → Room 迁移（schema 变更时处理）

    **注意：** 这些 P2 项目影响范围大，需要独立验证。建议 v3.0 milestone 再实现。
  </action>
  <done>
    - RetryPolicy 接口和默认实现
    - Room Database + DAO + Entity
    - Repository 装饰器模式缓存层
  </done>
</task>

<task type="manual" optional="true">
  <name>Task 3 (OPTIONAL P3): 图标库优化 + CI/CD</name>
  <files>
    android/app/build.gradle.kts
    .github/workflows/android.yml
  </files>
  <action>
    **P3-1: 图标库按需引入**

    当前项目使用 `material-icons-extended`（~2MB APK 增量），仅使用了约 20 个图标。
    - 方案 A：替换为精确的 `material-icons-filled` + `material-icons-outlined` 子库
    - 方案 B：将使用的图标复制为 `ImageVector` 常量，完全移除 `material-icons-extended` 依赖
    - R8 shrinkResources 已启用，未使用图标应被 tree-shaking 移除。建议先验证 Release APK 中图标库实际占比，再决定是否优化。

    **P3-3: CI/CD (GitHub Actions)**

    创建 `.github/workflows/android.yml`：
    - PR 触发：`./gradlew :app:compileDebugKotlin :app:testDebugUnitTest`
    - Push to main：`./gradlew :app:assembleRelease` + APK artifact upload
    - 缓存 Gradle wrapper 和 dependencies

    **注意：** P3 项目为锦上添花，不影响功能和稳定性。R8 shrinkResources 可能已自动移除未使用的图标。
  </action>
  <done>
    - 图标库按需引入评估或替换
    - GitHub Actions CI/CD workflow
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RetryPolicy → 网络重试 | 重试策略可能放大服务器负载（DDoS 效应） |
| Room 本地缓存 → 数据新鲜度 | 离线缓存可能导致用户看到过期数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-12 | D (Denial of Service) | RetryPolicy 指数退避 | mitigate | maxRetries 上限 + maxDelayMs 上限 + jitter |
| T-23-13 | I (Information Disclosure) | Room 本地缓存 | accept | 缓存为公开数据（戏剧内容），无敏感信息 |
</threat_model>

<verification>
1. 编译检查：`cd /workspace/director-actor-drama/android && ./gradlew :app:compileDebugKotlin` 无错误
2. 测试运行：`./gradlew :app:testDebugUnitTest` 通过
3. grep 验证：
   - ConnectionOrchestratorTest 包含 isWsConnected 测试
   - BaseUrlInterceptorTest 包含 URL 替换测试
   - MainDispatcherRule 包含 TestWatcher
4. 全部 ARCH-01~ARCH-17 验证覆盖
</verification>

<success_criteria>
## 必做项（测试补全）
- ConnectionOrchestrator 单元测试覆盖 WS 连接/降级行为（per ARCH-02）
- BaseUrlInterceptor 单元测试覆盖动态 URL 替换（per ARCH-04）
- MainDispatcherRule 共享测试基础设施（per D-23-12）
- Phase 23 全部 17 个 ARCH 需求有对应测试或验证

## 可选 P2/P3（deferred per D-23-18）
- RetryPolicy 可配置重试策略
- Room 离线缓存
- 图标库按需引入
- CI/CD GitHub Actions
</success_criteria>

<output>
After completion, create `.planning/phases/23-android-tech-debt/23-03-SUMMARY.md`
</output>
