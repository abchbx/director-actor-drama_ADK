---
phase: 11-timeline-tracking
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - app/tools.py
  - app/context_builder.py
  - app/agent.py
  - app/coherence_checker.py
  - app/state_manager.py
  - tests/unit/test_timeline_integration.py
autonomous: true
requirements:
  - COHERENCE-05

must_haves:
  truths:
    - "导演可通过 advance_time() Tool 推进时间线，工具是 timeline_tracker.advance_time_logic 的薄代理"
    - "导演可通过 detect_timeline_jump() Tool 触发跳跃检测"
    - "add_fact() Tool 接受可选 time_context 参数，存入事实 dict"
    - "导演上下文包含【时间线】段落，展示当前时间、累计天数、跳跃检测和时间脉络"
    - "演员上下文包含【当前时间】段落，简洁一行展示当前时间"
    - "导演 prompt 包含 §12 时间线管理段落"
    - "validate_consistency_prompt 包含时序检查指令"
    - "write_scene 自动记录 time_label 字段"
  artifacts:
    - path: "app/tools.py"
      provides: "2 个新 Tool 函数 + add_fact 扩展"
      exports: ["advance_time", "detect_timeline_jump"]
    - path: "app/context_builder.py"
      provides: "_build_timeline_section + 演员时间段落 + priorities 更新"
      contains: "_build_timeline_section"
    - path: "app/agent.py"
      provides: "STRATEGY §12 + tools 注册"
      contains: "§12"
    - path: "app/coherence_checker.py"
      provides: "validate_consistency_prompt 时序检查增强"
      contains: "时序"
    - path: "tests/unit/test_timeline_integration.py"
      provides: "集成测试"
      contains: "TestTimelineIntegration"
  key_links:
    - from: "app/tools.py"
      to: "app/timeline_tracker.py"
      via: "薄代理调用 _logic 函数"
      pattern: "from .timeline_tracker import"
    - from: "app/context_builder.py"
      to: "app/timeline_tracker.py"
      via: "_build_time脉络 构建【时间线】段落"
      pattern: "from .timeline_tracker import"
    - from: "app/agent.py"
      to: "app/tools.py"
      via: "tools 列表注册 advance_time, detect_timeline_jump"
      pattern: "advance_time|detect_timeline_jump"
---

<objective>
集成 timeline_tracker 到导演/演员上下文、Tool 函数、agent prompt 和 coherence_checker。实现 D-31/D-32/D-33 的 Tool 函数，D-20/D-21/D-22/D-23 的上下文段落，D-24/D-25 的导演 prompt §12，D-16/D-17/D-18 的 coherence_checker 扩展，以及 D-08 的 write_scene time_label。

Purpose: 将 Plan 01 的纯函数接入完整的导演-演员工作流，让时间线追踪在每场戏中实际运作。
Output: 集成到 tools.py、context_builder.py、agent.py、coherence_checker.py、state_manager.py，加集成测试
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-timeline-tracking/11-CONTEXT.md
@.planning/phases/11-timeline-tracking/11-01-SUMMARY.md

<interfaces>
<!-- Plan 01 产出 — executor 必须使用这些接口 -->

From app/timeline_tracker.py (Plan 01 产出):
```python
TIME_PERIODS = ["清晨", "上午", "中午", "下午", "黄昏", "夜晚", "深夜"]
TIMELINE_JUMP_THRESHOLDS = {"minor": 1, "significant": 3}
MAX_TIME_PERIODS = 20

def advance_time_logic(
    state: dict, time_description: str,
    day: int | None = None, period: str | None = None,
    flashback: bool = False,
) -> dict:
    # 返回 {"status": "success"/"info", "message": ..., "timeline": ...}

def detect_timeline_jump_logic(state: dict) -> dict:
    # 返回 {"status": "success", "jumps": [...], "max_gap": N}

def parse_time_description(text: str) -> dict:
    # 返回 {"day": int|None, "period": str|None}

def _build_time脉络(state: dict) -> str:
    # 返回格式化的时间脉络字符串
```

From app/coherence_checker.py — 需要扩展的函数:
```python
def add_fact_logic(fact: str, category: str, importance: str, state: dict) -> dict:
    # Phase 11 扩展: 新增 time_context 可选参数

def validate_consistency_prompt(facts: list[dict], recent_scenes: list[dict]) -> str:
    # Phase 11 扩展: 新增时序检查指令
```

From app/tools.py — 需要扩展的 Tool 函数:
```python
def add_fact(fact, category, importance, *, tool_context) -> dict:
    # Phase 11 扩展: 新增 time_context 参数

def write_scene(scene_number, scene_title, scene_description, dialogue_content, tool_context) -> dict:
    # Phase 11 扩展: 自动记录 time_label
```

From app/context_builder.py — 需要新增的段落:
```python
_DIRECTOR_SECTION_PRIORITIES = {
    # ... existing ...
    # Phase 11 新增: "timeline": 5
}

_ACTOR_SECTION_PRIORITIES = {
    # ... existing ...
    # Phase 11 新增: "current_time": 6
}

def build_director_context(tool_context, token_budget) -> str:
    # Phase 11 新增: _build_timeline_section(state) 插入 sections 列表

def build_actor_context_from_memory(actor_name, tool_context, token_budget) -> str:
    # Phase 11 新增: 【当前时间】段落
```

From app/agent.py — STRATEGY 层模式:
```python
_INSTRUCTION_STRATEGY = """
## §8 张力评估与冲突注入
...
## §11 一致性保障
...
"""
# Phase 11 新增 §12
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tool functions (advance_time, detect_timeline_jump) + add_fact time_context extension + write_scene time_label</name>
  <files>app/tools.py, app/coherence_checker.py, app/state_manager.py</files>
  <read_first>
    app/tools.py
    app/coherence_checker.py
    app/state_manager.py
    app/timeline_tracker.py
  </read_first>
  <action>
1. **Add import to `app/tools.py`** — at the existing coherence_checker import block, add:
```python
from .timeline_tracker import advance_time_logic, detect_timeline_jump_logic
```

2. **Add `advance_time` Tool function (D-31/D-07):**
```python
def advance_time(
    time_description: str,
    day: int | None = None,
    period: str | None = None,
    flashback: bool = False,
    tool_context: ToolContext,
) -> dict:
    """Advance the drama timeline. Use when scene time changes.

    推进时间线（D-31/D-07）。导演声明时间推进——换天、换时段、闪回时调用。

    Args:
        time_description: Full descriptive time, e.g. "第三天黄昏".
        day: Day number (optional, parsed from time_description if omitted).
        period: Time period (optional, must be one of: 清晨/上午/中午/下午/黄昏/夜晚/深夜).
        flashback: Set True for flashback scenes.

    Returns:
        dict with status, message, and timeline info.
    """
    state = _get_state(tool_context)
    state.setdefault("timeline", {
        "current_time": "第一天",
        "days_elapsed": 1,
        "current_period": None,
        "time_periods": [],
        "last_jump_check": None,
    })

    result = advance_time_logic(state, time_description, day, period, flashback)
    if result["status"] in ("success", "info"):
        _set_state(state, tool_context)
        return {
            "status": result["status"],
            "message": f"⏰ 时间推进至：{time_description}",
            "timeline": result.get("timeline", state["timeline"]),
        }
    return result
```

3. **Add `detect_timeline_jump` Tool function (D-32):**
```python
def detect_timeline_jump(tool_context: ToolContext) -> dict:
    """Detect timeline jumps between scenes.

    检测场景时间跳跃（D-32）。对比 time_periods 中相邻条目的天数差，
    返回跳跃检测结果和建议。也可由 validate_consistency 自动触发。

    Args:
        tool_context: Tool context for state access.

    Returns:
        dict with status, jumps, max_gap.
    """
    state = _get_state(tool_context)
    state.setdefault("timeline", {
        "current_time": "第一天",
        "days_elapsed": 1,
        "current_period": None,
        "time_periods": [],
        "last_jump_check": None,
    })

    result = detect_timeline_jump_logic(state)
    if result["status"] == "success":
        state["timeline"]["last_jump_check"] = result
        _set_state(state, tool_context)
    return result
```

4. **Extend `add_fact` in `app/tools.py` (D-33/D-18):**
Change the function signature to add `time_context`:
```python
def add_fact(
    fact: str,
    category: str = "event",
    importance: str = "medium",
    time_context: str | None = None,
    *,
    tool_context: ToolContext,
) -> dict:
```
After calling `add_fact_logic`, if `time_context` is not None, add it to the fact object:
```python
    result = add_fact_logic(fact, category, importance, state)
    if result["status"] == "success":
        if time_context is not None:
            # Add time_context to the newly added fact (D-18/D-19)
            established_facts = state["established_facts"]
            for f in established_facts:
                if f.get("id") == result["fact_id"]:
                    f["time_context"] = time_context
                    break
        _set_state(state, tool_context)
        return {
            "status": "success",
            "fact_id": result["fact_id"],
            "message": f"📌 事实已记录：{fact}",
        }
    return result
```
Also update the docstring to document the new `time_context` parameter.

5. **Extend `write_scene` in `app/tools.py` (D-08):**
After `result = update_script(...)`, before the formatted_scene build, add time_label:
```python
    # Phase 11: Auto-record time_label in scene data (D-08)
    state = _get_state(tool_context)
    timeline = state.get("timeline", {})
    current_time = timeline.get("current_time", "")
    if current_time:
        # Update the scene dict with time_label
        scenes = state.get("scenes", [])
        for s in scenes:
            if s.get("scene_number") == scene_number:
                s["time_label"] = current_time
                break
        _set_state(state, tool_context)
```

6. **Extend `validate_consistency_prompt` in `app/coherence_checker.py` (D-17):**
After the existing "非矛盾" line (around line 314), add temporal check instruction:
```python
    # Phase 11: Temporal consistency check instruction (D-17)
    sections.append(
        "检查事件时序：事实中标记了 time_context 的，其因果顺序应与时间线一致。"
        "如果事实 A 发生在事实 B 之后但 time_context 更早，这是时序矛盾。"
    )
```

7. **Extend `add_fact_logic` in `app/coherence_checker.py` (D-18):**
The pure function should accept time_context but since the Tool handles adding it to the fact dict (step 4 above), the _logic function doesn't need a signature change. The time_context is added to the fact dict post-creation in the Tool layer. This is consistent with how repair_contradiction_logic works — the logic function doesn't handle repair_note either, it's added separately.
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && python -c "from app.tools import advance_time, detect_timeline_jump; print('Tool imports OK')" && python -m pytest tests/unit/test_timeline_integration.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `app/tools.py` contains `def advance_time(time_description, day=None, period=None, flashback=False, tool_context)`
    - `app/tools.py` contains `def detect_timeline_jump(tool_context)`
    - `app/tools.py` add_fact signature includes `time_context: str | None = None`
    - `app/tools.py` write_scene contains `time_label` assignment
    - `app/coherence_checker.py` validate_consistency_prompt contains "时序矛盾" string
    - `grep "from .timeline_tracker import" app/tools.py` returns match
    - advance_time returns dict with "status" and "message" keys
    - detect_timeline_jump returns dict with "status", "jumps", "max_gap" keys
  </acceptance_criteria>
  <done>advance_time and detect_timeline_jump Tool functions registered. add_fact extended with time_context. write_scene auto-records time_label. validate_consistency_prompt enhanced with temporal check. All changes verified.</done>
</task>

<task type="auto">
  <name>Task 2: context_builder timeline section + actor time paragraph + agent §12 prompt + tools registration + integration tests</name>
  <files>app/context_builder.py, app/agent.py, tests/unit/test_timeline_integration.py</files>
  <read_first>
    app/context_builder.py
    app/agent.py
    app/timeline_tracker.py
    app/tools.py
  </read_first>
  <action>
1. **Add import to `app/context_builder.py`:**
```python
from .timeline_tracker import _build_time脉络, TIME_PERIODS
```

2. **Add `_build_timeline_section(state)` to `app/context_builder.py` (D-20/D-23):**
Add this new function after `_build_facts_section` (around line 1004). Follow the exact same return format as other section builders:
```python
def _build_timeline_section(state: dict) -> dict:
    """Build the timeline section for director context (D-20/D-23).

    导演上下文【时间线】段落——展示当前时间、累计天数、场景覆盖、
    最近跳跃检测结果和时间脉络。

    Args:
        state: The drama state dict.

    Returns:
        Section dict for timeline.
    """
    timeline = state.get("timeline")
    if not timeline:
        return {"key": "timeline", "text": "", "priority": _DIRECTOR_SECTION_PRIORITIES["timeline"], "truncatable": True}

    current_time = timeline.get("current_time", "")
    days_elapsed = timeline.get("days_elapsed", 1)
    time_periods = timeline.get("time_periods", [])
    last_jump_check = timeline.get("last_jump_check")

    # Calculate scene coverage
    if time_periods:
        first_scene = time_periods[0].get("scene_range", [1])[0]
        last_scene = time_periods[-1].get("scene_range", [1])[-1]
        scene_coverage = f"第{first_scene}场～第{last_scene}场"
    else:
        scene_coverage = "尚未记录"

    lines = []
    # Header line (D-20)
    lines.append(f"当前：{current_time} | 累计：{days_elapsed} 天 | 场景覆盖：{scene_coverage}")

    # Jump detection alert (D-20)
    if last_jump_check and last_jump_check.get("jumps"):
        significant_jumps = [j for j in last_jump_check["jumps"] if j.get("severity") == "significant"]
        if significant_jumps:
            j = significant_jumps[-1]
            lines.append(
                f"💡 最近跳跃：第{j['from_scene']}场→第{j['to_scene']}场"
                f"跨{j['day_gap']}天（显著跳跃），建议补充时间过渡"
            )
        elif last_jump_check["jumps"]:
            j = last_jump_check["jumps"][-1]
            lines.append(
                f"💡 最近跳跃：第{j['from_scene']}场→第{j['to_scene']}场"
                f"跨{j['day_gap']}天（轻微跳跃）"
            )

    # Time lineage (D-20)
    time_脉络 = _build_time脉络(state)
    if time_脉络:
        lines.append(f"时间脉络：\n{time_脉络}")

    text = "【时间线】\n" + "\n".join(lines) if lines else ""
    return {
        "key": "timeline",
        "text": text,
        "priority": _DIRECTOR_SECTION_PRIORITIES["timeline"],
        "truncatable": True,
    }
```

3. **Update `_DIRECTOR_SECTION_PRIORITIES` in `app/context_builder.py` (D-21):**
Add `"timeline": 5` after `"facts": 5`:
```python
    "facts": 5,
    "timeline": 5,     # Phase 11 (D-21)
    "tension": 5,
```

4. **Update `_ACTOR_SECTION_PRIORITIES` in `app/context_builder.py` (D-22):**
Add `"current_time": 6`:
```python
    "anchor": 6,
    "current_time": 6,  # Phase 11 (D-22) — time info constrains behavior
    "actor_dna": 7,
```

5. **Add 【当前时间】paragraph to `_assemble_actor_sections` (D-22):**
After the emotion section (around line 306), add:
```python
    # Phase 11: Current time for actor (D-22)
    state_ref = _get_state(tool_context)
    timeline = state_ref.get("timeline", {})
    current_time = timeline.get("current_time", "")
    if current_time:
        sections.append({
            "key": "current_time",
            "text": f"【当前时间】{current_time}",
            "priority": _ACTOR_SECTION_PRIORITIES["current_time"],
            "truncatable": False,
        })
```

6. **Insert `_build_timeline_section` into `build_director_context` sections list (D-23):**
After `_build_facts_section(state)`, add:
```python
        _build_timeline_section(state),               # priority 5 — Phase 11
```

7. **Add §12 to `_INSTRUCTION_STRATEGY` in `app/agent.py` (D-24/D-25):**
After §11 一致性保障 block, add:
```python
## §12 时间线管理
- 场景时间变化时调用 advance_time() 声明时间（换天、换时段、闪回）
- 【时间线】段落显示跳跃检测提醒时，考虑用旁白补充时间过渡
- 关键事件建议在 add_fact() 时附带 time_context 参数记录时间上下文
- 时间线与已确立事实交叉验证——事件因果顺序必须与时间线一致
```

8. **Register tools in `_improv_director` Agent (D-31/D-32):**
After `repair_contradiction,    # Phase 10`, add:
```python
        advance_time,             # Phase 11
        detect_timeline_jump,     # Phase 11
```

9. **Create `tests/unit/test_timeline_integration.py`:**
```python
"""Integration tests for timeline tracking with tools, context_builder, agent, and coherence_checker.

Tests for Phase 11 integration (COHERENCE-05).
"""
import pytest
from app.timeline_tracker import advance_time_logic, detect_timeline_jump_logic, TIME_PERIODS, _build_time脉络
from app.coherence_checker import validate_consistency_prompt


def _make_state(**overrides) -> dict:
    """Create a minimal state dict for timeline integration tests."""
    state = {
        "current_scene": 5,
        "theme": "测试戏剧",
        "status": "acting",
        "actors": {"朱棣": {"role": "燕王", "emotions": "neutral", "personality": "果断"}},
        "established_facts": [],
        "coherence_checks": {
            "last_check_scene": 0, "last_result": None,
            "check_history": [], "total_contradictions": 0,
        },
        "scenes": [
            {"scene_number": 3, "content": "朱棣在府中", "actors_present": ["朱棣"]},
            {"scene_number": 4, "content": "朱棣起兵", "actors_present": ["朱棣"]},
        ],
        "timeline": {
            "current_time": "第一天",
            "days_elapsed": 1,
            "current_period": None,
            "time_periods": [],
            "last_jump_check": None,
        },
    }
    state.update(overrides)
    return state


class TestTimelineIntegration:
    """Integration tests for timeline + coherence."""

    def test_advance_time_updates_timeline_state(self):
        state = _make_state()
        result = advance_time_logic(state, "第三天黄昏", day=3, period="黄昏")
        assert result["status"] == "success"
        assert state["timeline"]["current_time"] == "第三天黄昏"
        assert state["timeline"]["days_elapsed"] == 3
        assert state["timeline"]["current_period"] == "黄昏"
        assert len(state["timeline"]["time_periods"]) == 1

    def test_advance_time_auto_detects_jump(self):
        state = _make_state()
        advance_time_logic(state, "第一天", day=1, period="清晨")
        advance_time_logic(state, "第五天", day=5, period="上午")
        assert state["timeline"]["last_jump_check"] is not None
        assert state["timeline"]["last_jump_check"]["max_gap"] == 4

    def test_validate_consistency_prompt_includes_temporal(self):
        facts = [{"id": "f1", "fact": "朱棣起兵", "category": "event", "actors": ["朱棣"], "time_context": "第三天黄昏"}]
        scenes = [{"scene_number": 5, "content": "朱棣还在犹豫"}]
        prompt = validate_consistency_prompt(facts, scenes)
        assert "时序" in prompt

    def test_build_time脉络_with_multiple_periods(self):
        state = _make_state()
        advance_time_logic(state, "第一天清晨", day=1, period="清晨")
        advance_time_logic(state, "第一天夜晚", day=1, period="夜晚")
        advance_time_logic(state, "第三天黄昏", day=3, period="黄昏")
        result = _build_time脉络(state)
        assert "第一天" in result
        assert "第三天" in result

    def test_fact_with_time_context(self):
        """Test that time_context field is stored on facts."""
        from app.coherence_checker import add_fact_logic
        state = _make_state()
        result = add_fact_logic("朱棣已起兵", "event", "high", state)
        assert result["status"] == "success"
        # time_context is added by the Tool layer, not the _logic function
        # So we verify the fact dict can hold it
        fact = result["fact"]
        fact["time_context"] = "第三天黄昏"
        assert fact["time_context"] == "第三天黄昏"


class TestTimelineContextBuilder:
    """Test timeline section formatting."""

    def test_timeline_section_shows_current_time(self):
        from app.context_builder import _build_timeline_section
        state = _make_state()
        section = _build_timeline_section(state)
        assert section["key"] == "timeline"
        assert "第一天" in section["text"]

    def test_timeline_section_shows_jump_alert(self):
        from app.context_builder import _build_timeline_section
        state = _make_state()
        advance_time_logic(state, "第一天", day=1, period="清晨")
        advance_time_logic(state, "第五天", day=5, period="上午")
        section = _build_timeline_section(state)
        assert "显著跳跃" in section["text"] or "轻微跳跃" in section["text"] or "跳跃" in section["text"]

    def test_timeline_priority_is_5(self):
        from app.context_builder import _DIRECTOR_SECTION_PRIORITIES
        assert _DIRECTOR_SECTION_PRIORITIES.get("timeline") == 5

    def test_actor_current_time_priority_is_6(self):
        from app.context_builder import _ACTOR_SECTION_PRIORITIES
        assert _ACTOR_SECTION_PRIORITIES.get("current_time") == 6
```
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && python -m pytest tests/unit/test_timeline_integration.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `app/context_builder.py` contains `def _build_timeline_section(state: dict) -> dict`
    - `app/context_builder.py` _DIRECTOR_SECTION_PRIORITIES contains `"timeline": 5`
    - `app/context_builder.py` _ACTOR_SECTION_PRIORITIES contains `"current_time": 6`
    - `app/context_builder.py` build_director_context sections list contains `_build_timeline_section(state)`
    - `app/context_builder.py` _assemble_actor_sections contains `current_time` section
    - `app/agent.py` _INSTRUCTION_STRATEGY contains `§12 时间线管理`
    - `app/agent.py` _INSTRUCTION_STRATEGY contains `advance_time()`
    - `app/agent.py` _improv_director tools list contains `advance_time` and `detect_timeline_jump`
    - `grep "from .timeline_tracker import" app/context_builder.py` returns match
    - `_build_timeline_section` output contains `【时间线】`
    - Actor `current_time` section format is `【当前时间】{time}`
    - All integration tests pass
  </acceptance_criteria>
  <done>context_builder has _build_timeline_section and actor time paragraph. agent.py has §12 and tools registered. coherence_checker prompt enhanced. Integration tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Director LLM → advance_time() | LLM may call advance_time with unexpected time_description values |
| Director LLM → add_fact() time_context | LLM may provide incorrect time_context values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Spoofing | advance_time time_description | accept | Parse failures handled gracefully — updates current_time string but warns |
| T-11-04 | Tampering | add_fact time_context | accept | Optional field, no validation against timeline — LLM is trusted for creative decisions |
| T-11-05 | Information Disclosure | Actor context time | accept | Actor sees current time only, no sensitive timeline details exposed |
</threat_model>

<verification>
1. `python -m pytest tests/unit/test_timeline_tracker.py tests/unit/test_timeline_integration.py -v` — all tests pass
2. `grep "§12" app/agent.py` — §12 exists in STRATEGY
3. `grep "advance_time" app/agent.py` — tool registered in _improv_director
4. `grep "_build_timeline_section" app/context_builder.py` — section builder exists
5. `grep "时序" app/coherence_checker.py` — temporal check in prompt
6. `python -c "from app.context_builder import _build_timeline_section; print('OK')"` — import works
7. `python -c "from app.tools import advance_time, detect_timeline_jump; print('OK')"` — tool imports work
</verification>

<success_criteria>
1. advance_time() and detect_timeline_jump() Tool functions available in _improv_director
2. add_fact() accepts optional time_context parameter (D-33/D-18)
3. write_scene() auto-records time_label field (D-08)
4. Director context includes 【时间线】section with current time, days elapsed, jump alerts, time lineage (D-20)
5. Actor context includes 【当前时间】paragraph (D-22)
6. timeline section has priority 5 in _DIRECTOR_SECTION_PRIORITIES (D-21)
7. current_time has priority 6 in _ACTOR_SECTION_PRIORITIES (D-22)
8. Director prompt §12 时间线管理 exists with advance_time guidance (D-24)
9. validate_consistency_prompt includes temporal consistency instruction (D-17)
10. All integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-timeline-tracking/11-02-SUMMARY.md`
</output>
