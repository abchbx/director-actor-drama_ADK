---
wave: 2
depends_on:
  - 08-01
files_modified:
  - app/tools.py
  - app/state_manager.py
  - app/context_builder.py
  - app/agent.py
requirements:
  - DSTORM-01
  - DSTORM-02
  - DSTORM-03
autonomous: true
---

# Plan 08-02: Integration Layer — Tools, State, Context Builder, Agent Prompt

## Objective

Integrate Dynamic STORM into the existing system: upgrade `trigger_storm()` →
`dynamic_storm()` in tools.py, extend `evaluate_tension()`, add state initialization
and backward compatibility, implement `_build_dynamic_storm_section()`, update director
prompt with §5 revision and new §10.

## must_haves

- [ ] `dynamic_storm(focus_area, tool_context)` tool function works end-to-end
- [ ] `trigger_storm` preserved as backward-compat alias
- [ ] `evaluate_tension()` returns "dynamic_storm" in suggested_action when appropriate
- [ ] `state["dynamic_storm"]` initialized in `init_drama_state()` and compatible in `load_progress()`
- [ ] `advance_scene()` increments `scenes_since_last_storm`
- [ ] `_build_dynamic_storm_section()` fully implemented
- [ ] Director prompt §5 updated and §10 added
- [ ] `dynamic_storm` registered in `_improv_director` tools

---

<task id="1">

<title>Upgrade trigger_storm() to dynamic_storm() and extend evaluate_tension()</title>

<read_first>
- app/tools.py (L764-792 trigger_storm, L795-835 evaluate_tension, L838-870 inject_conflict)
- app/dynamic_storm.py (core module from plan 08-01)
- .planning/phases/08-dynamic-storm/08-CONTEXT.md (D-24 through D-31, D-09)
- app/memory_manager.py (L148-190 _call_llm async pattern)
</read_first>

<acceptance_criteria>
- `app/tools.py` contains `async def dynamic_storm(focus_area: str, tool_context: ToolContext) -> dict` function
- `dynamic_storm()` calls `discover_perspectives_prompt()` from dynamic_storm module
- `dynamic_storm()` calls `_call_llm()` for LLM perspective generation
- `dynamic_storm()` calls `parse_llm_perspectives()` to parse response
- `dynamic_storm()` calls `check_keyword_overlap()` for each new perspective
- `dynamic_storm()` calls `suggest_conflict_types()` for each new perspective
- `dynamic_storm()` merges new perspectives into `state["storm"]["perspectives"]` with `source: "dynamic_storm"` and `discovered_scene`
- `dynamic_storm()` calls `update_dynamic_storm_state()` and persists state
- `dynamic_storm()` returns dict with: `status`, `message`, `focus_area`, `new_perspectives`, `suggested_conflict_types`, `overlap_warnings`, `scenes_since_last`
- `app/tools.py` contains `def trigger_storm(focus_area: str, tool_context: ToolContext) -> dict` that wraps `dynamic_storm()` (backward compat alias per D-24/D-25)
- `trigger_storm` is NOT async — it calls `dynamic_storm` synchronously (or wraps with asyncio.run if needed)
- `evaluate_tension()` return dict contains `suggested_action` that can include "dynamic_storm"
- When `state["dynamic_storm"]["scenes_since_last_storm"] >= STORM_INTERVAL`, `evaluate_tension()` appends "dynamic_storm" to suggested_action
- When `state["conflict_engine"]["consecutive_low_tension"] >= 3`, `evaluate_tension()` also appends "dynamic_storm"
- `evaluate_tension()` return dict includes new field `suggested_storm_focus: str | None`
- `dynamic_storm` is added to tools.py import of dynamic_storm module
</acceptance_criteria>

<action>
Modify `/workspace/director-actor-drama/app/tools.py`:

1. **Add import** at top of file (near other app imports):
```python
from .dynamic_storm import (
    STORM_INTERVAL,
    check_keyword_overlap,
    discover_perspectives_prompt,
    parse_llm_perspectives,
    suggest_conflict_types,
    update_dynamic_storm_state,
)
```

2. **Replace `trigger_storm()` function (L764-792)** with new `dynamic_storm()` async function:
```python
async def dynamic_storm(focus_area: str, tool_context: ToolContext) -> dict:
    """Trigger Dynamic STORM to discover new perspectives from current drama.

    触发动态STORM——从当前剧情中挖掘未探索角度，生成1-2个新视角。
    新视角与已有视角去重，受已确立事实约束，结果合并入storm数据。

    Args:
        focus_area: Optional area to focus the discovery on (e.g., "角色关系", "权力斗争").

    Returns:
        dict with status, message, focus_area, new_perspectives, suggested_conflict_types,
        overlap_warnings, scenes_since_last.
    """
    state = _get_state(tool_context)
    
    # Ensure storm sub-dict exists
    if "storm" not in state:
        state["storm"] = {"last_review": {}}
    if "perspectives" not in state.get("storm", {}):
        state["storm"]["perspectives"] = []
    
    # Build prompt
    prompt = discover_perspectives_prompt(state, focus_area)
    
    # Call LLM for perspective generation
    try:
        from .memory_manager import _call_llm
        response_text = await _call_llm(prompt)
    except Exception as e:
        return {
            "status": "error",
            "message": f"❌ Dynamic STORM LLM调用失败：{e}",
            "focus_area": focus_area,
        }
    
    # Parse LLM response
    new_perspectives = parse_llm_perspectives(response_text)
    
    if not new_perspectives:
        # Still record the trigger attempt
        ds_state = update_dynamic_storm_state(state, trigger_type="auto", focus_area=focus_area, perspectives_found=0)
        state["dynamic_storm"] = ds_state
        _set_state(state, tool_context)
        return {
            "status": "success",
            "message": "🔍 Dynamic STORM 触发，但未发现新视角。可能当前剧情已有充分探索。",
            "focus_area": focus_area,
            "new_perspectives": [],
            "suggested_conflict_types": [],
            "overlap_warnings": [],
            "scenes_since_last": state.get("dynamic_storm", {}).get("scenes_since_last_storm", 0),
        }
    
    # Check overlap for each new perspective
    existing_names = [p.get("name", "") for p in state["storm"].get("perspectives", [])]
    overlap_warnings = []
    for p in new_perspectives:
        result = check_keyword_overlap(p.get("name", ""), existing_names)
        if result.get("overlap_warning"):
            overlap_warnings.append(result["overlap_warning"])
    
    # Suggest conflict types for each new perspective
    all_suggested_types = []
    for p in new_perspectives:
        types = suggest_conflict_types(p.get("description", ""))
        all_suggested_types.extend(types)
    suggested_conflict_types = list(dict.fromkeys(all_suggested_types))  # dedup preserving order
    
    # Merge new perspectives into storm["perspectives"]
    current_scene = state.get("current_scene", 0)
    for p in new_perspectives:
        p["source"] = "dynamic_storm"
        p["discovered_scene"] = current_scene
    state["storm"]["perspectives"].extend(new_perspectives)
    
    # Update dynamic_storm state
    ds_state = update_dynamic_storm_state(
        state,
        trigger_type="auto",
        focus_area=focus_area,
        perspectives_found=len(new_perspectives),
        new_perspectives=new_perspectives,
    )
    state["dynamic_storm"] = ds_state
    
    _set_state(state, tool_context)
    
    return {
        "status": "success",
        "message": f"🔍 Dynamic STORM 触发！发现 {len(new_perspectives)} 个新视角",
        "focus_area": focus_area,
        "new_perspectives": new_perspectives,
        "suggested_conflict_types": suggested_conflict_types,
        "overlap_warnings": overlap_warnings,
        "scenes_since_last": 0,  # just triggered, so 0
    }
```

3. **Add backward-compat alias** after dynamic_storm:
```python
def trigger_storm(focus_area: str, tool_context: ToolContext) -> dict:
    """Backward-compatible alias for dynamic_storm() (D-24/D-25).

    trigger_storm() 的向后兼容别名，内部调用 dynamic_storm()。
    返回值格式兼容旧版，但新增 new_perspectives 等字段。
    """
    import asyncio
    try:
        loop = asyncio.get_event_loop()
        if loop.is_running():
            # We're inside an async context — create task
            import concurrent.futures
            with concurrent.futures.ThreadPoolExecutor() as pool:
                result = pool.submit(asyncio.run, dynamic_storm(focus_area, tool_context)).result()
            return result
        else:
            return loop.run_until_complete(dynamic_storm(focus_area, tool_context))
    except RuntimeError:
        return asyncio.run(dynamic_storm(focus_area, tool_context))
```

4. **Extend `evaluate_tension()` function** (after line 815 where `calculate_tension` is called, before the return):
   - After `updated_ce = update_conflict_engine_state(state, result)` and `state["conflict_engine"] = updated_ce`:
   - Read `scenes_since_last = state.get("dynamic_storm", {}).get("scenes_since_last_storm", 0)`
   - Read `consecutive_low = updated_ce.get("consecutive_low_tension", 0)`
   - If `scenes_since_last >= STORM_INTERVAL`: append `"dynamic_storm"` to a `suggested_actions` list
   - If `consecutive_low >= 3`: append `"dynamic_storm"` to suggested_actions list
   - Build `suggested_storm_focus` string: if tension is low, suggest "张力恢复"; if scenes_since_last >= STORM_INTERVAL, suggest "周期性视角刷新"
   - Update the return dict to include `suggested_action` (now potentially a string with multiple actions or the original single action + "dynamic_storm"), and add `suggested_storm_focus` field
   - For backward compat: if only one action, keep as string; if multiple, join with comma

</action>

</task>

<task id="2">

<title>Add dynamic_storm state initialization and advance_scene counter</title>

<read_first>
- app/state_manager.py (L321-373 init_drama_state, L447-530 load_progress, L866-883 advance_scene)
- app/dynamic_storm.py (_init_dynamic_storm_defaults function)
</read_first>

<acceptance_criteria>
- `init_drama_state()` contains `state["dynamic_storm"] = {"scenes_since_last_storm": 0, "trigger_history": [], "discovered_perspectives": []}`
- `load_progress()` contains `state.setdefault("dynamic_storm", {"scenes_since_last_storm": 0, "trigger_history": [], "discovered_perspectives": []})`
- `advance_scene()` contains code to increment `state["dynamic_storm"]["scenes_since_last_storm"]`
- `state_manager.py` imports `_init_dynamic_storm_defaults` from dynamic_storm (or inlines the defaults)
</acceptance_criteria>

<action>
Modify `/workspace/director-actor-drama/app/state_manager.py`:

1. **In `init_drama_state()`** (after Phase 7 `state["plot_threads"] = []` line, ~L360):
Add:
```python
    # Phase 8: Dynamic STORM fields (D-27/D-28)
    state["dynamic_storm"] = {
        "scenes_since_last_storm": 0,
        "trigger_history": [],
        "discovered_perspectives": [],
    }
```

2. **In `load_progress()`** (after Phase 7 `state.setdefault("plot_threads", [])` line, ~L519):
Add:
```python
    # Phase 8: Dynamic STORM backward compatibility (D-29)
    state.setdefault("dynamic_storm", {
        "scenes_since_last_storm": 0,
        "trigger_history": [],
        "discovered_perspectives": [],
    })
```

3. **In `advance_scene()`** (after `state["current_scene"] = state.get("current_scene", 0) + 1`, ~L873):
Add:
```python
    # Phase 8: Increment dynamic_storm counter (D-08/D-30)
    ds = state.setdefault("dynamic_storm", {})
    ds["scenes_since_last_storm"] = ds.get("scenes_since_last_storm", 0) + 1
```

</action>

</task>

<task id="3">

<title>Implement _build_dynamic_storm_section() fully</title>

<read_first>
- app/context_builder.py (L746-767 current empty shell of _build_dynamic_storm_section)
- .planning/phases/08-dynamic-storm/08-CONTEXT.md (D-32/D-33 specifics)
</read_first>

<acceptance_criteria>
- `_build_dynamic_storm_section()` returns formatted text when `dynamic_storm` exists
- Text includes "距上次视角发现" counter and "建议间隔：8 场"
- When `scenes_since_last_storm >= STORM_INTERVAL`, text shows "建议调用 dynamic_storm()"
- When `consecutive_low_tension >= 3`, text shows "张力持续低迷——强烈建议调用 dynamic_storm()"
- Most recent trigger_history entry shown as "最近发现" with scene number and focus area
- Returns empty text only when no dynamic_storm data at all
- Priority remains 3, truncatable remains True
</acceptance_criteria>

<action>
Replace the `_build_dynamic_storm_section()` function in `/workspace/director-actor-drama/app/context_builder.py` (L746-767) with:

```python
def _build_dynamic_storm_section(state: dict) -> dict:
    """Build the dynamic STORM section (D-32/D-33 fully implemented).

    构建 Dynamic STORM 上下文段落：距上次视角发现的场次数、建议间隔、
    最近发现摘要、张力低迷时强烈建议触发。

    Args:
        state: The drama state dict.

    Returns:
        Section dict for dynamic STORM.
    """
    from .dynamic_storm import STORM_INTERVAL

    dynamic_storm = state.get("dynamic_storm")
    if not dynamic_storm:
        return {"key": "dynamic_storm", "text": "", "priority": _DIRECTOR_SECTION_PRIORITIES["dynamic_storm"], "truncatable": True}

    lines = []
    scenes_since = dynamic_storm.get("scenes_since_last_storm", 0)
    lines.append(f"距上次视角发现：{scenes_since} 场 | 建议间隔：{STORM_INTERVAL} 场")

    # Most recent discovery
    trigger_history = dynamic_storm.get("trigger_history", [])
    if trigger_history:
        last = trigger_history[-1]
        scene_num = last.get("scene", "?")
        focus = last.get("focus_area", "")
        focus_str = f"——聚焦：{focus}" if focus else ""
        lines.append(f"最近发现：[第{scene_num}场]{focus_str} （发现 {last.get('perspectives_found', 0)} 个新视角）")

    # Suggestion when interval exceeded
    if scenes_since >= STORM_INTERVAL:
        lines.append(f"⚡ 已达建议间隔——建议调用 dynamic_storm() 发现新视角")

    # Strong suggestion when tension is low
    conflict_engine = state.get("conflict_engine", {})
    consecutive_low = conflict_engine.get("consecutive_low_tension", 0)
    if consecutive_low >= 3:
        lines.append("🔥 张力持续低迷——强烈建议调用 dynamic_storm() 发现新视角")

    text = "【Dynamic STORM】\n" + "\n".join(lines)
    return {"key": "dynamic_storm", "text": text, "priority": _DIRECTOR_SECTION_PRIORITIES["dynamic_storm"], "truncatable": True}
```

</action>

</task>

<task id="4">

<title>Update agent.py — replace trigger_storm with dynamic_storm, update §5, add §10</title>

<read_first>
- app/agent.py (full file — L1-50 imports, L117-317 _improv_director prompt, L457 tools list)
- .planning/phases/08-dynamic-storm/08-CONTEXT.md (D-34 §10 prompt content)
</read_first>

<acceptance_criteria>
- `agent.py` imports `dynamic_storm` from tools (replacing or alongside `trigger_storm`)
- `_improv_director` tools list includes `dynamic_storm` (not just `trigger_storm`)
- `trigger_storm` may still be in imports for backward compat but `dynamic_storm` is the primary tool registered
- Director prompt §5 changed from "视角审视（/storm）" referencing `trigger_storm` to "Dynamic STORM（/storm）" referencing `dynamic_storm`
- §5 content includes: "调用 dynamic_storm(focus_area) 发现新视角", "evaluate_tension() 建议时优先调用", "新视角发现后考虑基于新角度调用 inject_conflict()"
- New §10 "Dynamic STORM（视角重新发现）" section added after §9
- §10 contains: trigger conditions (every ~8 scenes, tension low 3+ scenes, user /storm), usage rules (1-2 new perspectives per trigger, must be consistent with established facts, merge into storm data), conflict linkage (consider inject_conflict after new perspective)
- §10 text matches D-34 decisions
</acceptance_criteria>

<action>
Modify `/workspace/director-actor-drama/app/agent.py`:

1. **Update imports** (L21-50): Add `dynamic_storm` to the import from `.tools`:
   - Add `dynamic_storm,` to the import list (keep `trigger_storm` for backward compat in case other code references it)

2. **Update `_improv_director` tools list**: In the `_improv_director = Agent(...)` call, find the `tools=[...]` parameter. Replace `trigger_storm` with `dynamic_storm` in the tools list. Keep `trigger_storm` if you want backward compat, but `dynamic_storm` must be present.

3. **Update §5** in the `_improv_director` instruction string. Change:
```
## §5 视角审视（/storm）
当用户发送 /storm [焦点] 时：
1. 调用 trigger_storm(focus_area) 触发审视
2. 重新审视当前剧情，输出 1-2 个新角度或未探索方向
3. 以【视角审视】标记输出，与正常旁白区分
4. 审视结果可融入后续场景，但不要当场强行使用
```
To:
```
## §5 Dynamic STORM（/storm）
当用户发送 /storm [焦点] 或 evaluate_tension() 建议触发时：
1. 调用 dynamic_storm(focus_area) 发现新视角
2. 新视角自动合并入 storm 数据——后续场景自然融入
3. 发现新视角后，考虑基于新角度调用 inject_conflict() 注入冲突
4. 新视角必须与已发生事件一致，是扩展而非推翻
5. 审视结果可融入后续场景，但不要当场强行使用
```

4. **Add §10** after §9 (after the "弧线追踪与线索管理" section, before the "🎭 A2A 多 Agent 架构" section):
```
## §10 Dynamic STORM（视角重新发现）
- 每 8 场左右（或张力持续低迷时），调用 dynamic_storm() 重新发现新视角
- evaluate_tension() 返回 suggested_action 包含 "dynamic_storm" 时，优先调用
- 用户可通过 /storm [焦点] 手动触发
- 新视角发现后，考虑基于新角度调用 inject_conflict() 注入冲突
- 新视角必须与已发生事件一致，是扩展而非推翻
- 每次 Dynamic STORM 仅发现 1-2 个新视角，避免过载导致剧情失焦
- 发现的新视角会在 2-3 场内逐步融入，而非当场强行使用
```

</action>

</task>
