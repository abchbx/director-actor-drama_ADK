---
phase: 22-chat-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/drama/app/ui/screens/dramadetail/components/ChatInputBar.kt
  - app/api/models.py
  - app/api/routers/commands.py
files_deleted:
  - android/app/src/main/java/com/drama/app/ui/screens/dramadetail/components/CommandInputBar.kt
autonomous: true
requirements: [CHAT-01, CHAT-02, CHAT-03, CHAT-04, CHAT-05, CHAT-06, CHAT-07, CHAT-08]

must_haves:
  truths:
    - "ChatInputBar 斜杠命令下拉仅展示功能性命令（/next, /end, /save, /load, /list, /delete）"
    - "输入框下方有 /next 和 /end 快捷芯片按钮"
    - "后端 ChatRequest 包含 sender_name 字段，/drama/chat 路由注入发送者信息到命令文本"
    - "旧 CommandInputBar.kt 已删除"
    - "用户消息右对齐气泡、@提及选择器、/drama/chat 路由正常工作（回归验证）"
  artifacts:
    - path: "android/app/src/main/java/com/drama/app/ui/screens/dramadetail/components/ChatInputBar.kt"
      provides: "清理斜杠命令 + 添加快捷芯片"
      contains: "QUICK_ACTION"
    - path: "app/api/models.py"
      provides: "ChatRequest 添加 sender_name 字段"
      contains: "sender_name"
    - path: "app/api/routers/commands.py"
      provides: "/drama/chat 路由注入 sender_name"
      contains: "sender_name"
  key_links:
    - from: "ChatInputBar.kt"
      to: "DramaDetailViewModel.kt"
      via: "onCommand(\"/next\"), onCommand(\"/end\")"
      pattern: "onCommand"
    - from: "commands.py"
      to: "models.py"
      via: "body.sender_name"
      pattern: "sender_name"
---

<objective>
将 Android 剧本详情页打磨为群聊模式。Phase 17-18 已提前实现 6/8 核心需求（用户消息气泡、@提及、/drama/chat 路由、角色插话），本 Plan 补全剩余缺口：清理斜杠命令体系为仅功能性命令、添加 /next /end 快捷芯片、后端 ChatRequest 接收并注入 sender_name、删除旧 CommandInputBar 组件。

Purpose: 当前 ChatInputBar 仍展示 12 个斜杠命令（含 /action, /speak 等有聊天等价物的命令），缺乏 /next /end 快捷入口，后端 ChatRequest 忽略 Android 已发送的 senderName 字段，旧 CommandInputBar 死代码残留。

Output: 3 个文件修改 + 1 个文件删除，群聊体验完整。
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-chat-mode/22-DISCUSSION-LOG.md
@.planning/phases/22-chat-mode/22-CONTEXT.md

<!-- Android ChatRequestDto 已包含 senderName 和 senderType 字段，但后端 ChatRequest 不接收 -->
<!-- sendChatMessageAsBubbles() 已传入 senderName 到 ChatRequestDto，但后端 /drama/chat 忽略 -->
<!-- ChatInputBar 当前有 12 个 SLASH_COMMANDS，需删除 6 个有聊天等价物的命令 -->
<!-- CommandInputBar.kt 是旧版命令式输入组件，不被任何 Screen 引用 -->

<interfaces>
<!-- ChatInputBar.kt — 当前接口（本 Plan 将修改内部实现，接口不变） -->
```kotlin
@Composable
fun ChatInputBar(
    actors: List<String>,
    onSend: (String, String?) -> Unit,
    onCommand: (String) -> Unit,
    isProcessing: Boolean,
    isTyping: Boolean = false,
    isWsConnected: Boolean = true,
    isReconnecting: Boolean = false,
    modifier: Modifier = Modifier,
)
```

<!-- ChatRequestDto.kt — 已存在 sender_name/senderType 字段（本 Plan 不修改） -->
```kotlin
data class ChatRequestDto(
    val message: String,
    val mention: String? = null,
    val senderType: String = "user",
    val senderName: String = "主角",
)
```

<!-- 后端 ChatRequest — 当前结构（本 Plan 将添加 sender_name） -->
```python
class ChatRequest(BaseModel):
    message: str = Field(..., min_length=1, description="Chat message text")
    mention: str | None = Field(default=None, description="Optional @mention actor name")
```

<!-- 后端 /drama/chat 路由 — 当前逻辑（本 Plan 将注入 sender_name） -->
```python
@router.post("/drama/chat", response_model=CommandResponse)
async def chat_message(body: ChatRequest, ...):
    if body.mention:
        msg = f"/speak {body.mention} {body.message}"
    else:
        msg = f"/action {body.message}"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatInputBar — 清理斜杠命令 + 添加快捷芯片</name>
  <files>
    android/app/src/main/java/com/drama/app/ui/screens/dramadetail/components/ChatInputBar.kt
  </files>
  <read_first>
    android/app/src/main/java/com/drama/app/ui/screens/dramadetail/components/ChatInputBar.kt
  </read_first>
  <action>
    **1. 精简 SLASH_COMMANDS 列表** — 将 12 个命令缩减为 6 个功能性命令：

    替换整个 `SLASH_COMMANDS` 列表（41-54 行）：
    ```kotlin
    private val SLASH_COMMANDS = listOf(
        SlashCommand("/next", "", "推进到下一个情节"),
        SlashCommand("/end", "", "落幕，结束当前剧本"),
        SlashCommand("/save", "[名称]", "保存当前场景到本地"),
        SlashCommand("/load", "<名称>", "加载已保存的本地场景"),
        SlashCommand("/list", "", "列出所有本地存档"),
        SlashCommand("/delete", "<名称>", "删除指定的本地存档"),
    )
    ```

    删除的命令：`/action`, `/steer`, `/auto`, `/storm`, `/speak`, `/cast`
    理由：这些命令有聊天等价物（@提及 → /speak，自由文本 → /action），在群聊模式下不应直接暴露。

    **2. 添加快捷操作数据类** — 在 `SlashCommand` data class 之后（62 行后）添加：
    ```kotlin

    private data class QuickAction(
        val label: String,
        val command: String,
        val icon: String,
    )
    ```

    **3. 添加快捷操作列表** — 在 `QuickAction` data class 之后添加：
    ```kotlin

    private val QUICK_ACTIONS = listOf(
        QuickAction("下一场", "/next", "»"),
        QuickAction("落幕", "/end", "🏁"),
    )
    ```

    **4. 添加快捷芯片行** — 在 `MentionChip` Row（108-126 行）的 `}` 之后、`// Slash command dropdown menu` Box 之前，插入快捷芯片 Row：
    ```kotlin

        // Quick action chips — /next /end shortcuts
        Row(
            modifier = Modifier
                .fillMaxWidth()
                .padding(bottom = 4.dp),
            horizontalArrangement = Arrangement.spacedBy(8.dp, Alignment.Start),
        ) {
            QUICK_ACTIONS.forEach { action ->
                Surface(
                    shape = RoundedCornerShape(16.dp),
                    color = MaterialTheme.colorScheme.tertiaryContainer,
                    onClick = { onCommand(action.command) },
                ) {
                    Text(
                        text = "${action.icon} ${action.label}",
                        style = MaterialTheme.typography.labelSmall,
                        color = MaterialTheme.colorScheme.onTertiaryContainer,
                        modifier = Modifier.padding(horizontal = 12.dp, vertical = 7.dp),
                    )
                }
            }
        }
    ```

    **5. 更新 placeholder 文本** — 将第 158 行的 placeholder 从：
    ```kotlin
    else -> "以主角身份发言或输入命令..."
    ```
    改为：
    ```kotlin
    else -> "发消息给角色们..."
    ```
    这更贴合群聊体验。
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama/android && grep -c 'SlashCommand("/action"' app/src/main/java/com/drama/app/ui/screens/dramadetail/components/ChatInputBar.kt || echo "PASS: /action removed" && grep -c 'SlashCommand("/speak"' app/src/main/java/com/drama/app/ui/screens/dramadetail/components/ChatInputBar.kt || echo "PASS: /speak removed" && grep 'QuickAction' app/src/main/java/com/drama/app/ui/screens/dramadetail/components/ChatInputBar.kt && grep 'QUICK_ACTIONS' app/src/main/java/com/drama/app/ui/screens/dramadetail/components/ChatInputBar.kt && grep '下一场' app/src/main/java/com/drama/app/ui/screens/dramadetail/components/ChatInputBar.kt && grep '落幕' app/src/main/java/com/drama/app/ui/screens/dramadetail/components/ChatInputBar.kt && grep '发消息给角色们' app/src/main/java/com/drama/app/ui/screens/dramadetail/components/ChatInputBar.kt</automated>
  </verify>
  <done>
    - SLASH_COMMANDS 仅包含 6 个功能性命令（/next, /end, /save, /load, /list, /delete）
    - QuickAction data class 和 QUICK_ACTIONS 列表定义
    - 快捷芯片行（下一场、落幕）渲染在 MentionChip 下方
    - placeholder 文本改为群聊风格
  </done>
  <acceptance_criteria>
    - ChatInputBar.kt 不包含 `SlashCommand("/action"` 
    - ChatInputBar.kt 不包含 `SlashCommand("/speak"`
    - ChatInputBar.kt 不包含 `SlashCommand("/steer"`
    - ChatInputBar.kt 不包含 `SlashCommand("/auto"`
    - ChatInputBar.kt 不包含 `SlashCommand("/storm"`
    - ChatInputBar.kt 不包含 `SlashCommand("/cast"`
    - ChatInputBar.kt 包含 `QuickAction` data class
    - ChatInputBar.kt 包含 `QUICK_ACTIONS` 列表
    - ChatInputBar.kt 包含 "下一场" 快捷芯片
    - ChatInputBar.kt 包含 "落幕" 快捷芯片
    - ChatInputBar.kt placeholder 包含 "发消息给角色们"
    - SLASH_COMMANDS 包含 `/next` 和 `/end`（保留在命令列表中）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 后端 ChatRequest 添加 sender_name + 路由注入</name>
  <files>
    app/api/models.py
    app/api/routers/commands.py
  </files>
  <read_first>
    app/api/models.py
    app/api/routers/commands.py
  </read_first>
  <action>
    **1. app/api/models.py** — 找到 `ChatRequest` 类定义（54-62 行），在 `mention` 字段之后添加 `sender_name` 字段：

    ```python
    class ChatRequest(BaseModel):
        """Request to send a chat message in group chat mode.

        If mention is provided, routes to /speak for that actor.
        Otherwise, routes to /action (broadcast to all actors).
        """

        message: str = Field(..., min_length=1, description="Chat message text")
        mention: str | None = Field(default=None, description="Optional @mention actor name")
        sender_name: str = Field(default="导演", description="Name of the message sender")
    ```

    **2. app/api/routers/commands.py** — 找到 `/drama/chat` 路由（287-316 行），修改消息格式以注入 sender_name：

    将第 305-310 行：
    ```python
        if body.mention:
            # @提及 → /speak 角色名 情境
            msg = f"/speak {body.mention} {body.message}"
        else:
            # 群消息 → /action（用户以主角身份行动）
            msg = f"/action {body.message}"
    ```
    替换为：
    ```python
        sender_prefix = f"[{body.sender_name}]" if body.sender_name != "导演" else ""
        if body.mention:
            # @提及 → /speak 角色名 情境（含发送者标识）
            msg = f"/speak {body.mention} {sender_prefix}{body.message}".strip()
        else:
            # 群消息 → /action（含发送者标识）
            msg = f"/action {sender_prefix}{body.message}".strip()
    ```

    理由：`sender_name` 默认 "导演"（向后兼容），当用户自定义名称时（如 "主角"），在消息前标注 `[主角]`，让 Director Agent 和 A2A Actor 知道"谁"在说话。
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && grep 'sender_name' app/api/models.py && grep 'sender_name' app/api/routers/commands.py</automated>
  </verify>
  <done>
    - ChatRequest 模型包含 sender_name: str = "导演" 字段
    - /drama/chat 路由将 sender_name 注入命令文本（[发送者名] 前缀）
    - 默认 "导演" 保持向后兼容（不添加前缀）
  </done>
  <acceptance_criteria>
    - app/api/models.py 的 ChatRequest 类包含 `sender_name: str = Field(default="导演"`
    - app/api/routers/commands.py 的 /drama/chat 路由包含 `body.sender_name` 引用
    - 当 sender_name != "导演" 时，消息前添加 `[sender_name]` 前缀
    - 当 sender_name == "导演" 时，消息格式不变（向后兼容）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 删除旧 CommandInputBar.kt</name>
  <files>
    android/app/src/main/java/com/drama/app/ui/screens/dramadetail/components/CommandInputBar.kt
  </files>
  <read_first>
    android/app/src/main/java/com/drama/app/ui/screens/dramadetail/components/CommandInputBar.kt
  </read_first>
  <action>
    删除文件 `android/app/src/main/java/com/drama/app/ui/screens/dramadetail/components/CommandInputBar.kt`

    验证无引用：先搜索 `CommandInputBar` 在其他文件中的 import 或使用。如果 `DramaDetailScreen.kt` 有 import 但未使用，一并清理 import 行。

    ```bash
    grep -r "CommandInputBar" android/app/src/main/java/com/drama/app/ --include="*.kt"
    ```

    如果仅在 CommandInputBar.kt 自身出现（定义），可安全删除。如有其他文件 import，先清理 import 再删除。
  </action>
  <verify>
    <automated>test ! -f /workspace/director-actor-drama/android/app/src/main/java/com/drama/app/ui/screens/dramadetail/components/CommandInputBar.kt && echo "PASS: File deleted" || echo "FAIL: File still exists"</automated>
  </verify>
  <done>
    - CommandInputBar.kt 已删除
    - 无残留 import 引用
  </done>
  <acceptance_criteria>
    - CommandInputBar.kt 文件不存在
    - `grep -r "CommandInputBar" android/app/src/main/java/` 无结果
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android → 后端 ChatRequest | sender_name 由客户端传入，后端不验证真实性 |
| 后端 → Runner 命令 | sender_name 注入命令文本，进入 LLM prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | S (Spoofing) | sender_name 伪造 | accept | 局域网/单用户场景，无实际安全风险；sender_name 仅用于 LLM prompt 上下文，不影响权限 |
| T-22-02 | I (Info Disclosure) | 快捷芯片 onCommand | accept | 命令通过现有 auth 通道发送，无新攻击面 |
</threat_model>

<verification>
1. grep 验证：所有 acceptance_criteria 中的字符串存在于对应文件
2. 编译检查：`cd /workspace/director-actor-drama/android && ./gradlew :app:compileDebugKotlin` 无错误
3. 后端测试：`POST /drama/chat` 接受 sender_name 字段，默认 "导演"
4. 回归验证：
   - ChatInputBar 输入文本点发送 → onSend(text, mention) 正常调用
   - ChatInputBar 输入 / 开头 → onCommand(text) 正常调用
   - 点击快捷芯片 → onCommand("/next") 或 onCommand("/end") 正常调用
   - /drama/chat 不带 sender_name → 默认 "导演"，消息格式不变
   - /drama/chat 带 sender_name="主角" → 消息前加 [主角] 前缀
5. 删除验证：CommandInputBar.kt 不存在，无残留 import
</verification>

<success_criteria>
- ChatInputBar 斜杠命令下拉仅展示 6 个功能性命令
- 输入框下方有 /next "» 下一场" 和 /end "🏁 落幕" 快捷芯片
- 后端 ChatRequest 包含 sender_name 字段
- /drama/chat 路由在 sender_name 非 "导演" 时注入 [sender_name] 前缀
- CommandInputBar.kt 已删除，无残留引用
- Android 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/22-chat-mode/22-01-SUMMARY.md`
</output>
