---
phase: 05-mixed-autonomy-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/tools.py
  - tests/unit/test_tools_phase5.py
  - tests/unit/conftest.py
autonomous: true
requirements:
  - LOOP-02
  - LOOP-04

must_haves:
  truths:
    - "auto_advance() sets state['drama']['remaining_auto_scenes'] and returns success status"
    - "auto_advance() with scenes > 10 returns status 'info' with warning, does NOT set counter"
    - "steer_drama() sets state['drama']['steer_direction'] and returns confirmation"
    - "end_drama() sets state['drama']['status'] to 'ended', returns epilogue template, clears steer_direction"
    - "trigger_storm() returns storm review guidance, does not crash on missing storm sub-dict"
  artifacts:
    - path: "app/tools.py"
      provides: "auto_advance, steer_drama, end_drama, trigger_storm tool functions"
      contains: "def auto_advance"
    - path: "tests/unit/test_tools_phase5.py"
      provides: "Unit tests for all 4 new tool functions"
      contains: "test_auto_advance"
    - path: "tests/unit/conftest.py"
      provides: "Updated mock_tool_context with Phase 5 state fields"
      contains: "remaining_auto_scenes"
  key_links:
    - from: "app/tools.py::auto_advance"
      to: "state['drama']['remaining_auto_scenes']"
      via: "direct state write"
      pattern: "remaining_auto_scenes"
    - from: "app/tools.py::end_drama"
      to: "state['drama']['status']"
      via: "setting ended status"
      pattern: "status.*ended"
---

<objective>
Implement 4 new Tool functions (auto_advance, steer_drama, end_drama, trigger_storm) that set Phase 5 state fields per D-06, D-10, D-15, D-19. These are pure state-setters following existing tool function patterns — no LLM interaction, no loop logic, no router changes.

Purpose: These 4 tools are the state-management foundation for mixed autonomy mode. They set counters, directions, and status flags that the prompt (Plan 03) and context builder (Plan 02) will read.

Output: 4 working tool functions in app/tools.py + comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mixed-autonomy-mode/05-CONTEXT.md
@.planning/phases/05-mixed-autonomy-mode/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/tools.py — Existing tool function pattern:
```python
def tool_name(param: type, tool_context: ToolContext) -> dict:
    """English first line. 中文详细描述.

    Args:
        param: Description (Chinese OK).

    Returns:
        dict with status and message.
    """
    state = tool_context.state.get("drama", {})
    # ... logic ...
    return {"status": "success", "message": "中文消息", ...}
```

State access pattern (from tools.py):
```python
state = tool_context.state.get("drama", {})
# Read: state.get("remaining_auto_scenes", 0)
# Write: state["remaining_auto_scenes"] = scenes; then persist
```

From app/state_manager.py — State persistence:
```python
# tools.py does NOT import _get_state/_set_state directly
# Instead it writes to tool_context.state["drama"] directly
# and relies on state_manager functions for persistence
# For new tool functions, follow existing pattern: read from tool_context.state.get("drama", {}),
# write to tool_context.state["drama"][key] = value
# The state auto-persists on next state_manager call (e.g., _set_state)
# BUT: tools.py doesn't call _set_state — it relies on state_manager functions being called
# For new tools that ONLY set state without calling state_manager, you MUST
# import and call _set_state to persist the change
```

From app/state_manager.py:
```python
def _get_state(tool_context) -> dict:
    if tool_context is not None:
        return tool_context.state.get("drama", {})
    return {}

def _set_state(state: dict, tool_context):
    if tool_context is not None:
        tool_context.state["drama"] = state
        theme = state.get("theme")
        if theme:
            _save_state_to_file(theme, state)
```

From app/tools.py — user_action() pattern (reference for steer_drama):
```python
def user_action(action_description: str, tool_context: ToolContext) -> dict:
    return {
        "status": "success",
        "message": (
            f"用户注入事件: {action_description}\n"
            "请作为导演：\n"
            "1. 考虑这个事件如何影响当前剧情\n"
            "2. 用 director_narrate 描述事件的发生\n"
            "3. 用 actor_speak 让相关角色做出反应\n"
            "4. 更新角色的情绪和记忆"
        ),
        "action": action_description,
    }
```

From tests/unit/conftest.py — mock_tool_context fixture:
```python
@pytest.fixture
def mock_tool_context():
    tc = MagicMock()
    tc.state = {
        "drama": {
            "theme": "测试戏剧",
            "current_scene": 3,
            "status": "acting",
            "actors": {"朱棣": {...}},
        }
    }
    return tc
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 4 new Tool functions to app/tools.py + update imports</name>
  <files>app/tools.py, tests/unit/test_tools_phase5.py, tests/unit/conftest.py</files>
  <behavior>
    - Test: auto_advance(3, mock_tool_context) returns status="success", sets state["drama"]["remaining_auto_scenes"]=3, message contains "3 场"
    - Test: auto_advance(15, mock_tool_context) returns status="info", does NOT set remaining_auto_scenes, message contains "超过建议上限"
    - Test: auto_advance(10, mock_tool_context) returns status="success" (boundary, 10 is allowed)
    - Test: steer_drama("让朱棣更偏执", mock_tool_context) returns status="success", sets state["drama"]["steer_direction"]="让朱棣更偏执", message contains "方向已设置"
    - Test: end_drama(mock_tool_context) returns status="success", sets state["drama"]["status"]="ended", returns "epilogue_template" key, message contains "终幕已触发"
    - Test: end_drama(mock_tool_context) clears steer_direction (per RESEARCH A5 mitigation)
    - Test: trigger_storm("角色关系", mock_tool_context) returns status="success", message contains "视角审视", creates state["drama"]["storm"] sub-dict if missing (D-22)
    - Test: trigger_storm("角色关系", mock_tool_context_no_storm) does not crash when storm sub-dict doesn't exist
  </behavior>
  <read_first>
    app/tools.py (full file — understand import patterns, function signatures, return dict formats)
    app/state_manager.py (lines 939-953 — _get_state/_set_state for persistence)
    tests/unit/conftest.py (existing mock_tool_context fixture)
    .planning/phases/05-mixed-autonomy-mode/05-CONTEXT.md (D-01 through D-23 locked decisions)
    .planning/phases/05-mixed-autonomy-mode/05-RESEARCH.md (code examples in Code Examples section)
  </read_first>
  <action>
    **Step 1: Create test file tests/unit/test_tools_phase5.py with TDD tests**

    Create `tests/unit/test_tools_phase5.py` with 8 test functions covering the behaviors above. Use `mock_tool_context` fixture from conftest.py.

    Each test must:
    - Call the tool function with mock_tool_context
    - Assert the return dict has expected status, message content, and extra keys
    - Assert the state was mutated correctly (check `mock_tool_context.state["drama"][field]`)

    **Step 2: Update tests/unit/conftest.py — add Phase 5 state fields to mock_tool_context**

    Add these fields to the `mock_tool_context` fixture's `drama` state dict:
    ```python
    "remaining_auto_scenes": 0,
    "steer_direction": None,
    "storm": {"last_review": {}},
    ```

    Also create a second fixture `mock_tool_context_no_storm` that does NOT have the `storm` key, for testing trigger_storm initialization.

    **Step 3: Add _get_state/_set_state imports to app/tools.py**

    Add `_get_state` and `_set_state` to the existing import block from `.state_manager`:
    ```python
    from .state_manager import (
        add_narration,
        ...
        _get_state,
        _set_state,
    )
    ```
    This is needed because the new tool functions set state directly and need persistence.

    **Step 4: Implement auto_advance() in app/tools.py (per D-01, D-03, D-04, D-05, D-06)**

    Add function after the existing `user_action()` function (~line 586):

    ```python
    def auto_advance(scenes: int, tool_context: ToolContext) -> dict:
        """Enable auto-advance mode for N scenes. The director will advance N scenes autonomously.

        设置自动推进模式，AI 将自主推进指定场数的戏剧。用户可随时输入任何内容中断。

        Args:
            scenes: Number of scenes to auto-advance. Default 3, soft cap at 10.

        Returns:
            dict with auto-advance status and guidance.
        """
        state = _get_state(tool_context)

        # D-05: Soft cap at 10 with warning
        if scenes > 10:
            current = state.get("remaining_auto_scenes", 0)
            return {
                "status": "info",
                "message": (
                    f"⚠️ 请求推进 {scenes} 场超过建议上限(10场)。\n"
                    f"大量自动推进可能消耗较多 token。建议使用 /auto 10 以内。\n"
                    f"如果确认，请再次发送 /auto {scenes}"
                ),
                "remaining_auto_scenes": current,
            }

        # D-04: Default 3 handled by caller (router/CLI passes 3 if no arg)
        state["remaining_auto_scenes"] = scenes
        _set_state(state, tool_context)

        return {
            "status": "success",
            "message": (
                f"🎬 自动推进模式已启动！将自主推进 {scenes} 场戏。\n"
                f"每场结束后会提示剩余场数。\n"
                f"输入任何内容即可中断，回到手动模式。"
            ),
            "remaining_auto_scenes": scenes,
        }
    ```

    **Step 5: Implement steer_drama() in app/tools.py (per D-07, D-08, D-09, D-10)**

    ```python
    def steer_drama(direction: str, tool_context: ToolContext) -> dict:
        """Set a directional guidance for the next scene. Lighter than /action which injects specific events.

        设置下一场的方向引导，导演会在此方向上发挥创意。
        与 /action 不同：/steer 给方向（"让朱棣更偏执"），/action 给事件（"朱棣发现密信"）。

        Args:
            direction: The direction or guidance for the next scene.

        Returns:
            dict with confirmation that the steer is set.
        """
        state = _get_state(tool_context)
        state["steer_direction"] = direction
        _set_state(state, tool_context)

        return {
            "status": "success",
            "message": (
                f"🧭 方向已设置：{direction}\n"
                f"下一场将在此方向上发挥创意。\n"
                f"效力仅一场，之后自动清除。"
            ),
            "steer_direction": direction,
        }
    ```

    **Step 6: Implement end_drama() in app/tools.py (per D-11, D-12, D-13, D-14, D-15)**

    ```python
    def end_drama(tool_context: ToolContext) -> dict:
        """End the drama with epilogue narration, auto-save, and script export.

        触发终幕机制：设置 drama_status 为 ended，提示导演生成终幕旁白，
        自动保存存档，并导出完整剧本。结束后可继续番外篇。

        Args:
            tool_context: The tool context.

        Returns:
            dict with end status and epilogue template.
        """
        state = _get_state(tool_context)
        state["status"] = "ended"

        # A5 mitigation: Clear steer_direction on end to prevent residue
        state["steer_direction"] = None

        # Reset auto-advance counter
        state["remaining_auto_scenes"] = 0

        _set_state(state, tool_context)

        return {
            "status": "success",
            "message": (
                "🎭 终幕已触发！\n"
                "请按照终幕协议生成终幕旁白：\n"
                "1. 回顾全剧主线\n"
                "2. 各角色结局\n"
                "3. 主题升华\n"
                "4. 落幕致辞\n\n"
                "生成终幕旁白后，自动调用 save_drama 和 export_drama。\n"
                "结束后用户仍可用 /next 继续番外篇。"
            ),
            "drama_status": "ended",
            "epilogue_template": (
                "🎭 终幕 ——\n"
                "【回顾】全剧主线梳理\n"
                "【角色结局】各角色命运\n"
                "【主题升华】核心主题回响\n"
                "【落幕致辞】最后的旁白"
            ),
        }
    ```

    **Step 7: Implement trigger_storm() in app/tools.py (per D-19, D-20, D-22)**

    ```python
    def trigger_storm(focus_area: str, tool_context: ToolContext) -> dict:
        """Trigger a lightweight perspective review of the current drama.

        手动触发视角审视——让导演重新审视当前剧情，发现新的角度或未探索方向。
        轻量版（Phase 5），Phase 8 将升级为完整 Dynamic STORM。

        Args:
            focus_area: Optional area to focus the review on (e.g., "角色关系", "权力斗争").

        Returns:
            dict with storm review status.
        """
        state = _get_state(tool_context)

        # D-22: Ensure storm sub-dict exists
        if "storm" not in state:
            state["storm"] = {"last_review": {}}

        _set_state(state, tool_context)

        return {
            "status": "success",
            "message": (
                f"🔍 视角审视已触发！聚焦领域：{focus_area}\n"
                f"请重新审视当前剧情，输出 1-2 个新角度或未探索方向。\n"
                f"格式：以【视角审视】标记输出。"
            ),
            "focus_area": focus_area,
        }
    ```
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && uv run pytest tests/unit/test_tools_phase5.py -x -q</automated>
  </verify>
  <done>
    - auto_advance(3) → status="success", state["remaining_auto_scenes"]=3
    - auto_advance(15) → status="info", state unchanged, warning message
    - steer_drama("方向") → status="success", state["steer_direction"]="方向"
    - end_drama() → status="success", state["status"]="ended", steer_direction cleared, epilogue_template returned
    - trigger_storm("角色关系") → status="success", storm sub-dict created if missing
    - All 8 unit tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance next_scene() with auto-advance counter decrement + update _build_current_status_section</name>
  <files>app/tools.py, app/context_builder.py, tests/unit/test_tools_phase5.py</files>
  <read_first>
    app/tools.py (lines 493-562 — existing next_scene function)
    app/context_builder.py (lines 484-505 — existing _build_current_status_section)
    app/context_builder.py (lines 57-67 — _DIRECTOR_SECTION_PRIORITIES dict)
    .planning/phases/05-mixed-autonomy-mode/05-CONTEXT.md (D-23 state fields)
    .planning/phases/05-mixed-autonomy-mode/05-RESEARCH.md (Pitfall 1 — counter decrement timing)
  </read_first>
  <action>
    **Step 1: Add auto-advance counter decrement to next_scene() in app/tools.py**

    Per RESEARCH.md Pitfall 1 and A4 mitigation: the code-level decrement in next_scene() is the safety net. The prompt also instructs LLM to decrement, but code is more reliable.

    In `next_scene()` function (around line 507, after `result = advance_scene(tool_context)` and after `state = tool_context.state.get("drama", {})`), add:

    ```python
    # Phase 5: Auto-advance counter decrement (A4 mitigation)
    # When remaining_auto_scenes > 0, decrement on each new scene start.
    # This is a code-level safety net — the prompt also instructs LLM to decrement.
    auto_remaining = state.get("remaining_auto_scenes", 0)
    auto_status = ""
    if auto_remaining > 0:
        state["remaining_auto_scenes"] = max(0, auto_remaining - 1)
        auto_remaining = state["remaining_auto_scenes"]
        if auto_remaining == 0:
            auto_status = "\n\n🔄 自动推进已结束，回到手动模式。"
        else:
            auto_status = f"\n\n[自动推进中... 剩余 {auto_remaining} 场，输入任意内容中断]"
        # Persist the counter change
        from .state_manager import _set_state as _persist_state
        _persist_state(state, tool_context)
    ```

    Then append `auto_status` to the existing return message string.

    Also add `"auto_remaining": auto_remaining` to the return dict.

    **Step 2: Add steer_direction clear-after-read to next_scene()**

    Per D-09: steer效力仅下一场。The clear should happen after next_scene() reads it (so the director gets the steer for this scene), then it's cleared for subsequent scenes.

    In `next_scene()`, after the transition extraction but before the return statement:

    ```python
    # Phase 5: Clear steer_direction after it's been read for this scene (D-09)
    steer_info = state.get("steer_direction")
    if steer_info:
        state["steer_direction"] = None
        from .state_manager import _set_state as _persist_state
        _persist_state(state, tool_context)
    ```

    Add `"steer_direction": steer_info` to the return dict so the director knows the steer for this scene.

    **Step 3: Update _build_current_status_section() in context_builder.py**

    Add auto-advance status info to the current_status section. This is the simplest integration point since current_status already shows drama state.

    In `_build_current_status_section()` (around line 484-505), add after the existing status lines:

    ```python
    # Phase 5: Auto-advance status
    auto_remaining = state.get("remaining_auto_scenes", 0)
    if auto_remaining > 0:
        lines.append(f"自动推进: 剩余 {auto_remaining} 场")
        lines.append("⚠️ 每场 write_scene 后递减计数器，归零时回到手动模式")
        lines.append("输出后插入提示: [自动推进中... 剩余 N 场，输入任意内容中断]")
    ```

    Also add steer direction if present:
    ```python
    # Phase 5: User steer direction
    steer = state.get("steer_direction")
    if steer:
        lines.append(f"用户引导: {steer}")
        lines.append("此引导仅本场生效，之后自动清除")
    ```

    **Step 4: Add tests for next_scene() enhancements**

    In `tests/unit/test_tools_phase5.py`, add:

    - `test_next_scene_decrement_auto`: Set mock state with `remaining_auto_scenes=3`, call `next_scene()`, assert `state["drama"]["remaining_auto_scenes"]` is now 2, return dict contains `auto_remaining=2` and `[自动推进中... 剩余 2 场]`
    - `test_next_scene_decrement_to_zero`: Set `remaining_auto_scenes=1`, call `next_scene()`, assert counter is 0 and message contains "回到手动模式"
    - `test_next_scene_clears_steer`: Set `steer_direction="让朱棣更偏执"`, call `next_scene()`, assert `state["drama"]["steer_direction"]` is None, return dict contains `steer_direction="让朱棣更偏执"` (the value was read before clearing)
    - `test_next_scene_no_auto_no_steer`: Call `next_scene()` with `remaining_auto_scenes=0` and `steer_direction=None`, verify no auto/steer info in return
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && uv run pytest tests/unit/test_tools_phase5.py tests/unit/test_context_builder.py -x -q</automated>
  </verify>
  <done>
    - next_scene() decrements remaining_auto_scenes when > 0 (A4 mitigation)
    - next_scene() clears steer_direction after reading (D-09)
    - next_scene() returns auto_remaining and steer_direction in dict
    - next_scene() message includes auto-advance status or manual mode transition
    - _build_current_status_section() shows auto-advance and steer info
    - All new unit tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → ToolContext | User input (scene count, direction text) flows into state via tool functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | auto_advance | mitigate | Validate `scenes` param is positive int; soft cap at 10 with warning (D-05) |
| T-05-02 | Denial of Service | auto_advance | mitigate | Soft cap 10 + warning prevents excessive token consumption; code-level counter prevents infinite loops |
| T-05-03 | Tampering | steer_drama | accept | Steer is user-facing by design; only the user can set steer direction |
| T-05-04 | Tampering | trigger_storm | accept | Focus area is user input used only for prompt guidance, no security boundary crossed |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_tools_phase5.py -x -q` — all Phase 5 tool tests pass
- `uv run pytest tests/unit/test_context_builder.py -x -q` — context builder tests still pass
- `uv run pytest tests/unit/ -x -q` — no regressions across all unit tests
</verification>

<success_criteria>
1. All 4 new tool functions (auto_advance, steer_drama, end_drama, trigger_storm) exist in app/tools.py with correct signatures, docstrings, and return formats
2. next_scene() decrements remaining_auto_scenes counter and clears steer_direction after reading
3. _build_current_status_section() displays auto-advance and steer state
4. All Phase 5 unit tests pass (12+ test cases)
5. No regressions in existing unit tests
</success_criteria>

<output>
After completion, create `.planning/phases/05-mixed-autonomy-mode/05-01-SUMMARY.md`
</output>
