---
phase: 05-mixed-autonomy-mode
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - app/agent.py
  - cli.py
  - tests/unit/test_agent.py
autonomous: true
requirements:
  - LOOP-02
  - LOOP-04

must_haves:
  truths:
    - "DramaRouter routes /auto, /steer, /end, /storm commands to improv_director"
    - "DramaRouter clears remaining_auto_scenes when user sends non-/auto input during auto-advance (D-02)"
    - "_improv_director prompt has 7-section structure per D-26"
    - "_improv_director tools list includes auto_advance, steer_drama, end_drama, trigger_storm"
    - "CLI help text lists /auto, /steer, /end, /storm commands"
    - "CLI _send_message displays auto_advance, steer_drama, end_drama, trigger_storm function calls"
  artifacts:
    - path: "app/agent.py"
      provides: "Restructured _improv_director prompt with 7 sections + new tools registered + router updates"
      contains: "自动推进协议"
    - path: "cli.py"
      provides: "Updated help text and function call display for Phase 5 commands"
      contains: "/auto"
  key_links:
    - from: "app/agent.py::DramaRouter._run_async_impl"
      to: "state['drama']['remaining_auto_scenes']"
      via: "interrupt safety net check"
      pattern: "remaining_auto_scenes"
    - from: "app/agent.py::_improv_director"
      to: "app/tools.py::auto_advance"
      via: "tool registration"
      pattern: "auto_advance"
    - from: "app/agent.py::_improv_director"
      to: "app/context_builder.py::build_director_context"
      via: "prompt references context sections"
      pattern: "【用户引导】"
---

<objective>
Restructure _improv_director system prompt with 7-section architecture, update DramaRouter routing for new commands + auto-interrupt safety net, register 4 new tools, and update CLI. This is the integration plan that connects the tools (Plan 01) and context builder (Plan 02) into a working system.

Purpose: The prompt is the "brain" of the mixed autonomy system. It must instruct the LLM how to behave in manual mode, auto-advance mode, steer mode, end mode, and storm mode. The router is the "gate" that ensures commands reach the right agent and auto-interrupt works reliably.

Output: Fully functional mixed autonomy mode — all 4 new commands routeable, prompt-driven behaviors defined, CLI updated.
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mixed-autonomy-mode/05-CONTEXT.md
@.planning/phases/05-mixed-autonomy-mode/05-RESEARCH.md
@.planning/phases/05-mixed-autonomy-mode/05-01-SUMMARY.md
@.planning/phases/05-mixed-autonomy-mode/05-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. From Plans 01 and 02 output. -->

From app/tools.py (Plan 01 output) — New tool functions:
```python
def auto_advance(scenes: int, tool_context: ToolContext) -> dict:
    # Sets state["drama"]["remaining_auto_scenes"] = scenes
    # Returns {"status": "success", "message": "🎬 自动推进模式已启动！...", "remaining_auto_scenes": scenes}
    # If scenes > 10: returns {"status": "info", "message": "⚠️ ...", "remaining_auto_scenes": current}

def steer_drama(direction: str, tool_context: ToolContext) -> dict:
    # Sets state["drama"]["steer_direction"] = direction
    # Returns {"status": "success", "message": "🧭 方向已设置：...", "steer_direction": direction}

def end_drama(tool_context: ToolContext) -> dict:
    # Sets state["drama"]["status"] = "ended", clears steer_direction, resets remaining_auto_scenes
    # Returns {"status": "success", "message": "🎭 终幕已触发！...", "drama_status": "ended", "epilogue_template": "..."}

def trigger_storm(focus_area: str, tool_context: ToolContext) -> dict:
    # Ensures state["drama"]["storm"] exists
    # Returns {"status": "success", "message": "🔍 视角审视已触发！...", "focus_area": focus_area}
```

From app/context_builder.py (Plan 02 output) — New sections:
```python
# build_director_context() now includes:
# - _build_steer_section(state) → 【用户引导】 when steer_direction set (priority 8)
# - _build_epilogue_section(state) → 【番外篇模式】 when status="ended" (priority 9)
# - _build_auto_advance_section(state) → 【自动推进模式】 when remaining_auto_scenes > 0 (priority 9)
```

From app/tools.py — next_scene() enhancements (Plan 01 output):
```python
# next_scene() now:
# - Decrements remaining_auto_scenes when > 0 (code-level safety net)
# - Clears steer_direction after reading (D-09)
# - Returns auto_remaining and steer_direction in dict
# - Appends auto-advance status to message
```

From app/agent.py — Current _improv_director (lines 106-291):
```python
_improv_director = Agent(
    name="improv_director",
    model=_get_model(),
    instruction="""⚠️ 无终点声明
你永远不会自行结束戏剧。只有用户发送 /end 时才终止。...""",
    tools=[actor_speak, director_narrate, get_director_context, write_scene,
           next_scene, user_action, save_drama, load_drama, export_drama,
           show_cast, show_status, list_all_dramas, update_emotion,
           mark_memory, retrieve_relevant_scenes_tool, backfill_tags_tool],
)
```

From app/agent.py — Current DramaRouter (lines 297-337):
```python
class DramaRouter(BaseAgent):
    async def _run_async_impl(self, ctx):
        drama = ctx.session.state.get("drama", {})
        actors = drama.get("actors", {})
        user_message = ""  # extracted from ctx.user_content
        utility_commands = ["/save", "/load", "/export", "/cast", "/status", "/list"]
        force_improvise = any(cmd in user_message for cmd in utility_commands)
        if force_improvise or (actors and len(actors) > 0):
            agent = self._sub_agents_map.get("improv_director")
        else:
            agent = self._sub_agents_map.get("setup_agent")
        async for event in agent.run_async(ctx):
            yield event
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure _improv_director prompt + register new tools + update DramaRouter</name>
  <files>app/agent.py, tests/unit/test_agent.py</files>
  <read_first>
    app/agent.py (full file — _setup_agent, _improv_director, DramaRouter, imports, root_agent)
    app/tools.py (lines 1-60 — import block, to confirm new function names)
    .planning/phases/05-mixed-autonomy-mode/05-CONTEXT.md (D-25, D-26 — prompt restructure, D-02 — auto interrupt, D-16/D-17/D-18 — options)
    .planning/phases/05-mixed-autonomy-mode/05-RESEARCH.md (Pitfall 2 — auto-interrupt safety net, Pitfall 6 — prompt length)
  </read_first>
  <action>
    **Step 1: Add new tool imports to app/agent.py**

    In the import block from `.tools` (lines 21-42), add these 4 new imports:
    ```python
    from .tools import (
        ...
        auto_advance,
        steer_drama,
        end_drama,
        trigger_storm,
    )
    ```

    **Step 2: Restructure _improv_director system prompt (per D-25, D-26)**

    Replace the entire `instruction="""..."""` string of `_improv_director` with a new 7-section prompt. The new prompt MUST follow the structure defined in D-26:

    ```
    ⚠️ 无终点声明（修订）
    你永远不会自行结束戏剧——除非用户发送 /end。戏剧只有两种状态：进行中或已结束。进行中的每一场都是新故事的开始。

    ## §1 核心循环协议（手动模式）
    当 remaining_auto_scenes == 0（手动模式），每次用户发送 /next 或 /action：
    1. next_scene() → 推进场景计数器，获取衔接信息
    2. director_narrate() → 描述本场环境、氛围、时间、地点
    3. actor_speak() × N → 逐个让参与角色回应
    4. write_scene() → 将完整内容记录到剧本
    5. 回顾局势 → 可选调用 get_director_context() 审视全局

    ⚠️ 手动模式下：完成上述步骤后等待用户指令，不要自动推进下一场。

    ⚠️ 最高优先级规则：必须先调用工具！
    当用户输入命令时，你**必须首先调用对应的工具**，然后再基于工具的返回结果进行回复。

    ## §2 自动推进协议（自动模式）
    当 remaining_auto_scenes > 0（自动模式），核心循环的步骤不变，但在 write_scene() 之后：

    ① write_scene() —— 记录当前场
    ② 递减 remaining_auto_scenes（next_scene() 会自动递减，无需手动操作）
    ③ 如果 next_scene() 返回 auto_remaining > 0：继续调用 next_scene() 开始下一场
    ④ 如果 auto_remaining == 0：回到手动模式，向用户报告"自动推进已结束"

    ⚠️ 每场输出后插入提示行：[自动推进中... 剩余 N 场，输入任意内容中断]
    ⚠️ 用户输入任何非 /auto 内容即中断自动推进（代码级安全网已处理）

    ## §3 用户引导与干预
    ### /steer <direction> —— 方向引导
    调用 steer_drama(direction) 设置方向。导演在此方向上发挥创意，不必拘泥。
    ⚠️ steer 效力仅下一场，之后自动清除。

    ### /action <event> —— 事件注入
    调用 user_action(event) 注入具体事件。与 steer 区分：
    - /steer = 给方向（"让朱棣更偏执"），导演自由发挥
    - /action = 给事件（"朱棣发现密信"），导演必须执行

    ## §4 终幕协议（/end + 番外篇）
    当用户发送 /end 时：
    1. 调用 end_drama() —— 设置 status="ended"，获取终幕模板
    2. 按模板生成终幕旁白：
       🎭 终幕 ——
       【回顾】全剧主线梳理
       【角色结局】各角色命运
       【主题升华】核心主题回响
       【落幕致辞】最后的旁白
    3. 调用 save_drama() 自动保存
    4. 调用 export_drama() 导出完整剧本
    5. 告知用户可继续 /next 进入番外篇

    ⚠️ 番外篇模式：/end 后如果用户继续 /next 或 /action，以番外篇/后日谈风格叙事。场景标注「番外第 X 场」。

    ## §5 视角审视（/storm）
    当用户发送 /storm [焦点] 时：
    1. 调用 trigger_storm(focus_area) 触发审视
    2. 重新审视当前剧情，输出 1-2 个新角度或未探索方向
    3. 以【视角审视】标记输出，与正常旁白区分
    4. 审视结果可融入后续场景，但不要当场强行使用

    ## §6 选项呈现规范
    ⚠️ 每场结束后（无论手动/自动模式），在导演批注区提供 2-3 个选项：

    > 🎯 接下来你想...
    > A. [剧情方向建议1]
    > B. [剧情方向建议2]
    > C. /action 注入事件 · /steer 引导方向 · /end 结束戏剧

    选项内容混合型：既有剧情方向（A/B），也有操作指引（C）。
    自动推进模式下额外包含："中断自动推进"选项。

    ## §7 输出格式
    [保留现有输出格式规则——剧本格式结构、格式要求详解、关键操作要点，完整不变]

    ## 你的角色
    [保留现有内容]

    ## 记忆检索
    [保留现有内容]

    ## 工作流程
    [保留现有工作流程，但更新命令提示列表——添加 /auto, /steer, /end, /storm]

    ## 重要原则
    [保留现有原则 1-11，修改第5条：]
    5. **混合模式**：手动模式下等待用户指令；自动模式下连续推进直到计数器归零或用户中断

    ## 回复风格
    [保留现有内容]

    ## 命令提示
    - /next - 推进下一场（输出完整剧本片段）
    - /action <描述> - 注入具体事件
    - /steer <方向> - 设置下一场方向引导
    - /auto [N] - 自动推进 N 场（默认3场）
    - /end - 终幕：生成终幕旁白 + 导出剧本
    - /storm [焦点] - 触发视角审视
    - /save [名称] - 保存进度
    - /load <名称> - 加载进度
    - /export - 导出完整剧本
    - /list - 列出已保存剧本
    - /cast - 查看角色列表
    - /status - 查看当前状态
    - /quit - 退出（自动保存）
    ```

    CRITICAL: When writing the actual prompt, preserve ALL existing formatting rules from §7 (剧本格式结构, 格式要求详解, 关键操作要点) exactly as-is. Only add the new sections §2-§6 and modify §1 and the command list.

    **Step 3: Register 4 new tools in _improv_director**

    In the `tools=[...]` list of `_improv_director` (around line 273-290), add:
    ```python
    auto_advance,
    steer_drama,
    end_drama,
    trigger_storm,
    ```

    **Step 4: Update DramaRouter._run_async_impl()**

    Replace the existing `_run_async_impl` method (lines 307-333) with:

    ```python
    async def _run_async_impl(
        self, ctx: InvocationContext
    ) -> AsyncGenerator[Event, None]:
        drama = ctx.session.state.get("drama", {})
        actors = drama.get("actors", {})

        # Extract user message
        user_message = ""
        if ctx.user_content and ctx.user_content.parts:
            for part in ctx.user_content.parts:
                text = getattr(part, 'text', None) or ''
                user_message += text.lower()

        # D-02: Auto-interrupt safety net — any non-/auto input clears remaining_auto_scenes
        if drama.get("remaining_auto_scenes", 0) > 0:
            if "/auto" not in user_message:
                # User sent non-auto input during auto-advance → interrupt
                ctx.session.state["drama"]["remaining_auto_scenes"] = 0

        # Route: utility commands + Phase 5 new commands → improv_director
        utility_commands = [
            "/save", "/load", "/export", "/cast", "/status", "/list",
            "/auto", "/steer", "/end", "/storm",  # Phase 5 additions
        ]
        force_improvise = any(cmd in user_message for cmd in utility_commands)

        if force_improvise or (actors and len(actors) > 0):
            agent = self._sub_agents_map.get("improv_director")
        else:
            agent = self._sub_agents_map.get("setup_agent")

        # D-03: Fallback to improv_director (safest default)
        if agent is None:
            agent = self._sub_agents_map.get("improv_director")

        async for event in agent.run_async(ctx):
            yield event
    ```

    **Step 5: Write tests in tests/unit/test_agent.py**

    Add tests for the router and prompt structure:

    - `test_router_routes_auto_to_improv`: Verify that "/auto 3" in user_message leads to improv_director being selected (mock the sub_agents_map, check which agent is called)
    - `test_router_routes_steer_to_improv`: Verify "/steer 方向" routes to improv_director
    - `test_router_routes_end_to_improv`: Verify "/end" routes to improv_director
    - `test_router_routes_storm_to_improv`: Verify "/storm" routes to improv_director
    - `test_router_auto_interrupt_clears_counter`: Create a mock ctx with session.state["drama"]["remaining_auto_scenes"]=5 and user_message="something", run _run_async_impl, verify remaining_auto_scenes is set to 0
    - `test_router_auto_not_interrupted_by_auto_command`: Create ctx with remaining_auto_scenes=5 and user_message="/auto 5", verify remaining_auto_scenes stays 5
    - `test_improv_director_has_phase5_tools`: Verify _improv_director.tools includes auto_advance, steer_drama, end_drama, trigger_storm
    - `test_improv_prompt_contains_seven_sections`: Verify _improv_director.instruction contains "核心循环协议", "自动推进协议", "用户引导与干预", "终幕协议", "视角审视", "选项呈现规范", "输出格式"

    Note: Router tests should mock the sub-agents' `run_async` method to avoid actual LLM calls.
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && uv run pytest tests/unit/test_agent.py -x -q</automated>
  </verify>
  <done>
    - _improv_director prompt has 7-section structure (核心循环协议, 自动推进协议, 用户引导与干预, 终幕协议, 视角审视, 选项呈现规范, 输出格式)
    - _improv_director tools list includes auto_advance, steer_drama, end_drama, trigger_storm
    - DramaRouter routes /auto, /steer, /end, /storm to improv_director
    - DramaRouter clears remaining_auto_scenes on non-/auto input during auto-advance (D-02)
    - All 8 router/prompt tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLI help text + function call display for Phase 5 commands</name>
  <files>cli.py, tests/unit/test_agent.py</files>
  <read_first>
    cli.py (full file — print_banner, _send_message, run_interactive)
    app/agent.py (updated _improv_director tools list from Task 1)
  </read_first>
  <action>
    **Step 1: Update print_banner() in cli.py**

    In `print_banner()` (lines 32-59), update the command list section to add Phase 5 commands. Replace the existing command list block with:

    ```python
    print("  命令列表:")
    print("  /start <主题>   - 开始新剧作")
    print("  /next           - 推进下一场")
    print("  /action <描述>  - 注入具体事件")
    print("  /steer <方向>   - 设置下一场方向引导")
    print("  /auto [N]       - 自动推进 N 场（默认3场）")
    print("  /end            - 终幕：生成终幕旁白 + 导出剧本")
    print("  /storm [焦点]   - 触发视角审视")
    print("  /save [名称]    - 保存进度（含对话记录）")
    print("  /load <名称>    - 加载进度")
    print("  /export         - 导出剧本为 Markdown")
    print("  /list           - 列出所有已保存的剧本")
    print("  /cast           - 查看角色列表（含A2A状态）")
    print("  /status         - 查看当前状态")
    print("  /help           - 显示帮助")
    print("  /quit           - 退出系统（自动保存）")
    ```

    **Step 2: Update _send_message() function call display list**

    In `_send_message()` (around line 140-148), the existing code displays function calls for certain tool names. Add the Phase 5 tool names to the display list:

    ```python
    if fn_name in [
        "start_drama",
        "create_actor",
        "next_scene",
        "user_action",
        "save_drama",
        "load_drama",
        "export_drama",
        "auto_advance",    # Phase 5
        "steer_drama",     # Phase 5
        "end_drama",       # Phase 5
        "trigger_storm",   # Phase 5
    ]:
    ```

    **Step 3: Add /auto default handling in CLI (per D-04)**

    In `run_interactive()` (around line 77-103), add a preprocessing step before sending to the agent. When the user types just `/auto` (without a number), expand it to `/auto 3`:

    After `user_input = input("\n🎬 你: ").strip()` and the empty check, add:
    ```python
    # D-04: /auto without number defaults to 3 scenes
    if user_input.lower() == "/auto":
        user_input = "/auto 3"
    ```

    **Step 4: Verify no regressions with existing tests**

    Run the full test suite to confirm everything works together.
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && uv run pytest tests/unit/ -x -q</automated>
  </verify>
  <done>
    - CLI banner lists /auto, /steer, /end, /storm commands
    - CLI _send_message displays auto_advance, steer_drama, end_drama, trigger_storm function calls
    - /auto without number defaults to 3 scenes (D-04)
    - All unit tests pass with no regressions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → DramaRouter → _improv_director | User commands flow through router to LLM agent |
| User input during auto-advance | Non-/auto input must interrupt auto-advance (D-02 safety net) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Denial of Service | DramaRouter auto-interrupt | mitigate | Code-level safety net: remaining_auto_scenes cleared on non-/auto input (D-02, Pitfall 2) |
| T-05-08 | Tampering | _improv_director prompt | accept | Prompt injection via /steer is user-facing by design; no untrusted third-party input flows through |
| T-05-09 | Denial of Service | auto_advance token consumption | mitigate | Soft cap 10 + warning (D-05) + code-level counter decrement in next_scene() (A4 mitigation) |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_agent.py -x -q` — all agent tests pass
- `uv run pytest tests/unit/ -x -q` — full unit test suite green
- Manual smoke test: start drama, use /auto 3, verify 3 scenes advance then stop
- Manual smoke test: use /steer, verify next scene reflects direction
- Manual smoke test: use /end, verify epilogue and export
- Manual smoke test: use /storm, verify perspective review output
</verification>

<success_criteria>
1. _improv_director prompt has 7-section structure per D-26
2. All 4 new tools (auto_advance, steer_drama, end_drama, trigger_storm) registered in _improv_director
3. DramaRouter routes /auto, /steer, /end, /storm to improv_director
4. DramaRouter auto-interrupt safety net clears remaining_auto_scenes on non-/auto input (D-02)
5. CLI lists all Phase 5 commands in help text
6. CLI displays Phase 5 function calls in _send_message
7. /auto defaults to 3 scenes when no number provided (D-04)
8. All unit tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-mixed-autonomy-mode/05-03-SUMMARY.md`
</output>
