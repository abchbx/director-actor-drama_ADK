---
phase: 17-android-interaction
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - app/api/routers/queries.py
  - app/api/models.py
  - app/state_manager.py
  - android/app/src/main/java/com/drama/app/data/remote/dto/SceneDto.kt
  - android/app/src/main/java/com/drama/app/data/remote/api/DramaApiService.kt
  - android/app/src/main/java/com/drama/app/domain/repository/DramaRepository.kt
  - android/app/src/main/java/com/drama/app/data/repository/DramaRepositoryImpl.kt
  - android/app/src/main/java/com/drama/app/ui/screens/dramadetail/DramaDetailViewModel.kt
  - android/app/src/main/java/com/drama/app/ui/screens/dramadetail/DramaDetailScreen.kt
  - android/app/src/main/java/com/drama/app/ui/screens/dramadetail/components/SceneHistorySheet.kt
autonomous: true
requirements: [APP-06, APP-12]

must_haves:
  truths:
    - "User can browse scene history in a bottom sheet"
    - "User can click a historical scene to view its content"
    - "Save operation shows confirmation feedback via Snackbar"
    - "Load operation shows confirmation feedback via Snackbar"
  artifacts:
    - path: "android/app/src/main/java/com/drama/app/ui/screens/dramadetail/components/SceneHistorySheet.kt"
      provides: "ModalBottomSheet for scene history browsing"
      contains: "SceneHistorySheet"
    - path: "app/api/routers/queries.py"
      provides: "GET /drama/scenes endpoint for scene list"
      contains: "get_drama_scenes"
    - path: "android/app/src/main/java/com/drama/app/data/remote/dto/SceneDto.kt"
      provides: "Scene summary DTO for API response"
      contains: "SceneSummaryDto"
  key_links:
    - from: "SceneHistorySheet"
      to: "DramaDetailViewModel"
      via: "onSceneClick callback"
      pattern: "onSceneClick"
    - from: "DramaDetailViewModel"
      to: "DramaApiService.getDramaScenes()"
      via: "DramaRepository.getScenes()"
      pattern: "getDramaScenes"
    - from: "DramaDetailScreen"
      to: "SnackbarHostState"
      via: "save/load confirmation"
      pattern: "showSnackbar"
---

<objective>
实现场景历史浏览和保存/加载确认反馈，包括后端新增场景列表 API 端点。

Purpose: 用户可以回顾历史场景、保存/加载戏剧进度并获得确认反馈。场景历史是 Phase 17 成功标准 #5，保存/加载确认是 #6。

Output: GET /drama/scenes 后端端点、SceneHistorySheet ModalBottomSheet、保存/加载 Snackbar 确认、历史场景查看
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-android-interaction/17-CONTEXT.md
@.planning/phases/17-android-interaction/17-RESEARCH.md
@.planning/phases/17-android-interaction/17-02-SUMMARY.md

<interfaces>
<!-- Key types from Plan 02 that this plan depends on -->

From Plan 02 — DramaDetailViewModel:
```kotlin
@HiltViewModel
class DramaDetailViewModel @Inject constructor(
    private val dramaRepository: DramaRepository,
    private val webSocketManager: WebSocketManager,
    private val serverPreferences: ServerPreferences,
    savedStateHandle: SavedStateHandle,
) : ViewModel()
```

From Plan 02 — DramaDetailUiState:
```kotlin
data class DramaDetailUiState(
    val theme: String = "",
    val currentScene: Int = 0,
    val tensionScore: Int = 0,
    val bubbles: List<SceneBubble> = emptyList(),
    val isTyping: Boolean = false,
    val isProcessing: Boolean = false,
    val stormPhase: String? = null,
    val isWsConnected: Boolean = false,
    val error: String? = null,
)
```

From Plan 01 — DramaRepository interface:
```kotlin
interface DramaRepository {
    suspend fun saveDrama(saveName: String = ""): Result<SaveLoadResponseDto>
    suspend fun loadDrama(saveName: String): Result<SaveLoadResponseDto>
    // ... other methods
}
```

From app/state_manager.py — load_archived_scene function:
```python
def load_archived_scene(theme: str, scene_num: int) -> dict | None:
    """Load a single archived scene from disk."""
    archive_path = os.path.join(_get_drama_folder(theme), "scenes", f"scene_{scene_num:04d}.json")
    if os.path.exists(archive_path):
        with open(archive_path, "r", encoding="utf-8") as f:
            return json.load(f)
    return None
```

From app/api/routers/queries.py:
```python
@router.get("/drama/status", response_model=DramaStatusResponse)
async def get_status(...)
```

From app/api/deps.py:
```python
def get_tool_context(...)
def require_auth(...)
```

From android/app/src/main/java/com/drama/app/data/remote/dto/WsEventDto.kt:
```kotlin
@Serializable
data class WsEventDto(val type: String, val timestamp: String, val data: Map<String, JsonElement> = emptyMap())
```

From android/app/src/main/java/com/drama/app/domain/model/SceneBubble.kt:
```kotlin
sealed class SceneBubble {
    abstract val id: String
    data class Narration(...) : SceneBubble()
    data class Dialogue(...) : SceneBubble()
    data class SceneDivider(...) : SceneBubble()
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端场景列表 API + Android 场景历史 BottomSheet + 保存/加载确认</name>
  <files>
    app/state_manager.py,
    app/api/models.py,
    app/api/routers/queries.py,
    android/app/src/main/java/com/drama/app/data/remote/dto/SceneDto.kt,
    android/app/src/main/java/com/drama/app/data/remote/api/DramaApiService.kt,
    android/app/src/main/java/com/drama/app/domain/repository/DramaRepository.kt,
    android/app/src/main/java/com/drama/app/data/repository/DramaRepositoryImpl.kt,
    android/app/src/main/java/com/drama/app/ui/screens/dramadetail/DramaDetailViewModel.kt,
    android/app/src/main/java/com/drama/app/ui/screens/dramadetail/components/SceneHistorySheet.kt
  </files>
  <read_first>
    app/state_manager.py,
    app/api/models.py,
    app/api/routers/queries.py,
    android/app/src/main/java/com/drama/app/data/remote/api/DramaApiService.kt,
    android/app/src/main/java/com/drama/app/domain/repository/DramaRepository.kt,
    android/app/src/main/java/com/drama/app/data/repository/DramaRepositoryImpl.kt,
    android/app/src/main/java/com/drama/app/ui/screens/dramadetail/DramaDetailViewModel.kt
  </read_first>
  <action>
    **Part A: 后端新增 GET /drama/scenes 端点（D-21 关键缺口）**

    1. 在 `app/state_manager.py` 中新增 `get_scene_summaries(tool_context) -> dict` 函数：
       - 从 `tool_context.state` 读取 `drama.theme` 和 `num_scenes`
       - 遍历 scene 1~num_scenes，对每个场景调用 `load_archived_scene(theme, scene_num)`
       - 如果 `load_archived_scene` 返回 None，跳过该场景
       - 对每个找到的场景，提取摘要：`{"scene_number": scene_num, "title": scene.get("title", ""), "description": (scene.get("narration", "") 或 scene.get("content", ""))[:50]}`
       - 返回 `{"scenes": [summaries], "total": len(summaries)}`

       同时新增 `get_scene_detail(theme: str, scene_num: int) -> dict` 函数：
       - 调用 `load_archived_scene(theme, scene_num)`
       - 如果返回 None，返回 `{"status": "error", "message": f"Scene {scene_num} not found"}`
       - 否则返回 scene dict 原始数据

    2. 在 `app/api/models.py` 中新增场景相关模型：
       ```python
       class SceneSummaryItem(BaseModel):
           scene_number: int
           title: str = ""
           description: str = ""

       class ScenesResponse(BaseModel):
           scenes: list[SceneSummaryItem] = Field(default_factory=list)
           total: int = 0

       class SceneDetailResponse(BaseModel):
           scene_number: int = 0
           title: str = ""
           narration: str = ""
           dialogue: list[dict] = Field(default_factory=list)
           raw: dict = Field(default_factory=dict)
       ```

    3. 在 `app/api/routers/queries.py` 中新增两个端点：
       ```python
       @router.get("/drama/scenes", response_model=ScenesResponse)
       async def get_drama_scenes(
           _auth: bool = Depends(require_auth),
           tool_context=Depends(get_tool_context),
       ):
           _require_active_drama(tool_context)
           from app.state_manager import get_scene_summaries
           result = get_scene_summaries(tool_context)
           return ScenesResponse(**result)

       @router.get("/drama/scenes/{scene_number}", response_model=SceneDetailResponse)
       async def get_drama_scene_detail(
           scene_number: int,
           _auth: bool = Depends(require_auth),
           tool_context=Depends(get_tool_context),
       ):
           _require_active_drama(tool_context)
           theme = tool_context.state.get("drama", {}).get("theme", "")
           from app.state_manager import get_scene_detail
           result = get_scene_detail(theme, scene_number)
           if result.get("status") == "error":
               raise HTTPException(status_code=404, detail=result["message"])
           return SceneDetailResponse(
               scene_number=scene_number,
               title=result.get("title", ""),
               narration=result.get("narration", ""),
               dialogue=result.get("dialogue", []),
               raw=result,
           )
       ```

    **Part B: Android DTO + API + Repository 扩展**

    4. 创建 `android/app/src/main/java/com/drama/app/data/remote/dto/SceneDto.kt`：
       ```kotlin
       @Serializable
       data class SceneSummaryDto(
           val scene_number: Int = 0,
           val title: String = "",
           val description: String = "",
       )

       @Serializable
       data class ScenesResponseDto(
           val scenes: List<SceneSummaryDto> = emptyList(),
           val total: Int = 0,
       )

       @Serializable
       data class SceneDetailDto(
           val scene_number: Int = 0,
           val title: String = "",
           val narration: String = "",
           val dialogue: List<Map<String, JsonElement>> = emptyList(),
       )
       ```

    5. 在 `DramaApiService.kt` 新增两个方法：
       ```kotlin
       @GET("drama/scenes")
       suspend fun getDramaScenes(): ScenesResponseDto

       @GET("drama/scenes/{sceneNumber}")
       suspend fun getDramaSceneDetail(@Path("sceneNumber") sceneNumber: Int): SceneDetailDto
       ```

    6. 在 `DramaRepository` 接口新增：
       ```kotlin
       suspend fun getScenes(): Result<ScenesResponseDto>
       suspend fun getSceneDetail(sceneNumber: Int): Result<SceneDetailDto>
       ```

    7. 在 `DramaRepositoryImpl` 实现新增方法：
       ```kotlin
       override suspend fun getScenes(): Result<ScenesResponseDto> = runCatching {
           dramaApiService.getDramaScenes()
       }
       override suspend fun getSceneDetail(sceneNumber: Int): Result<SceneDetailDto> = runCatching {
           dramaApiService.getDramaSceneDetail(sceneNumber)
       }
       ```

    **Part C: SceneHistorySheet 组件（D-18~D-20）**

    8. 创建 `android/app/src/main/java/com/drama/app/ui/screens/dramadetail/components/SceneHistorySheet.kt`：
       ```kotlin
       @OptIn(ExperimentalMaterial3Api::class)
       @Composable
       fun SceneHistorySheet(
           scenes: List<SceneSummaryDto>,
           onSceneClick: (Int) -> Unit,
           onDismiss: () -> Unit,
       ) {
           val sheetState = rememberModalBottomSheetState(skipPartiallyExpanded = false)

           ModalBottomSheet(
               onDismissRequest = onDismiss,
               sheetState = sheetState,
           ) {
               Text(
                   text = "场景历史",
                   style = MaterialTheme.typography.titleMedium,
                   modifier = Modifier.padding(horizontal = 16.dp, vertical = 8.dp),
               )
               if (scenes.isEmpty()) {
                   Box(Modifier.fillMaxWidth().padding(32.dp), contentAlignment = Alignment.Center) {
                       Text("暂无场景记录")
                   }
               } else {
                   LazyColumn(modifier = Modifier.padding(horizontal = 16.dp)) {
                       items(scenes, key = { it.scene_number }) { scene ->
                           ListItem(
                               headlineContent = { Text("第 ${scene.scene_number} 场") },
                               supportingContent = {
                                   if (scene.title.isNotBlank()) Text(scene.title)
                                   if (scene.description.isNotBlank()) Text(scene.description, maxLines = 1)
                               },
                               modifier = Modifier.clickable { onSceneClick(scene.scene_number) },
                           )
                       }
                   }
               }
               Spacer(modifier = Modifier.height(32.dp))
           }
       }
       ```

    **Part D: DramaDetailViewModel 扩展**

    9. 修改 `DramaDetailViewModel.kt`，新增以下功能：

       - 在 `DramaDetailUiState` 中新增字段：
         ```kotlin
         val viewingHistoryScene: Int? = null,
         val historyScenes: List<SceneSummaryDto> = emptyList(),
         val showHistorySheet: Boolean = false,
         val showSaveDialog: Boolean = false,
         ```

       - 新增方法：
         ```kotlin
         fun loadScenes() {
             viewModelScope.launch {
                 dramaRepository.getScenes()
                     .onSuccess { response ->
                         _uiState.update { it.copy(historyScenes = response.scenes) }
                     }
             }
         }

         fun showHistorySheet() {
             loadScenes()
             _uiState.update { it.copy(showHistorySheet = true) }
         }

         fun hideHistorySheet() {
             _uiState.update { it.copy(showHistorySheet = false) }
         }

         fun viewHistoryScene(sceneNumber: Int) {
             viewModelScope.launch {
                 dramaRepository.getSceneDetail(sceneNumber)
                     .onSuccess { detail ->
                         // D-20: 替换主屏幕内容显示该场景
                         val historyBubbles = mutableListOf<SceneBubble>()
                         if (detail.narration.isNotBlank()) {
                             historyBubbles.add(SceneBubble.Narration(id = "hist_${sceneNumber}_n", text = detail.narration))
                         }
                         for ((idx, d) in detail.dialogue.withIndex()) {
                             val actorName = d["actor_name"]?.jsonPrimitive?.contentOrNull ?: ""
                             val text = d["text"]?.jsonPrimitive?.contentOrNull ?: ""
                             historyBubbles.add(SceneBubble.Dialogue(id = "hist_${sceneNumber}_d$idx", actorName = actorName, text = text))
                         }
                         _uiState.update { it.copy(
                             viewingHistoryScene = sceneNumber,
                             bubbles = historyBubbles,
                             showHistorySheet = false,
                         ) }
                     }
             }
         }

         fun returnToCurrentScene() {
             _uiState.update { it.copy(viewingHistoryScene = null) }
             // 重新连接 WS 以获取当前场景
             connectWebSocket()
         }

         // D-23: 保存操作
         fun showSaveDialog() {
             _uiState.update { it.copy(showSaveDialog = true) }
         }

         fun hideSaveDialog() {
             _uiState.update { it.copy(showSaveDialog = false) }
         }

         fun saveDrama(saveName: String = "") {
             viewModelScope.launch {
                 dramaRepository.saveDrama(saveName)
                     .onSuccess { response ->
                         _events.emit(DramaDetailEvent.ShowSnackbar("已保存：${saveName.ifBlank { response.theme }}"))
                     }
                     .onFailure { e ->
                         _events.emit(DramaDetailEvent.ShowSnackbar("保存失败：${e.message}"))
                     }
                 _uiState.update { it.copy(showSaveDialog = false) }
             }
         }
         ```

       - 处理 WS `save_confirm` 和 `load_confirm` 事件（D-22）：
         在 `handleWsEvent` 中新增：
         ```kotlin
         "save_confirm" -> {
             val msg = event.data["message"]?.jsonPrimitive?.contentOrNull ?: "已保存"
             _uiState.update { it.copy(isTyping = false) }
             viewModelScope.launch { _events.emit(DramaDetailEvent.ShowSnackbar(msg)) }
         }
         "load_confirm" -> {
             val msg = event.data["message"]?.jsonPrimitive?.contentOrNull ?: "已加载"
             _uiState.update { it.copy(isTyping = false) }
             viewModelScope.launch { _events.emit(DramaDetailEvent.ShowSnackbar(msg)) }
         }
         ```
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && grep -q "get_scene_summaries" app/state_manager.py && grep -q "ScenesResponse" app/api/models.py && grep -q "drama/scenes" app/api/routers/queries.py && grep -q "SceneSummaryDto" android/app/src/main/java/com/drama/app/data/remote/dto/SceneDto.kt && grep -q "getDramaScenes" android/app/src/main/java/com/drama/app/data/remote/api/DramaApiService.kt && grep -q "SceneHistorySheet" android/app/src/main/java/com/drama/app/ui/screens/dramadetail/components/SceneHistorySheet.kt && grep -q "showHistorySheet\|viewHistoryScene\|saveDrama\|showSaveDialog" android/app/src/main/java/com/drama/app/ui/screens/dramadetail/DramaDetailViewModel.kt && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - 后端 GET /drama/scenes 返回场景摘要列表，GET /drama/scenes/{scene_number} 返回完整场景
    - SceneHistorySheet 用 ModalBottomSheet 展示场景摘要列表（D-18/D-19）
    - 点击历史场景替换主屏幕内容显示该场景，TopAppBar 出现返回按钮（D-20）
    - 保存操作通过 TopAppBar 溢出菜单入口，弹 Dialog 输入保存名（D-23）
    - 保存/加载操作 Snackbar 确认，成功/失败都有反馈（D-22）
    - WS save_confirm/load_confirm 事件触发 Snackbar 确认
  </done>
</task>

<task type="auto">
  <name>Task 2: DramaDetailScreen 整合历史面板 + 保存 Dialog + 导航更新</name>
  <files>
    android/app/src/main/java/com/drama/app/ui/screens/dramadetail/DramaDetailScreen.kt
  </files>
  <read_first>
    android/app/src/main/java/com/drama/app/ui/screens/dramadetail/DramaDetailScreen.kt,
    android/app/src/main/java/com/drama/app/ui/screens/dramadetail/DramaDetailViewModel.kt,
    android/app/src/main/java/com/drama/app/ui/screens/dramadetail/components/SceneHistorySheet.kt
  </read_first>
  <action>
    重写 `DramaDetailScreen.kt`，在 Plan 02 基础上整合以下新功能：

    1. **历史按钮（D-18）**：TopAppBar actions 中新增 `IconButton(onClick = viewModel::showHistorySheet)` + `Icon(Icons.Filled.History, "场景历史")`，放在 TensionIndicator 之前

    2. **保存入口（D-23）**：TopAppBar 新增 `overflow` 菜单：
       ```kotlin
       var showOverflowMenu by remember { mutableStateOf(false) }
       TopAppBar(
           actions = {
               TensionIndicator(score = uiState.tensionScore)
               IconButton(onClick = viewModel::showHistorySheet) {
                   Icon(Icons.Filled.History, "场景历史")
               }
               Box {
                   IconButton(onClick = { showOverflowMenu = true }) {
                       Icon(Icons.Filled.MoreVert, "更多")
                   }
                   DropdownMenu(expanded = showOverflowMenu, onDismissRequest = { showOverflowMenu = false }) {
                       DropdownMenuItem(
                           text = { Text("保存") },
                           onClick = { viewModel.showSaveDialog(); showOverflowMenu = false },
                       )
                   }
               }
           },
       )
       ```

    3. **返回按钮（D-20）**：当 `uiState.viewingHistoryScene != null` 时，TopAppBar navigationIcon 显示返回按钮：
       ```kotlin
       if (uiState.viewingHistoryScene != null) {
           IconButton(onClick = viewModel::returnToCurrentScene) {
               Icon(Icons.Filled.ArrowBack, "返回当前场景")
           }
       }
       ```

    4. **保存 Dialog（D-23）**：
       ```kotlin
       if (uiState.showSaveDialog) {
           var saveName by remember { mutableStateOf("") }
           AlertDialog(
               title = { Text("保存戏剧") },
               text = {
                   OutlinedTextField(
                       value = saveName,
                       onValueChange = { saveName = it },
                       placeholder = { Text("保存名（可选，默认用主题名）") },
                       singleLine = true,
                   )
               },
               confirmButton = {
                   TextButton(onClick = { viewModel.saveDrama(saveName) }) { Text("保存") }
               },
               dismissButton = {
                   TextButton(onClick = viewModel::hideSaveDialog) { Text("取消") }
               },
           )
       }
       ```

    5. **SceneHistorySheet（D-18/D-19）**：
       ```kotlin
       if (uiState.showHistorySheet) {
           SceneHistorySheet(
               scenes = uiState.historyScenes,
               onSceneClick = viewModel::viewHistoryScene,
               onDismiss = viewModel::hideHistorySheet,
           )
       }
       ```

    6. **WS 事件连接状态标签**：当 `uiState.viewingHistoryScene != null` 时，在 TopAppBar title 中显示 "查看第 X 场（历史）" 代替当前场景号
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && grep -q "SceneHistorySheet" android/app/src/main/java/com/drama/app/ui/screens/dramadetail/DramaDetailScreen.kt && grep -q "showSaveDialog" android/app/src/main/java/com/drama/app/ui/screens/dramadetail/DramaDetailScreen.kt && grep -q "AlertDialog" android/app/src/main/java/com/drama/app/ui/screens/dramadetail/DramaDetailScreen.kt && grep -q "viewHistoryScene" android/app/src/main/java/com/drama/app/ui/screens/dramadetail/DramaDetailScreen.kt && grep -q "Icons.Filled.History" android/app/src/main/java/com/drama/app/ui/screens/dramadetail/DramaDetailScreen.kt && grep -q "Icons.Filled.MoreVert" android/app/src/main/java/com/drama/app/ui/screens/dramadetail/DramaDetailScreen.kt && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - 历史按钮在 TopAppBar actions 中，点击弹出 SceneHistorySheet
    - 场景历史 BottomSheet 显示摘要列表，点击跳转历史场景
    - 查看历史场景时 TopAppBar 出现返回按钮
    - 保存入口在 TopAppBar 溢出菜单，弹 Dialog 输入保存名
    - 保存/加载操作 Snackbar 确认反馈
    - DramaDetailScreen 完整整合所有交互功能
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android→GET /drama/scenes | Read-only query, authenticated |
| Android→GET /drama/scenes/{n} | Read-only query, scene number is path param |
| Save Dialog input→POST /drama/save | save_name text input crosses to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08 | Tampering | scene_number path param | mitigate | FastAPI path param type=int rejects non-integer; validate range 1-999 |
| T-17-09 | Information Disclosure | Scene history API | accept | Single-user LAN, all scenes visible to sole user |
| T-17-10 | Tampering | save_name input | mitigate | Backend SaveRequest.save_name has default="" and is optional; no injection risk in file-based storage |
</threat_model>

<verification>
1. 后端 GET /drama/scenes 返回场景摘要列表
2. 后端 GET /drama/scenes/{scene_number} 返回完整场景详情
3. SceneHistorySheet 用 ModalBottomSheet 显示场景摘要
4. 点击历史场景替换主屏幕内容
5. 返回按钮回到当前场景
6. 保存 Dialog 输入保存名并调用 API
7. Snackbar 确认保存/加载操作
8. WS save_confirm/load_confirm 事件触发 Snackbar
</verification>

<success_criteria>
- 场景历史可滚动浏览，BottomSheet 展示场景摘要列表
- 点击历史场景替换主屏幕内容，TopAppBar 出现返回按钮回到当前场景
- 保存/加载操作返回确认反馈（Snackbar 提示）
- 后端 GET /drama/scenes 端点返回场景摘要数据
</success_criteria>

<output>
After completion, create `.planning/phases/17-android-interaction/17-03-SUMMARY.md`
</output>
