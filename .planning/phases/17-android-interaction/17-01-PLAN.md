---
phase: 17-android-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/drama/app/data/remote/dto/DramaItemDto.kt
  - android/app/src/main/java/com/drama/app/data/remote/dto/DramaListResponseDto.kt
  - android/app/src/main/java/com/drama/app/data/remote/api/DramaApiService.kt
  - android/app/src/main/java/com/drama/app/domain/model/Drama.kt
  - android/app/src/main/java/com/drama/app/domain/repository/DramaRepository.kt
  - android/app/src/main/java/com/drama/app/data/repository/DramaRepositoryImpl.kt
  - android/app/src/main/java/com/drama/app/di/DramaModule.kt
  - android/app/src/main/java/com/drama/app/ui/screens/dramacreate/DramaCreateViewModel.kt
  - android/app/src/main/java/com/drama/app/ui/screens/dramacreate/DramaCreateScreen.kt
  - android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListViewModel.kt
  - android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListScreen.kt
  - android/app/src/main/java/com/drama/app/ui/navigation/DramaNavHost.kt
  - app/api/routers/queries.py
  - app/api/models.py
  - app/state_manager.py
autonomous: true
requirements: [APP-02, APP-03]

must_haves:
  truths:
    - "User can create a new drama by entering a theme and see STORM progress"
    - "User can see all saved dramas in a card list"
    - "User can load/resume/delete a drama from the list"
    - "Drama creation auto-navigates to detail on scene_start event"
  artifacts:
    - path: "android/app/src/main/java/com/drama/app/ui/screens/dramacreate/DramaCreateScreen.kt"
      provides: "Full-screen drama creation with STORM progress"
      contains: "DramaCreateScreen"
    - path: "android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListScreen.kt"
      provides: "Drama list with cards, three-dot menu, empty state"
      contains: "DramaListScreen"
    - path: "android/app/src/main/java/com/drama/app/domain/model/Drama.kt"
      provides: "Drama domain model"
      contains: "data class Drama"
    - path: "android/app/src/main/java/com/drama/app/data/repository/DramaRepositoryImpl.kt"
      provides: "Drama CRUD repository implementation"
      contains: "DramaRepositoryImpl"
  key_links:
    - from: "DramaCreateViewModel"
      to: "DramaApiService.startDrama()"
      via: "DramaRepository.startDrama()"
      pattern: "startDrama"
    - from: "DramaListViewModel"
      to: "DramaApiService.listDramas()"
      via: "DramaRepository.listDramas()"
      pattern: "listDramas"
    - from: "DramaListScreen"
      to: "DramaDetail(dramaId)"
      via: "navController.navigate"
      pattern: "navigate.*DramaDetail"
---

<objective>
实现戏剧创建屏幕和戏剧列表屏幕，包括后端新增删除戏剧 API 和 DramaItemDto 细化。

Purpose: 完成戏剧 CRUD 的创建和列表管理，是 Android 端业务逻辑的入口。用户创建戏剧后看到 STORM 进度，完成后自动跳转主屏幕；列表管理支持加载/恢复/删除操作。

Output: DramaCreateScreen（全屏创建 + STORM 进度）、DramaListScreen（紧凑卡片 + 三点菜单 + 空状态）、DramaRepository + DramaItemDto 细化、后端 DELETE /drama/{folder} 端点
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-android-interaction/17-CONTEXT.md
@.planning/phases/17-android-interaction/17-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From android/app/src/main/java/com/drama/app/data/remote/dto/DramaListResponseDto.kt:
```kotlin
@Serializable
data class DramaListResponseDto(
    val dramas: List<Map<String, JsonElement>> = emptyList(),
)
```
→ This will be REPLACED with typed DramaItemDto (D-10)

From android/app/src/main/java/com/drama/app/data/remote/dto/RequestDtos.kt:
```kotlin
@Serializable data class StartDramaRequestDto(val theme: String)
@Serializable data class SaveRequestDto(val save_name: String = "")
@Serializable data class LoadRequestDto(val save_name: String)
```

From android/app/src/main/java/com/drama/app/data/remote/dto/CommandResponseDto.kt:
```kotlin
@Serializable
data class CommandResponseDto(
    val final_response: String = "",
    val tool_results: List<Map<String, JsonElement>> = emptyList(),
    val status: String = "success",
    val message: String = "",
)
```

From android/app/src/main/java/com/drama/app/data/remote/dto/SaveLoadResponseDto.kt:
```kotlin
@Serializable
data class SaveLoadResponseDto(
    val status: String = "success",
    val message: String = "",
    val theme: String = "",
    val drama_status: String = "",
    val current_scene: Int = 0,
    val num_actors: Int = 0,
    val num_scenes: Int = 0,
    val actors_list: List<String> = emptyList(),
)
```

From android/app/src/main/java/com/drama/app/data/remote/ws/WebSocketManager.kt:
```kotlin
fun connect(host: String, port: String, token: String?): Flow<WsEventDto>
fun disconnect()
```

From android/app/src/main/java/com/drama/app/data/remote/dto/WsEventDto.kt:
```kotlin
@Serializable
data class WsEventDto(
    val type: String,
    val timestamp: String,
    val data: Map<String, JsonElement> = emptyMap(),
)
@Serializable
data class ReplayMessageDto(
    val type: String = "replay",
    val events: List<WsEventDto> = emptyList(),
)
```

From android/app/src/main/java/com/drama/app/data/local/ServerPreferences.kt:
```kotlin
val serverConfig: Flow<ServerConfig?>
```

From android/app/src/main/java/com/drama/app/ui/navigation/Route.kt:
```kotlin
@Serializable object DramaList
@Serializable object DramaCreate
@Serializable data class DramaDetail(val dramaId: String)
```

From app/state_manager.py list_dramas() return (line 790-797):
```python
dramas.append({
    "folder": folder,
    "theme": data.get("theme", folder),
    "status": data.get("status", "unknown"),
    "updated_at": data.get("updated_at", "Unknown"),
    "current_scene": data.get("current_scene", 0),
})
```

From app/api/models.py:
```python
class DramaListResponse(BaseModel):
    dramas: list[dict] = Field(default_factory=list)
```

From app/api/routers/queries.py:
```python
@router.get("/drama/list", response_model=DramaListResponse)
async def list_all_dramas(_auth: bool = Depends(require_auth)):
    result = list_dramas()
    return DramaListResponse(**result)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端删除戏剧 API + DramaItemDto/Repository/DI 层</name>
  <files>
    app/api/routers/queries.py,
    app/api/models.py,
    app/state_manager.py,
    android/app/src/main/java/com/drama/app/data/remote/dto/DramaItemDto.kt,
    android/app/src/main/java/com/drama/app/data/remote/dto/DramaListResponseDto.kt,
    android/app/src/main/java/com/drama/app/data/remote/api/DramaApiService.kt,
    android/app/src/main/java/com/drama/app/domain/model/Drama.kt,
    android/app/src/main/java/com/drama/app/domain/repository/DramaRepository.kt,
    android/app/src/main/java/com/drama/app/data/repository/DramaRepositoryImpl.kt,
    android/app/src/main/java/com/drama/app/di/DramaModule.kt
  </files>
  <read_first>
    app/api/routers/queries.py,
    app/api/models.py,
    app/state_manager.py,
    android/app/src/main/java/com/drama/app/data/remote/dto/DramaListResponseDto.kt,
    android/app/src/main/java/com/drama/app/data/remote/api/DramaApiService.kt,
    android/app/src/main/java/com/drama/app/di/NetworkModule.kt
  </read_first>
  <action>
    **Part A: 后端新增 DELETE /drama/{folder} 端点**

    1. 在 `app/state_manager.py` 中新增 `delete_drama(folder: str) -> dict` 函数：
       - 根据 `folder` 参数构建 `DRAMAS_DIR / folder` 路径
       - 如果文件夹存在，用 `shutil.rmtree()` 删除整个戏剧文件夹
       - 返回 `{"status": "success", "message": f"Deleted drama: {folder}"}`
       - 如果文件夹不存在，返回 `{"status": "error", "message": f"Drama not found: {folder}"}`

    2. 在 `app/api/models.py` 中新增 `DeleteDramaResponse` 模型：
       ```python
       class DeleteDramaResponse(BaseModel):
           status: str = "success"
           message: str = ""
       ```

    3. 在 `app/api/routers/queries.py` 中新增 `DELETE /drama/{folder}` 端点：
       ```python
       @router.delete("/drama/{folder}", response_model=DeleteDramaResponse)
       async def delete_drama(
           folder: str,
           _auth: bool = Depends(require_auth),
       ):
           from app.state_manager import delete_drama as delete_drama_fn
           result = delete_drama_fn(folder)
           if result.get("status") == "error":
               raise HTTPException(status_code=404, detail=result["message"])
           return DeleteDramaResponse(**result)
       ```

    **Part B: Android 数据层 — DramaItemDto + Repository + DI**

    4. 创建 `android/app/src/main/java/com/drama/app/data/remote/dto/DramaItemDto.kt`（D-10 细化）：
       ```kotlin
       @Serializable
       data class DramaItemDto(
           val folder: String = "",
           val theme: String = "",
           val status: String = "unknown",
           val updated_at: String = "Unknown",
           val current_scene: Int = 0,
           val snapshots: List<String> = emptyList(),
       )
       ```

    5. 修改 `DramaListResponseDto.kt`，替换 `List<Map<String, JsonElement>>` 为 `List<DramaItemDto>`：
       ```kotlin
       @Serializable
       data class DramaListResponseDto(
           val dramas: List<DramaItemDto> = emptyList(),
       )
       ```

    6. 在 `DramaApiService.kt` 新增 `deleteDrama` 方法：
       ```kotlin
       @DELETE("drama/{folder}")
       suspend fun deleteDrama(@Path("folder") folder: String): DeleteDramaResponseDto
       ```

    7. 创建 `DeleteDramaResponseDto.kt`：
       ```kotlin
       @Serializable
       data class DeleteDramaResponseDto(
           val status: String = "success",
           val message: String = "",
       )
       ```

    8. 创建 `android/app/src/main/java/com/drama/app/domain/model/Drama.kt`：
       ```kotlin
       data class Drama(
           val folder: String,
           val theme: String,
           val status: String,
           val updatedAt: String,
           val currentScene: Int,
           val snapshots: List<String> = emptyList(),
       )
       ```

    9. 创建 `android/app/src/main/java/com/drama/app/domain/repository/DramaRepository.kt`：
       ```kotlin
       interface DramaRepository {
           suspend fun startDrama(theme: String): Result<CommandResponseDto>
           suspend fun listDramas(): Result<List<Drama>>
           suspend fun deleteDrama(folder: String): Result<String>
           suspend fun saveDrama(saveName: String = ""): Result<SaveLoadResponseDto>
           suspend fun loadDrama(saveName: String): Result<SaveLoadResponseDto>
           suspend fun getDramaStatus(): Result<DramaStatusResponseDto>
           suspend fun nextScene(): Result<CommandResponseDto>
           suspend fun userAction(description: String): Result<CommandResponseDto>
           suspend fun actorSpeak(actorName: String, situation: String): Result<CommandResponseDto>
           suspend fun endDrama(): Result<CommandResponseDto>
       }
       ```

    10. 创建 `android/app/src/main/java/com/drama/app/data/repository/DramaRepositoryImpl.kt`：
        - `@Inject constructor(private val dramaApiService: DramaApiService)`
        - `startDrama`: 调用 `dramaApiService.startDrama(StartDramaRequestDto(theme))`
        - `listDramas`: 调用 `dramaApiService.listDramas()`，DTO→Domain 转换 `dramaItemDtoToDrama()`
        - `deleteDrama`: 调用 `dramaApiService.deleteDrama(folder)`，返回 `Result.success(folder)`
        - 所有方法包裹 `runCatching { ... }`，不手动 try-catch
        - `dramaItemDtoToDrama()` 私有函数：`DramaItemDto.folder→folder, .theme→theme, .status→status, .updated_at→updatedAt, .current_scene→currentScene, .snapshots→snapshots`

    11. 创建 `android/app/src/main/java/com/drama/app/di/DramaModule.kt`：
        ```kotlin
        @Module
        @InstallIn(SingletonComponent::class)
        abstract class DramaModule {
            @Binds
            @Singleton
            abstract fun bindDramaRepository(impl: DramaRepositoryImpl): DramaRepository
        }
        ```
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && grep -q "delete_drama" app/state_manager.py && grep -q "DeleteDramaResponse" app/api/models.py && grep -q "drama/{folder}" app/api/routers/queries.py && grep -q "DramaItemDto" android/app/src/main/java/com/drama/app/data/remote/dto/DramaItemDto.kt && grep -q "DramaRepository" android/app/src/main/java/com/drama/app/domain/repository/DramaRepository.kt && grep -q "DramaRepositoryImpl" android/app/src/main/java/com/drama/app/data/repository/DramaRepositoryImpl.kt && grep -q "DramaModule" android/app/src/main/java/com/drama/app/di/DramaModule.kt && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - 后端 `DELETE /drama/{folder}` 端点可删除指定戏剧文件夹
    - `DramaItemDto` 替代 `Map<String, JsonElement>` 提供类型安全的列表数据
    - `DramaRepository` 接口和实现完整覆盖 CRUD 操作
    - Hilt `DramaModule` 绑定 Repository 接口到实现
  </done>
</task>

<task type="auto">
  <name>Task 2: DramaCreateScreen + DramaListScreen 完整实现</name>
  <files>
    android/app/src/main/java/com/drama/app/ui/screens/dramacreate/DramaCreateViewModel.kt,
    android/app/src/main/java/com/drama/app/ui/screens/dramacreate/DramaCreateScreen.kt,
    android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListViewModel.kt,
    android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListScreen.kt,
    android/app/src/main/java/com/drama/app/ui/navigation/DramaNavHost.kt
  </files>
  <read_first>
    android/app/src/main/java/com/drama/app/ui/screens/dramacreate/DramaCreateViewModel.kt,
    android/app/src/main/java/com/drama/app/ui/screens/dramacreate/DramaCreateScreen.kt,
    android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListViewModel.kt,
    android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListScreen.kt,
    android/app/src/main/java/com/drama/app/ui/navigation/DramaNavHost.kt,
    android/app/src/main/java/com/drama/app/data/remote/dto/WsEventDto.kt,
    android/app/src/main/java/com/drama/app/ui/components/AppBottomNavigationBar.kt
  </read_first>
  <action>
    **Part A: DramaCreateViewModel + DramaCreateScreen（D-01~D-05）**

    1. 重写 `DramaCreateViewModel.kt`：
       ```kotlin
       @HiltViewModel
       class DramaCreateViewModel @Inject constructor(
           private val dramaRepository: DramaRepository,
           private val webSocketManager: WebSocketManager,
           private val serverPreferences: ServerPreferences,
       ) : ViewModel() {
           private val _uiState = MutableStateFlow(DramaCreateUiState())
           val uiState: StateFlow<DramaCreateUiState> = _uiState.asStateFlow()

           private val _events = MutableSharedFlow<DramaCreateEvent>()
           val events: SharedFlow<DramaCreateEvent> = _events.asSharedFlow()

           fun createDrama(theme: String) {
               if (theme.isBlank()) return
               viewModelScope.launch {
                   _uiState.update { it.copy(isCreating = true, error = null) }
                   // D-02: 调用 POST /drama/start 后进入加载态
                   dramaRepository.startDrama(theme)
                       .onSuccess {
                           // D-04: 不在此处导航，等 WS scene_start 事件
                           connectWebSocketForStormProgress()
                       }
                       .onFailure { e ->
                           _uiState.update { it.copy(isCreating = false, error = e.message) }
                       }
               }
           }

           private fun connectWebSocketForStormProgress() {
               viewModelScope.launch {
                   val config = serverPreferences.serverConfig.first() ?: return@launch
                   webSocketManager.connect(config.ip, config.port, config.token)
                       .catch { e -> _uiState.update { it.copy(error = e.message) } }
                       .collect { event -> handleStormEvent(event) }
               }
           }

           private fun handleStormEvent(event: WsEventDto) {
               when (event.type) {
                   // D-03: STORM 进度实时展示
                   "storm_discover" -> _uiState.update { it.copy(stormPhase = "发现新视角...") }
                   "storm_research" -> _uiState.update { it.copy(stormPhase = "深入研究...") }
                   "storm_outline" -> _uiState.update { it.copy(stormPhase = "综合构思大纲...") }
                   // D-04: 创建完成自动跳转
                   "scene_start" -> {
                       _uiState.update { it.copy(isCreating = false) }
                       viewModelScope.launch {
                           _events.emit(DramaCreateEvent.NavigateToDetail("current"))
                       }
                   }
                   "error" -> {
                       val msg = event.data["message"]?.jsonPrimitive?.contentOrNull ?: "Unknown error"
                       _uiState.update { it.copy(isCreating = false, error = msg) }
                   }
               }
           }

           override fun onCleared() {
               super.onCleared()
               webSocketManager.disconnect()
           }
       }
       ```

       定义 UiState 和 Event：
       ```kotlin
       data class DramaCreateUiState(
           val theme: String = "",
           val isCreating: Boolean = false,
           val stormPhase: String? = null,
           val error: String? = null,
       )

       sealed class DramaCreateEvent {
           data class NavigateToDetail(val dramaId: String) : DramaCreateEvent()
       }
       ```

    2. 重写 `DramaCreateScreen.kt`（D-01 全屏创建表单）：
       - 默认状态：`Box(Modifier.fillMaxSize())` 居中显示 `OutlinedTextField`
         - placeholder = "输入你的戏剧主题..."，大字体（`style = MaterialTheme.typography.headlineMedium`）
         - 单行输入，最大 200 字符
         - 下方 `Button(onClick = { viewModel.createDrama(theme) })` 文字"开始创作"
         - enabled = `!uiState.isCreating && theme.isNotBlank()`
       - 创建中状态（D-02）：`LinearProgressIndicator` + `Text("导演正在构思世界观...")`
         - 如果 `uiState.stormPhase != null`，显示 STORM 进度文案（D-03）：`Text(uiState.stormPhase)`
       - 错误状态：`Text(uiState.error, color = MaterialTheme.colorScheme.error)`
       - 收集 events：`LaunchedEffect(Unit) { viewModel.events.collect { ... } }`，NavigateToDetail 时调用 `onNavigateToDetail(dramaId)`

    **Part B: DramaListViewModel + DramaListScreen（D-06~D-10, D-24）**

    3. 重写 `DramaListViewModel.kt`：
       ```kotlin
       @HiltViewModel
       class DramaListViewModel @Inject constructor(
           private val dramaRepository: DramaRepository,
       ) : ViewModel() {
           private val _uiState = MutableStateFlow(DramaListUiState())
           val uiState: StateFlow<DramaListUiState> = _uiState.asStateFlow()

           private val _events = MutableSharedFlow<DramaListEvent>()
           val events: SharedFlow<DramaListEvent> = _events.asSharedFlow()

           init { loadDramas() }

           fun loadDramas() {
               viewModelScope.launch {
                   _uiState.update { it.copy(isLoading = true) }
                   dramaRepository.listDramas()
                       .onSuccess { dramas -> _uiState.update { it.copy(dramas = dramas, isLoading = false) } }
                       .onFailure { e -> _uiState.update { it.copy(error = e.message, isLoading = false) } }
               }
           }

           fun deleteDrama(folder: String) {
               viewModelScope.launch {
                   dramaRepository.deleteDrama(folder)
                       .onSuccess {
                           _uiState.update { it.copy(dramas = it.dramas.filter { d -> d.folder != folder }) }
                           _events.emit(DramaListEvent.ShowSnackbar("已删除：$folder"))
                       }
                       .onFailure { e -> _events.emit(DramaListEvent.ShowSnackbar("删除失败：${e.message}")) }
               }
           }

           fun loadDrama(folder: String) {
               viewModelScope.launch {
                   dramaRepository.loadDrama(folder)
                       .onSuccess { _events.emit(DramaListEvent.ShowSnackbar("已加载：$folder")) }
                       .onFailure { e -> _events.emit(DramaListEvent.ShowSnackbar("加载失败：${e.message}")) }
               }
           }
       }
       ```

       定义 UiState 和 Event：
       ```kotlin
       data class DramaListUiState(
           val dramas: List<Drama> = emptyList(),
           val isLoading: Boolean = false,
           val error: String? = null,
       )

       sealed class DramaListEvent {
           data class ShowSnackbar(val message: String) : DramaListEvent()
       }
       ```

    4. 重写 `DramaListScreen.kt`（D-06~D-10）：
       - Scaffold + SnackbarHost
       - 空列表状态（D-09）：居中 `Column`，`Icon(Icons.Filled.TheaterComedy, modifier = Modifier.size(120.dp))` + `Text("还没有戏剧，点击下方创建按钮开始创作")`
       - 列表：`LazyColumn` 渲染 `DramaCard` 列表
       - DramaCard（D-06 紧凑卡片）：`Card(modifier = Modifier.fillMaxWidth().padding(horizontal = 16.dp, vertical = 8.dp))`
         - 第一行：`Text(drama.theme, style = MaterialTheme.typography.titleLarge, fontWeight = FontWeight.Bold)` + `Badge { Text(drama.status) }`（D-06：状态 badge Active/Saved）
         - 第二行：`Text("${drama.currentScene} 场 · ${drama.updatedAt}")`
         - 三点菜单（D-07）：`IconButton(onClick = { showMenu = true })` + `Icon(Icons.Filled.MoreVert)` + `DropdownMenu`
           - "继续" → `onDramaClick(drama.folder)` — 直接导航到 DramaDetail
           - "加载存档" → `viewModel.loadDrama(drama.theme)`（D-24：调用 POST /drama/load）
           - "删除" → `showDeleteDialog = true`（D-08：AlertDialog 确认）
       - 删除确认 Dialog（D-08）：`AlertDialog(title = { Text("删除戏剧？") }, text = { Text("此操作不可恢复") }, confirmButton = { TextButton(onClick = { viewModel.deleteDrama(drama.folder); showDeleteDialog = false }) { Text("删除") } }, dismissButton = { TextButton(onClick = { showDeleteDialog = false }) { Text("取消") } })`
       - 收集 events 显示 Snackbar

    **Part C: 导航回调更新**

    5. 修改 `DramaNavHost.kt`：
       - `DramaCreateScreen` 新增 `onNavigateToDetail` 参数：
         ```kotlin
         composable<DramaCreate> {
             DramaCreateScreen(
                 onNavigateToDetail = { dramaId ->
                     navController.navigate(DramaDetail(dramaId)) {
                         popUpTo<DramaList> { inclusive = false }
                     }
                 },
             )
         }
         ```
       - `DramaListScreen` 保持现有 `onDramaClick` 参数不变
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && grep -q "DramaCreateUiState" android/app/src/main/java/com/drama/app/ui/screens/dramacreate/DramaCreateViewModel.kt && grep -q "storm_discover\|storm_research\|storm_outline" android/app/src/main/java/com/drama/app/ui/screens/dramacreate/DramaCreateViewModel.kt && grep -q "DramaListUiState" android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListViewModel.kt && grep -q "DropdownMenu" android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListScreen.kt && grep -q "AlertDialog" android/app/src/main/java/com/drama/app/ui/screens/dramalist/DramaListScreen.kt && grep -q "onNavigateToDetail" android/app/src/main/java/com/drama/app/ui/navigation/DramaNavHost.kt && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - 创建屏幕：全屏主题输入 + 创建后 STORM 进度展示 + scene_start 自动跳转（D-01~D-05）
    - 列表屏幕：紧凑卡片 + 三点菜单（继续/加载/删除）+ AlertDialog 确认 + 空状态（D-06~D-10, D-24）
    - 导航更新：创建→详情跳转、列表→详情跳转
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android→REST API | Untrusted input (theme, save_name) crosses to backend |
| Android→WebSocket | Authenticated WS connection, events received are trusted (server-sent) |
| REST API→state_manager | Server-side only, trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | DELETE /drama/{folder} path param | mitigate | Validate folder name contains only alphanumeric + underscore + hyphen; reject path traversal (`..`, `/`) |
| T-17-02 | Information Disclosure | Drama list API | accept | Single-user LAN scenario, all dramas visible to sole user |
| T-17-03 | Tampering | Theme input field | mitigate | Backend validates min_length=1, max_length=200; Compose TextField limits input length |
| T-17-04 | Denial of Service | DELETE /drama/{folder} | mitigate | Auth required (require_auth), single-user mode limits exposure |
</threat_model>

<verification>
1. 后端 `DELETE /drama/{folder}` 端点可通过 curl 测试：`curl -X DELETE http://localhost:8000/api/v1/drama/test_folder`
2. Android 编译通过：`cd android && ./gradlew assembleDebug`
3. DramaItemDto 字段与后端 `list_dramas()` 返回值一致
4. DramaCreateScreen 显示全屏创建表单 + STORM 进度
5. DramaListScreen 显示卡片列表 + 三点菜单 + 删除确认
</verification>

<success_criteria>
- 创建戏剧屏幕接受主题输入，点击后显示 STORM 进度，收到 scene_start 事件自动跳转
- 戏剧列表屏幕显示所有已保存戏剧（卡片：主题/状态/场数/更新时间），支持继续/加载/删除
- 删除操作弹出 AlertDialog 确认，空列表显示引导文案
- 后端 DELETE /drama/{folder} 端点可删除指定戏剧
- DramaItemDto 替代 Map<String, JsonElement>，类型安全
</success_criteria>

<output>
After completion, create `.planning/phases/17-android-interaction/17-01-SUMMARY.md`
</output>
