---
phase: 03-semantic-retrieval
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/context_builder.py
  - app/tools.py
  - app/agent.py
autonomous: true
requirements:
  - MEMORY-05

must_haves:
  truths:
    - "导演可通过 retrieve_relevant_scenes Tool 函数按标签检索所有演员的历史记忆"
    - "导演可通过 backfill_tags Tool 函数回填已有场景摘要的标签"
    - "演员上下文末尾自动注入【相关回忆】段落，包含 top-3 最相关记忆"
    - "相关回忆段落优先级最低（0），token 预算不足时最先被截断"
    - "导演 agent instruction 引导使用 retrieve_relevant_scenes 回忆特定过往"
  artifacts:
    - path: "app/tools.py"
      provides: "retrieve_relevant_scenes_tool + backfill_tags_tool 两个 Tool 函数"
      contains: "def retrieve_relevant_scenes_tool"
    - path: "app/context_builder.py"
      provides: "_assemble_actor_sections 末尾新增 semantic_recall section"
      contains: "semantic_recall"
    - path: "app/agent.py"
      provides: "导演 agent 工具注册 + instruction 检索引导"
      contains: "retrieve_relevant_scenes"
  key_links:
    - from: "app/tools.py"
      to: "app/semantic_retriever.py"
      via: "import retrieve_relevant_scenes and backfill_tags for tool function wrappers"
      pattern: "from .semantic_retriever import retrieve_relevant_scenes"
    - from: "app/context_builder.py"
      to: "app/semantic_retriever.py"
      via: "import retrieve_relevant_scenes and _extract_auto_tags for auto-injection"
      pattern: "from .semantic_retriever import retrieve_relevant_scenes, _extract_auto_tags"
    - from: "app/agent.py"
      to: "app/tools.py"
      via: "import retrieve_relevant_scenes_tool and backfill_tags_tool"
      pattern: "retrieve_relevant_scenes_tool"
---

<objective>
集成语义检索到导演和演员两侧：导演侧注册 Tool 函数，演员侧自动注入相关回忆段落。

Purpose: 让检索能力真正被使用——导演可主动回忆特定过往，演员自动获得相关历史上下文。
Output: 修改后的 tools.py + context_builder.py + agent.py
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-semantic-retrieval/03-CONTEXT.md
@.planning/phases/03-semantic-retrieval/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — semantic_retriever.py (created in 03-01) -->

From app/semantic_retriever.py:
```python
TAG_WEIGHTS = {"角色": 3.0, "冲突": 2.0, "事件": 2.0, "情感": 1.5, "地点": 1.0}

def retrieve_relevant_scenes(
    tags: list[str],
    current_scene: int,
    tool_context: ToolContext,
    actor_name: str | None = None,
    top_k: int = 5,
) -> list[dict]:
    """Returns list of {"source", "scenes_covered", "text", "matched_tags", "score"}."""

def backfill_tags(tool_context: ToolContext) -> dict:
    """Returns {"status": str, "message": str, "tagged_count": int}."""

def _extract_auto_tags(actor_data: dict, tool_context: ToolContext) -> list[str]:
    """Extract tags from working_memory + current scene for auto-injection."""
```

From app/context_builder.py (current):
```python
_ACTOR_SECTION_PRIORITIES = {
    "working_memory": 1, "pending_memory": 1,
    "scene_summaries": 2, "arc_summary": 3,
    "critical_memories": 4, "emotion": 5, "anchor": 6,
}

def _assemble_actor_sections(actor_name, actor_data, tool_context) -> list[dict]:
    # Returns list of {"key", "text", "priority", "truncatable", ...} dicts

def estimate_tokens(text: str) -> int:
def _truncate_sections(sections: list[dict], token_budget: int) -> list[dict]:
```

From app/tools.py (current tool function pattern):
```python
def tool_name(param: type, tool_context: ToolContext) -> dict:
    # Returns {"status": "success/error", "message": "...", ...}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register retrieve_relevant_scenes_tool + backfill_tags_tool in tools.py and agent.py</name>
  <files>app/tools.py, app/agent.py</files>
  <read_first>app/tools.py, app/agent.py, app/semantic_retriever.py</read_first>
  <acceptance_criteria>
    - `grep "def retrieve_relevant_scenes_tool" app/tools.py` returns 1 match
    - `grep "def backfill_tags_tool" app/tools.py` returns 1 match
    - `grep "from .semantic_retriever import" app/tools.py` returns 1 line containing retrieve_relevant_scenes and backfill_tags
    - `grep "retrieve_relevant_scenes_tool" app/agent.py` returns at least 1 match (import line)
    - `grep "backfill_tags_tool" app/agent.py` returns at least 1 match (import line)
    - retrieve_relevant_scenes_tool and backfill_tags_tool appear in _storm_director's tools list
    - Director instruction contains "retrieve_relevant_scenes" guidance text
  </acceptance_criteria>
  <action>
    1. In `app/tools.py`:
       a) Add import at top: `from .semantic_retriever import retrieve_relevant_scenes, backfill_tags`
       b) Add `retrieve_relevant_scenes_tool` function (per D-05/D-07/D-08/D-13):
          ```python
          def retrieve_relevant_scenes_tool(
              tags: str,  # 逗号分隔的标签列表
              tool_context: ToolContext,
          ) -> dict:
              """Retrieve relevant scene memories by tags. Use when you need to recall specific past events.

              按标签检索相关历史记忆，导演全局搜索所有演员的记忆。

              Args:
                  tags: Comma-separated tags, e.g. "角色:朱棣,情感:愤怒,冲突:权力争夺"

              Returns:
                  dict with top-K relevant memories, sorted by relevance score.
              """
              tag_list = [t.strip() for t in tags.split(",") if t.strip()]
              if not tag_list:
                  return {"status": "error", "message": "请提供至少一个标签用于检索。"}

              # Validate tag length (T-03-01)
              for t in tag_list:
                  if len(t) > 50:
                      return {"status": "error", "message": f"标签过长（{len(t)}字符），限制50字符以内。"}

              state = tool_context.state.get("drama", {})
              current_scene = state.get("current_scene", 0)

              results = retrieve_relevant_scenes(
                  tags=tag_list,
                  current_scene=current_scene,
                  tool_context=tool_context,
                  actor_name=None,  # 导演全局搜索 (D-07)
                  top_k=5,  # 导演侧 top-5 (D-08)
              )

              # Format results for readability
              formatted = []
              for r in results:
                  matched = ", ".join(r.get("matched_tags", []))
                  formatted.append(
                      f"- 第{r['scenes_covered']}场[{r['source']}]: {r['text'][:150]} (匹配: {matched}, 相关度: {r['score']:.1f})"
                  )

              return {
                  "status": "success",
                  "message": f"找到 {len(results)} 条相关记忆。\n" + "\n".join(formatted),
                  "results": results,
              }
          ```

       c) Add `backfill_tags_tool` function (per D-02):
          ```python
          def backfill_tags_tool(tool_context: ToolContext) -> dict:
              """Backfill tags for existing scene summaries that lack tags. Call once when loading an old drama.

              对已有的 scene_summaries 批量生成标签（调用 LLM）。
              执行后标记 tags_backfilled=True，避免重复执行。

              Args:
                  tool_context: Tool context for state access.

              Returns:
                  dict with backfill status and count of tagged summaries.
              """
              result = backfill_tags(tool_context)
              return result
          ```

    2. In `app/agent.py`:
       a) Add to imports from `.tools`: `retrieve_relevant_scenes_tool`, `backfill_tags_tool`
       b) Add both to `_storm_director`'s `tools=[]` list
       c) Add to director instruction (after "## 视角回溯" section or near it, per D-13):
          ```
          ## 记忆检索
          当你需要回忆特定过往时，调用 retrieve_relevant_scenes_tool 工具。
          例如：
          - "朱棣上次在皇宫是什么情况？" → 调用 retrieve_relevant_scenes_tool(tags="角色:朱棣,地点:皇宫")
          - "之前有什么权力争夺的场景？" → 调用 retrieve_relevant_scenes_tool(tags="冲突:权力争夺")
          - 如果加载了旧的戏剧存档（无标签数据），调用 backfill_tags_tool 为已有场景摘要生成标签。
          ```
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && uv run pytest tests/ -x -q</automated>
  </verify>
  <done>
    - `retrieve_relevant_scenes_tool` and `backfill_tags_tool` defined in tools.py with correct signatures
    - Both imported and registered in agent.py _storm_director tools list
    - Director instruction includes retrieval guidance section
    - All existing tests still pass (no regressions)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 【相关回忆】section to context_builder.py actor context + extend tests</name>
  <files>app/context_builder.py, tests/unit/test_context_builder.py</files>
  <read_first>app/context_builder.py, app/semantic_retriever.py</read_first>
  <acceptance_criteria>
    - `grep "semantic_recall" app/context_builder.py` returns at least 1 match
    - `grep "from .semantic_retriever import" app/context_builder.py` returns 1 line
    - `grep "相关回忆" app/context_builder.py` returns at least 1 match
    - semantic_recall section has priority 0 (lowest, per D-16)
    - semantic_recall section has truncatable=True
    - test_context_builder.py has tests for semantic_recall section presence and priority
    - `uv run pytest tests/unit/test_context_builder.py -x -q` passes
  </acceptance_criteria>
  <action>
    1. In `app/context_builder.py`:
       a) Add import at top: `from .semantic_retriever import retrieve_relevant_scenes, _extract_auto_tags`
       b) Add `"semantic_recall": 0` to `_ACTOR_SECTION_PRIORITIES` dict (lowest priority, per D-16)
       c) In `_assemble_actor_sections()`, after the "pending_memory" section (last current section) and before `return sections`, add:

       ```python
       # Semantic recall section (priority 0, lowest — D-14/D-16)
       auto_tags = _extract_auto_tags(actor_data, tool_context)
       if auto_tags:
           state_ref = _get_state(tool_context)
           current_scene_num = state_ref.get("current_scene", 0)
           
           # Collect scene ranges already in sections to avoid duplication (Pitfall 2)
           existing_scene_ranges = set()
           for sec in sections:
               if sec.get("key") == "scene_summaries":
                   # Parse scene ranges from the section text (e.g., "第3-5场")
                   import re
                   for match in re.finditer(r'第(\d+)(?:-(\d+))?场', sec.get("text", "")):
                       start = int(match.group(1))
                       end = int(match.group(2) or match.group(1))
                       existing_scene_ranges.update(range(start, end + 1))
           
           recall_results = retrieve_relevant_scenes(
               tags=auto_tags,
               current_scene=current_scene_num,
               tool_context=tool_context,
               actor_name=actor_name,  # D-07: actor limited to own memories
               top_k=5,  # Over-fetch to allow dedup filtering
           )
           
           # Filter out results whose scenes overlap with existing scene_summaries (Pitfall 2)
           filtered_results = []
           for r in recall_results:
               result_scenes = _normalize_scene_range(r.get("scenes_covered", ""))
               if not result_scenes.intersection(existing_scene_ranges):
                   filtered_results.append(r)
               if len(filtered_results) >= 3:  # D-14: actor side top-3 after filtering
                   break
           
           if filtered_results:
               recall_lines = []
               for r in filtered_results:
                   recall_lines.append(
                       f"- 第{r['scenes_covered']}场：{r['text'][:100]} "
                       f"[匹配: {', '.join(r.get('matched_tags', []))}]"
                   )
               sections.append({
                   "key": "semantic_recall",
                   "text": "【相关回忆】\n" + "\n".join(recall_lines),
                   "priority": _ACTOR_SECTION_PRIORITIES["semantic_recall"],
                   "truncatable": True,
               })
       ```

       This implements:
       - D-14: Actor context includes "【相关回忆】" section at the end
       - D-15: Auto-tags extracted from working_memory latest entries + current scene
       - D-16: Priority 0 (lowest), truncatable=True — first to be cut when budget exceeded
       - Per Pitfall 2 in RESEARCH.md: deduplicate against existing scene_summaries section to avoid showing same scene twice
       - Per Pitfall 4 in RESEARCH.md: each recall line truncated to 100 chars

    2. In `tests/unit/test_context_builder.py`, add tests:
       a) Test that `build_actor_context_from_memory` with an actor that has scene_summaries with tags includes "【相关回忆】" section
       b) Test that semantic_recall section has priority 0
       c) Test that when actor context is near token budget, semantic_recall is truncated before other sections
       d) Test that `_ACTOR_SECTION_PRIORITIES["semantic_recall"]` == 0

       Use the existing mock ToolContext pattern from test_context_builder.py. Create mock actors with scene_summaries containing tags and working_memory entries, then verify the context output contains "相关回忆" when auto_tags can be extracted.
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && uv run pytest tests/unit/test_context_builder.py -x -q</automated>
  </verify>
  <done>
    - Actor context includes "【相关回忆】" section when relevant memories found
    - semantic_recall section has priority 0 and truncatable=True
    - `_ACTOR_SECTION_PRIORITIES` includes "semantic_recall": 0
    - `from .semantic_retriever import retrieve_relevant_scenes, _extract_auto_tags` in context_builder.py
    - New tests for semantic_recall section pass
    - All existing tests still pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool tag input → retrieval engine | User-provided tags via retrieve_relevant_scenes_tool enter matching |
| Auto-generated tags → retrieval | _extract_auto_tags produces tags from working memory text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | retrieve_relevant_scenes_tool tag input | mitigate | Tag length validation ≤ 50 chars per tag, comma-separated input only (per T-03-01) |
| T-03-05 | Information Disclosure | actor context auto-injection | accept | Auto-injection respects D-07 (actor limited to own memories), no cross-actor data leakage possible |
</threat_model>

<verification>
1. `uv run pytest tests/unit/test_context_builder.py -x -q` — context builder tests pass with semantic_recall
2. `uv run pytest tests/ -x -q` — full suite green
3. `python -c "from app.tools import retrieve_relevant_scenes_tool, backfill_tags_tool"` — tools importable
4. `python -c "from app.agent import _storm_director; print(retrieve_relevant_scenes_tool in _storm_director.tools)"` — tool registered
5. `grep "semantic_recall" app/context_builder.py | wc -l` ≥ 2 — section exists in code
</verification>

<success_criteria>
1. 导演侧 `retrieve_relevant_scenes_tool` 注册为 Tool 函数，按标签全局搜索所有演员记忆
2. 导演侧 `backfill_tags_tool` 注册为 Tool 函数，可回填旧存档的标签
3. 演员 `build_actor_context_from_memory()` 末尾自动注入【相关回忆】段落，top-3 最相关记忆
4. 相关回忆段落优先级最低（0），token 预算不足时最先被截断
5. 导演 agent instruction 包含检索引导
6. 所有 MEMORY-05 相关测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/03-semantic-retrieval/03-02-SUMMARY.md`
</output>
