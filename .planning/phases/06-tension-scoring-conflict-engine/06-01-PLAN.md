---
phase: 06-tension-scoring-conflict-engine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/conflict_engine.py
  - tests/unit/test_conflict_engine.py
autonomous: true
requirements:
  - CONFLICT-01
  - CONFLICT-03
  - CONFLICT-04

must_haves:
  truths:
    - "evaluate_tension 的核心函数 calculate_tension(state) 返回 tension_score(0-100)、is_boring、suggested_action、signals"
    - "4 个启发式信号各自归一化到 0-1，加权计算张力评分，无需 LLM 调用"
    - "7 种冲突模板完整定义，每种包含 name/description/prompt_hint/suggested_emotions"
    - "同类型 8 场内不重复，inject 前检查 used_conflict_types 去重"
    - "活跃冲突上限 4 条，达到上限时返回建议而非继续注入"
    - "连续低张力渐进升级：1场→urgency=normal，2场→urgency=high，3+场→urgency=critical"
  artifacts:
    - path: "app/conflict_engine.py"
      provides: "calculate_tension, select_conflict_type, generate_conflict_suggestion, update_conflict_engine_state, CONFLICT_TEMPLATES, _EMOTION_WEIGHTS"
      exports: ["calculate_tension", "select_conflict_type", "generate_conflict_suggestion", "update_conflict_engine_state", "CONFLICT_TEMPLATES"]
    - path: "tests/unit/test_conflict_engine.py"
      provides: "Unit tests for all conflict_engine functions"
      contains: "TestCalculateTension"
  key_links:
    - from: "app/conflict_engine.py::calculate_tension"
      to: "state['actors'][*]['emotions']"
      via: "_calc_emotion_variance reads actor emotions"
      pattern: "actors.*emotions"
    - from: "app/conflict_engine.py::select_conflict_type"
      to: "state['conflict_engine']['used_conflict_types']"
      via: "8-scene dedup check"
      pattern: "used_conflict_types"
---

<objective>
创建 conflict_engine.py 核心模块——纯启发式张力评分 + 冲突注入逻辑，含完整单元测试。

Purpose: 张力评分和冲突注入的所有计算逻辑集中于此模块，tools.py 仅做薄代理。此模块不依赖 ToolContext，所有函数接收 state: dict 参数，确保可测试性。
Output: app/conflict_engine.py + tests/unit/test_conflict_engine.py
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tension-scoring-conflict-engine/06-CONTEXT.md
@.planning/phases/06-tension-scoring-conflict-engine/06-RESEARCH.md
@app/memory_manager.py
@app/context_builder.py

<interfaces>
<!-- Existing interfaces that conflict_engine.py will READ from -->

From app/context_builder.py (line 73-77):
```python
_EMOTION_CN = {
    "neutral": "平静", "angry": "愤怒", "sad": "悲伤", "happy": "喜悦",
    "fearful": "恐惧", "confused": "困惑", "determined": "决绝",
    "anxious": "焦虑", "hopeful": "充满希望",
}
```

From app/memory_manager.py — CRITICAL_REASONS list (used for "未决事件" detection):
```python
CRITICAL_REASONS = ["角色首次登场", "重大转折", "情感高峰", "关键决定", "用户标记", "未决事件"]
```

State structure (from CONTEXT.md D-16):
```python
conflict_engine = {
    "tension_score": 0,
    "is_boring": False,
    "tension_history": [],        # [{scene, score, is_boring, signals}]
    "active_conflicts": [],       # [{id, type, description, involved_actors, introduced_scene}]
    "used_conflict_types": [],    # [{type, scene_used}]
    "last_inject_scene": 0,
    "consecutive_low_tension": 0,
}
```

Actor state structure (read by signals):
```python
actors[name] = {
    "emotions": "neutral",          # Signal 1: emotion variance
    "working_memory": [{"entry": "...", "scene": N}],  # Signal 3: repetition
    "critical_memories": [{"reason": "未决事件", ...}],  # Signal 2: unresolved
    "arc_summary": {"structured": {"unresolved": [...]}}, # Signal 2: unresolved
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: conflict_engine.py 核心计算函数 + CONFLICT_TEMPLATES + 单元测试</name>
  <files>app/conflict_engine.py, tests/unit/test_conflict_engine.py</files>
  <read_first>
    app/conflict_engine.py (will not exist yet — create from scratch)
    app/context_builder.py (for _EMOTION_CN mapping pattern, lines 73-77)
    app/memory_manager.py (for CRITICAL_REASONS pattern, detect_importance pattern)
    .planning/phases/06-tension-scoring-conflict-engine/06-CONTEXT.md (D-01 through D-07, D-19)
    .planning/phases/06-tension-scoring-conflict-engine/06-RESEARCH.md (Code Examples section)
  </read_first>
  <behavior>
    - Test 1: calculate_tension with all neutral emotions, no unresolved, no repetition → score < 30, is_boring=True
    - Test 2: calculate_tension with diverse emotions (angry+fearful+neutral), unresolved conflicts, no repetition → score > 30, is_boring=False
    - Test 3: _calc_emotion_variance with single actor → returns 0.0
    - Test 4: _calc_emotion_variance with "angry" and "neutral" → variance > 0
    - Test 5: _calc_unresolved_density counts critical_memories with reason="未决事件" + arc_summary.structured.unresolved + active_conflicts
    - Test 6: _calc_dialogue_repetition with < 2 scenes → returns 0.0
    - Test 7: _calc_dialogue_repetition with identical entries → returns value < 1.0 (high repetition = low score)
    - Test 8: _calc_scenes_since_inject with gap=0 → returns 0.0
    - Test 9: _calc_scenes_since_inject with gap=16 → returns 1.0
    - Test 10: CONFLICT_TEMPLATES has exactly 7 keys: new_character, secret_revealed, escalation, betrayal, accident, external_threat, dilemma
    - Test 11: Each CONFLICT_TEMPLATES entry has name, description, prompt_hint, suggested_emotions keys
    - Test 12: calculate_tension returns dict with keys: tension_score, is_boring, suggested_action, signals
    - Test 13: suggested_action is "inject_conflict" when is_boring=True, "maintain" when score 30-70, "cool_down" when score > 70
  </behavior>
  <action>
    Create `app/conflict_engine.py` with the following components (per D-01, D-02, D-06, D-07, D-19):

    1. **Module-level constants:**
       - `_EMOTION_WEIGHTS` dict mapping emotion strings to tension weights (per CONTEXT.md specifics): "neutral"=1, "happy"=2, "hopeful"=2, "confused"=2, "anxious"=3, "sad"=4, "determined"=4, "angry"=5, "fearful"=5
       - `CONFLICT_TEMPLATES` dict with 7 entries (per D-07):
         - `new_character`: name="新角色登场", description="引入新角色打破现有格局", prompt_hint="一个陌生的身影出现在故事中——他/她的到来将打破现有的平衡", suggested_emotions=["anxious", "confused", "determined"]
         - `secret_revealed`: name="秘密发现", description="隐藏信息被揭露，改变角色关系", prompt_hint="一个被深藏的秘密浮出水面——真相将重新定义所有人之间的关系", suggested_emotions=["fearful", "angry", "sad"]
         - `escalation`: name="矛盾升级", description="现有分歧激化为更严重的对抗", prompt_hint="现有分歧已到临界点——一个小火星就可能引爆全面对抗", suggested_emotions=["angry", "determined", "fearful"]
         - `betrayal`: name="信任背叛", description="盟友变敌或承诺被打破", prompt_hint="曾经的盟友突然倒戈——最信任的人成为了最大的威胁", suggested_emotions=["angry", "sad", "fearful"]
         - `accident`: name="意外事件", description="突发状况打乱计划", prompt_hint="突如其来的变故打乱了一切——没有人预料到这个转折", suggested_emotions=["confused", "fearful", "anxious"]
         - `external_threat`: name="外部威胁", description="外部力量介入迫使角色联合或分裂", prompt_hint="一个更大的威胁从外部逼近——它将迫使所有人做出选择", suggested_emotions=["fearful", "anxious", "determined"]
         - `dilemma`: name="抉择困境", description="角色面临两难选择，无论选哪个都有代价", prompt_hint="两难困境摆在面前——无论选择哪一边，都要付出沉重的代价", suggested_emotions=["anxious", "sad", "determined"]
       - `TENSION_LOW_THRESHOLD = 30`, `TENSION_HIGH_THRESHOLD = 70`, `MAX_TENSION_HISTORY = 20`, `DEDUP_WINDOW = 8`, `MAX_ACTIVE_CONFLICTS = 4`

    2. **Signal calculation functions** (all accept `state: dict`, return `float` 0-1):
       - `_calc_emotion_variance(state)`: Extract all actor emotions → weights via _EMOTION_WEIGHTS → compute variance → normalize by dividing by 4.0 (max theoretical variance), clamp to [0, 1]. Return 0.0 if < 2 actors.
       - `_calc_unresolved_density(state)`: Count critical_memories with reason="未决事件" + len(arc_summary.structured.unresolved) + len(conflict_engine.active_conflicts). Normalize by dividing by 5.0, clamp to [0, 1].
       - `_calc_dialogue_repetition(state)`: Collect working_memory entries from recent 3 scenes (current_scene - scene <= 3). Use first 20 chars + actor_name as match key. Count duplicates / total entries. Return `1.0 - repetition_ratio` (high repetition → low tension signal). Return 0.0 if < 2 entries or current_scene < 2.
       - `_calc_scenes_since_inject(state)`: `gap = current_scene - conflict_engine.last_inject_scene`. Return `min(1.0, gap / 16.0)`. Return 0.0 if gap <= 0.

    3. **Core function `calculate_tension(state: dict) -> dict`:**
       - Compute 4 signals, each 0-1
       - Weighted sum: emotion_variance*0.30 + unresolved_density*0.30 + dialogue_repetition*0.20 + scenes_since_inject*0.20
       - tension_score = int(score * 100), clamp to [0, 100]
       - is_boring = tension_score < 30 (per D-02)
       - suggested_action: "inject_conflict" if is_boring, "maintain" if 30-70, "cool_down" if > 70
       - Return: `{"tension_score": int, "is_boring": bool, "suggested_action": str, "signals": dict}`

    Create `tests/unit/test_conflict_engine.py` with pytest class-based tests:
    - `TestCalcEmotionVariance`: test single actor, diverse emotions, all same emotion
    - `TestCalcUnresolvedDensity`: test no unresolved, multiple sources, saturation
    - `TestCalcDialogueRepetition`: test too few scenes, identical entries, diverse entries
    - `TestCalcScenesSinceInject`: test gap=0, gap=8, gap=16+
    - `TestCalculateTension`: test low tension (boring), normal tension, high tension, return structure
    - `TestConflictTemplates`: test 7 types exist, each has required keys, all prompt_hints are non-empty

    All test helper functions should build minimal state dicts inline. Follow the pattern from tests/unit/test_memory_manager.py (class-based, descriptive test names).

    Use Chinese+English dual docstrings following project convention (see context_builder.py, memory_manager.py).
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && uv run pytest tests/unit/test_conflict_engine.py -x -q</automated>
  </verify>
  <done>
    - `app/conflict_engine.py` exists with CONFLICT_TEMPLATES (7 types), _EMOTION_WEIGHTS, 4 signal functions, calculate_tension()
    - `tests/unit/test_conflict_engine.py` exists with all test classes passing
    - `calculate_tension()` returns dict with tension_score (0-100), is_boring, suggested_action, signals
    - All 4 signal functions return 0-1 float values
    - grep -c "CONFLICT_TEMPLATES" app/conflict_engine.py returns >= 1
    - grep "new_character\|secret_revealed\|escalation\|betrayal\|accident\|external_threat\|dilemma" app/conflict_engine.py shows all 7 types
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 冲突选择/注入/去重/渐进升级 + 状态更新函数 + 单元测试</name>
  <files>app/conflict_engine.py, tests/unit/test_conflict_engine.py</files>
  <read_first>
    app/conflict_engine.py (read the file just created in Task 1)
    .planning/phases/06-tension-scoring-conflict-engine/06-CONTEXT.md (D-05, D-08, D-09, D-10, D-16)
    .planning/phases/06-tension-scoring-conflict-engine/06-RESEARCH.md (Pitfall 3, 4, 6)
  </read_first>
  <behavior>
    - Test 1: select_conflict_type skips types used within last 8 scenes
    - Test 2: select_conflict_type returns a type from CONFLICT_TEMPLATES keys
    - Test 3: select_conflict_type returns None when all types exhausted within dedup window
    - Test 4: generate_conflict_suggestion with urgency="normal" returns conflict_id, type, type_cn, description, prompt_hint, involved_actors, urgency, suggested_emotions
    - Test 5: generate_conflict_suggestion with urgency="high" includes stronger prompt_hint language
    - Test 6: generate_conflict_suggestion with urgency="critical" includes "必须处理" language
    - Test 7: generate_conflict_suggestion when active_conflicts >= 4 returns "上限已满" suggestion with oldest conflict info
    - Test 8: update_conflict_engine_state appends to tension_history and trims to 20
    - Test 9: update_conflict_engine_state increments consecutive_low_tension when is_boring=True
    - Test 10: update_conflict_engine_state resets consecutive_low_tension to 0 when is_boring=False
    - Test 11: update_conflict_engine_state appends to used_conflict_types on inject
    - Test 12: update_conflict_engine_state appends to active_conflicts on inject
    - Test 13: conflict_id format is "conflict_{scene}_{type}_{index}"
    - Test 14: Urgency progression: 1 consecutive low → "normal", 2 → "high", 3+ → "critical"
  </behavior>
  <action>
    Add the following functions to `app/conflict_engine.py` (per D-05, D-08, D-09, D-10, D-16):

    1. **`select_conflict_type(state: dict) -> str | None`:**
       - Get available types = set(CONFLICT_TEMPLATES.keys())
       - Get used = state.get("conflict_engine", {}).get("used_conflict_types", [])
       - current_scene = state.get("current_scene", 0)
       - For each used entry, if `current_scene - entry["scene_used"] < DEDUP_WINDOW(8)`, remove that type from available
       - If available is empty, return None
       - Otherwise, pick a random type from available (use `import random; random.choice(list(available))`)
       - Return the selected type string

    2. **`generate_conflict_suggestion(state: dict, conflict_type: str | None = None) -> dict`:**
       - If `conflict_type` is None, call `select_conflict_type(state)` to pick one
       - If still None (all types exhausted), return `{"status": "all_exhausted", "message": "所有冲突类型在近8场内均已使用，建议先解决已有冲突", "urgency": "high"}`
       - Get template = CONFLICT_TEMPLATES[conflict_type]
       - Determine urgency based on `state["conflict_engine"]["consecutive_low_tension"]`:
         - 1 → "normal", 2 → "high", 3+ → "critical" (per D-09)
       - Check active_conflicts count:
         - If len >= MAX_ACTIVE_CONFLICTS(4): return `{"status": "limit_reached", "message": "当前活跃冲突已达上限，建议优先解决已有冲突", "suggestion": description of oldest conflict, "urgency": urgency}` (per D-10, Pitfall 6 mitigation)
       - Generate conflict_id: `f"conflict_{current_scene}_{conflict_type}_{len(active_conflicts)+1}"`
       - Determine involved_actors: pick the 2 actors with highest emotion weights from state["actors"]
       - Build prompt_hint: if urgency=="high", prefix with "⚠️ 紧急："; if urgency=="critical", prefix with "🚨 必须处理："
       - Return: `{"status": "success", "conflict_id": str, "type": str, "type_cn": template["name"], "description": template["description"], "prompt_hint": str, "involved_actors": list[str], "urgency": str, "suggested_emotions": template["suggested_emotions"]}`

    3. **`update_conflict_engine_state(state: dict, tension_result: dict, conflict_suggestion: dict | None = None) -> dict`:**
       - Get or init conflict_engine sub-dict with defaults (per D-16)
       - Update tension_score and is_boring from tension_result
       - Update consecutive_low_tension: increment if is_boring else reset to 0
       - Append to tension_history: `{"scene": state.get("current_scene", 0), "score": tension_result["tension_score"], "is_boring": tension_result["is_boring"], "signals": tension_result["signals"]}`
       - Trim tension_history to MAX_TENSION_HISTORY(20) entries
       - If conflict_suggestion is not None and conflict_suggestion.get("status") == "success":
         - Append to active_conflicts: `{"id": conflict_suggestion["conflict_id"], "type": conflict_suggestion["type"], "description": conflict_suggestion["description"], "involved_actors": conflict_suggestion["involved_actors"], "introduced_scene": state.get("current_scene", 0)}`
         - Append to used_conflict_types: `{"type": conflict_suggestion["type"], "scene_used": state.get("current_scene", 0)}`
         - Update last_inject_scene = state.get("current_scene", 0)
       - Return the updated conflict_engine dict (caller is responsible for persisting to state)

    Add test classes to `tests/unit/test_conflict_engine.py`:
    - `TestSelectConflictType`: dedup logic, all exhausted, fresh state
    - `TestGenerateConflictSuggestion`: return structure, urgency levels, limit reached, exhausted types
    - `TestUpdateConflictEngineState`: tension_history append+trim, consecutive_low_tension tracking, conflict injection updates, used_conflict_types updates

    Build helper function `_make_state(**overrides)` in test file to create minimal state dicts with conflict_engine defaults.
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && uv run pytest tests/unit/test_conflict_engine.py -x -q</automated>
  </verify>
  <done>
    - `select_conflict_type()` implements 8-scene dedup per D-08
    - `generate_conflict_suggestion()` returns structured suggestion per D-05 with urgency per D-09
    - `generate_conflict_suggestion()` returns "limit_reached" when active_conflicts >= 4 per D-10
    - `update_conflict_engine_state()` manages all 7 conflict_engine fields per D-16
    - tension_history is trimmed to 20 entries
    - consecutive_low_tension increments on is_boring=True, resets on False
    - All tests pass: `uv run pytest tests/unit/test_conflict_engine.py -x -q`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state dict → conflict_engine | Internal state data, trusted within process |
| conflict_type parameter | Comes from LLM tool call — must validate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | select_conflict_type / generate_conflict_suggestion | mitigate | Validate conflict_type is in CONFLICT_TEMPLATES.keys() before use |
| T-06-02 | Tampering | _calc_dialogue_repetition | accept | Entry text is internal data, already length-limited by Phase 1 ENTRY_TEXT_MAX_LENGTH=500 |
| T-06-03 | Denial of Service | calculate_tension | accept | Pure computation on bounded state data, no external calls, no unbounded loops |
</threat_model>

<verification>
1. All unit tests pass: `uv run pytest tests/unit/test_conflict_engine.py -x -q`
2. Module imports cleanly: `uv run python -c "from app.conflict_engine import calculate_tension, CONFLICT_TEMPLATES; print(len(CONFLICT_TEMPLATES))"`
3. Lint passes: `uv run ruff check app/conflict_engine.py`
</verification>

<success_criteria>
- `app/conflict_engine.py` contains `calculate_tension()`, `select_conflict_type()`, `generate_conflict_suggestion()`, `update_conflict_engine_state()`, `CONFLICT_TEMPLATES` (7 types)
- All 4 signal functions return 0-1 float, no LLM calls
- `calculate_tension()` returns tension_score 0-100, is_boring < 30 threshold
- 8-scene dedup works, active conflict limit 4, consecutive low tension urgency escalation
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-tension-scoring-conflict-engine/06-01-SUMMARY.md`
</output>
