---
phase: 04-infinite-loop-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/agent.py
autonomous: true
requirements:
  - LOOP-01

must_haves:
  truths:
    - "DramaRouter routes to _setup_agent when no actors exist in state"
    - "DramaRouter routes to _improv_director when actors exist in state"
    - "DramaRouter falls back to _improv_director when agent lookup fails"
    - "Utility commands (/save, /load, /export, /cast, /status, /list) route to _improv_director"
    - "_improv_director system prompt declares infinite loop protocol with no-preset ending"
    - "_setup_agent merges discovery + research + outline into a single agent"
    - "root_agent is DramaRouter with 2 sub-agents (setup_agent + improv_director)"
  artifacts:
    - path: "app/agent.py"
      provides: "DramaRouter class + _setup_agent + _improv_director definitions"
      contains: "class DramaRouter"
  key_links:
    - from: "DramaRouter._run_async_impl"
      to: "_setup_agent / _improv_director"
      via: "actors existence check + sub_agents_map lookup"
      pattern: "self\\._sub_agents_map\\.get\\("
---

<objective>
将 StormRouter（4 子 Agent 线性流水线）重构为 DramaRouter（2 子 Agent：_setup_agent + _improv_director），实现路由逻辑简化和无限循环 prompt 声明。

Purpose: 这是 Phase 4 的核心架构变更——从线性 STORM 流水线切换为 setup/improvise 二阶段模型，路由由 actors 存在性驱动（D-04），无限循环由 System Prompt 声明驱动（D-05）。

Output: 重构后的 app/agent.py，包含 DramaRouter、_setup_agent、_improv_director
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-infinite-loop-engine/04-CONTEXT.md
@.planning/phases/04-infinite-loop-engine/04-RESEARCH.md
@app/agent.py
@app/tools.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/tools.py — tools available for _setup_agent:
```python
def start_drama(theme: str, tool_context: ToolContext) -> dict
def storm_discover_perspectives(theme: str, tool_context: ToolContext) -> dict
def storm_synthesize_outline(theme: str, tool_context: ToolContext) -> dict
def create_actor(actor_name: str, role: str, personality: str, background: str, knowledge_scope: str, tool_context: ToolContext) -> dict
```

From app/tools.py — tools available for _improv_director:
```python
def next_scene(tool_context: ToolContext) -> dict
def director_narrate(narration: str, tool_context: ToolContext) -> dict
async def actor_speak(actor_name: str, situation: str, tool_context: ToolContext) -> dict
def write_scene(scene_number: int, scene_title: str, scene_description: str, dialogue_content: str, tool_context: ToolContext) -> dict
def user_action(action_description: str, tool_context: ToolContext) -> dict
def get_director_context(tool_context: ToolContext) -> dict
def update_emotion(actor_name: str, emotion: str, tool_context: ToolContext) -> dict
def mark_memory(actor_name: str, reason: str, tool_context: ToolContext) -> dict
def save_drama(save_name: str, tool_context: ToolContext) -> dict
def load_drama(save_name: str, tool_context: ToolContext) -> dict
def export_drama(tool_context: ToolContext) -> dict
def show_cast(tool_context: ToolContext) -> dict
def show_status(tool_context: ToolContext) -> dict
def list_all_dramas(tool_context: ToolContext) -> dict
def retrieve_relevant_scenes_tool(tags: str, tool_context: ToolContext) -> dict
async def backfill_tags_tool(tool_context: ToolContext) -> dict
```

From google.adk.agents:
```python
class BaseAgent:
    sub_agents: list  # Sub-agents accessible via self.sub_agents
    async def _run_async_impl(self, ctx: InvocationContext) -> AsyncGenerator[Event, None]
    @property
    def name: str

class Agent(BaseAgent):
    def __init__(self, name: str, model, instruction: str, description: str, tools: list)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DramaRouter + _setup_agent + _improv_director</name>
  <files>app/agent.py</files>
  <read_first>
    - app/agent.py (full file — must understand current StormRouter, all 4 sub-agents, their prompts and tools)
    - app/tools.py (lines 924-1247 for STORM tool signatures)
  </read_first>
  <behavior>
    - Test 1: DramaRouter._run_async_impl routes to setup_agent when actors is empty dict
    - Test 2: DramaRouter._run_async_impl routes to improv_director when actors has entries
    - Test 3: DramaRouter._run_async_impl falls back to improv_director when agent lookup returns None
    - Test 4: DramaRouter._run_async_impl routes to improv_director for utility commands (/save, /load, /export, /cast, /status, /list)
    - Test 5: _improv_director instruction contains "无限" or "无预设终点" string
    - Test 6: _setup_agent instruction contains "多视角" string (preserving STORM exploration value)
    - Test 7: root_agent.sub_agents has exactly 2 agents
  </behavior>
  <action>
Rewrite `app/agent.py` entirely. The file should contain:

1. **Imports**: Same imports as current, plus keep all tool imports. Remove `storm_ask_perspective_questions` and `storm_research_perspective` imports (D-02: merge removes these middle-step tools per Research Pitfall 6 — they produce placeholder data [VERIFIED: CONCERNS.md]). Keep: `start_drama`, `storm_discover_perspectives`, `storm_synthesize_outline`, `create_actor` for _setup_agent.

2. **_setup_agent** (Agent, name="setup_agent"):
   - Model: `_get_model()`
   - Instruction: A single merged prompt that covers all setup phases. Must include:
     - Step 1 marker: "步骤 1：你必须先调用 start_drama(theme) 工具初始化戏剧" (per Pitfall 1 — explicit step markers prevent LLM from skipping)
     - Step 2 marker: "步骤 2：你必须调用 storm_discover_perspectives(theme) 工具，从多视角探索主题"
     - Step 3 marker: "步骤 3：你必须调用 storm_synthesize_outline(theme) 工具，将多视角发现融合为戏剧大纲"
     - Step 4 marker: "步骤 4：获得用户确认后，为每个角色调用 create_actor 工具创建 A2A 服务"
     - Emphasis on STORM multi-perspective exploration value (D-02, preserving core insight)
     - One-shot /start flow: "用户只发 /start <主题>，你自主推进到演员创建" (D-12)
   - Description: "Setup Agent — 一站式戏剧设定：发现视角→合成大纲→创建角色"
   - Tools: `[start_drama, storm_discover_perspectives, storm_synthesize_outline, create_actor]` (4 tools, no ask/research middle-step tools per Research Open Question 1 recommendation)

3. **_improv_director** (Agent, name="improv_director"):
   - Model: `_get_model()`
   - Instruction: Evolved from current _storm_director instruction with these critical changes:
     - **Top-priority constraint** (Pitfall 4): Add "⚠️ 无终点声明\n你永远不会自行结束戏剧。只有用户发送 /end 时才终止。每一场都是新故事的开始——即使上一场看似结束，下一场也可以有新的转折。" at the HIGHEST position in the prompt
     - **Loop protocol** (D-05): Add "## ⚠️ 核心循环协议\n每次用户发送 /next 或 /action，你必须按以下顺序执行：\n1. next_scene() → 推进场景计数器，获取衔接信息（transition_text 是必看的场景衔接要点）\n2. director_narrate() → 描述本场环境、氛围、时间、地点\n3. actor_speak() × N → 逐个让参与角色回应\n4. write_scene() → 将完整内容记录到剧本\n5. 回顾局势 → 考虑是否需要调用 get_director_context() 审视全局\n\n⚠️ 衔接信息使用规则（D-10 不重复）：next_scene() 返回的 transition_text 已包含上一场的结局、情绪、未决事件——这是你的主信息源。只有需要全局弧线、STORM 视角等宏观信息时，才调用 get_director_context()。两者信息不重复。"
     - **Evaluation hint** (D-07): Add "每场结束后，回顾当前局势。你可以调用 get_director_context() 审视全局故事进展。" (prompt-based evaluation, not code-forced, reserving interface for Phase 6 evaluate_tension)
     - **Remove STORM phase references**: Remove mentions of "STORM 框架的「导演」", "视角回溯" section, and STORM-specific terminology
     - **Keep**: All existing format rules (剧本格式、输出规则), A2A architecture explanation, workflow steps, save/load handling, all command hints
     - **User wait** (D-06): "输出完整剧本格式片段后，等待用户下一步指令。不要自动推进多场。"
   - Description: "即兴导演 — 无限演出模式，场景推进与角色对话"
   - Tools: Same as current _storm_director MINUS `create_actor` (setup only) and STORM-specific tools. List: `[actor_speak, director_narrate, get_director_context, write_scene, next_scene, user_action, save_drama, load_drama, export_drama, show_cast, show_status, list_all_dramas, update_emotion, mark_memory, retrieve_relevant_scenes_tool, backfill_tags_tool]`

4. **DramaRouter** (BaseAgent subclass, replaces StormRouter):
   ```python
   class DramaRouter(BaseAgent):
       """Routes user input to setup_agent or improv_director (D-01/D-04).

       Routing logic:
       - Utility commands → improv_director (always)
       - actors exist → improv_director
       - no actors → setup_agent
       - Fallback (D-03) → improv_director (safest default)
       """

       async def _run_async_impl(
           self, ctx: InvocationContext
       ) -> AsyncGenerator[Event, None]:
           drama = ctx.session.state.get("drama", {})
           actors = drama.get("actors", {})

           # Check for utility commands (D-04: route to improv_director)
           user_message = ""
           if ctx.user_content and ctx.user_content.parts:
               for part in ctx.user_content.parts:
                   text = getattr(part, 'text', None) or ''
                   user_message += text.lower()

           utility_commands = ["/save", "/load", "/export", "/cast", "/status", "/list"]
           force_improvise = any(cmd in user_message for cmd in utility_commands)

           if force_improvise or (actors and len(actors) > 0):
               agent = self._sub_agents_map.get("improv_director")
           else:
               agent = self._sub_agents_map.get("setup_agent")

           # D-03: Fallback to improv_director (safest default)
           if agent is None:
               agent = self._sub_agents_map.get("improv_director")

           async for event in agent.run_async(ctx):
               yield event

       @property
       def _sub_agents_map(self) -> dict:
           return {sa.name: sa for sa in self.sub_agents}
   ```

5. **root_agent** (DramaRouter instance):
   ```python
   root_agent = DramaRouter(
       name="drama_router",
       description="戏剧导演系统 - Setup设定 + 即兴演出无限循环",
       sub_agents=[
           _setup_agent,
           _improv_director,
       ],
   )
   ```

6. **app = App(...)** remains the same.

7. **Remove entirely**: _storm_discoverer, _storm_researcher, _storm_outliner, _storm_director agent definitions, StormRouter class, old root_agent assignment.

8. **Update module docstring** to reflect new architecture:
   ```python
   """Director-Actor Drama System - DramaRouter Architecture.

   Two-phase agent routing:
   - Setup Phase (_setup_agent): One-shot /start → discover perspectives → synthesize outline → create actors
   - Improvise Phase (_improv_director): Infinite scene loop driven by system prompt

   Each actor remains an independent A2A service with its own session/memory.
   Cognitive boundaries are physically enforced by A2A isolation.
   """
   ```
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && uv run pytest tests/unit/test_agent.py -x -q 2>/dev/null || echo "TEST_FILE_MISSING"</automated>
  </verify>
  <done>
    - app/agent.py contains `class DramaRouter(BaseAgent)` with `_run_async_impl` method
    - app/agent.py contains `_setup_agent` Agent with 4 tools (start_drama, storm_discover_perspectives, storm_synthesize_outline, create_actor)
    - app/agent.py contains `_improv_director` Agent with 16 tools (no create_actor, no STORM tools)
    - app/agent.py does NOT contain StormRouter, _storm_discoverer, _storm_researcher, _storm_outliner, _storm_director
    - root_agent is DramaRouter with 2 sub_agents
    - `grep -c "class DramaRouter" app/agent.py` returns 1
    - `grep -c "class StormRouter" app/agent.py` returns 0
    - `grep -c "_setup_agent" app/agent.py` returns ≥ 2
    - `grep -c "_improv_director" app/agent.py` returns ≥ 2
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → DramaRouter | Untrusted text input, may contain prompt injection via /action |
| DramaRouter → sub-agents | Routing decision based on state["actors"] existence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | DramaRouter routing | mitigate | D-03 fallback to _improv_director when agent lookup fails, prevents dead-end routing |
| T-04-02 | Tampering | User input parsing | accept | User message parsing is read-only string matching for command detection; no state mutation from parsed text |
| T-04-03 | Tampering | state["actors"] manipulation | accept | State only modified through tool calls which have their own validation; router only reads |
</threat_model>

<verification>
```bash
# Verify DramaRouter class exists
grep "class DramaRouter" app/agent.py

# Verify no old classes remain
grep "class StormRouter" app/agent.py  # should return nothing

# Verify 2 sub-agents
grep -c "sub_agents=" app/agent.py  # should be 1

# Verify _improv_director has infinite loop declaration
grep "无预设终点\|无限\|永远不会自行结束" app/agent.py

# Verify _setup_agent has step markers
grep "步骤" app/agent.py

# Verify imports still work
uv run python -c "from app.agent import root_agent; print(type(root_agent).__name__)"
```
</verification>

<success_criteria>
1. `app/agent.py` 中 `StormRouter` 已被替换为 `DramaRouter`，只有 2 个子 Agent（`_setup_agent` + `_improv_director`）
2. `DramaRouter._run_async_impl()` 按 `state["actors"]` 是否非空路由（D-04）
3. `_improv_director` 的 system prompt 包含无终点声明和循环协议（D-05）
4. `_setup_agent` 合并了发现→研究→大纲三阶段，有显式步骤标记（D-02）
5. Fallback 路由到 `_improv_director`（D-03）
6. 旧 Agent 定义（_storm_discoverer, _storm_researcher, _storm_outliner, _storm_director）已移除
7. `from app.agent import root_agent` 不报错
</success_criteria>

<output>
After completion, create `.planning/phases/04-infinite-loop-engine/04-01-SUMMARY.md`
</output>
