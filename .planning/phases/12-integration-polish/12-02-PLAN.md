---
phase: 12-integration-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - app/tools.py
  - app/actor_service.py
  - tests/unit/test_tools_phase12.py
autonomous: true
requirements:
  - INTEG-02a
  - INTEG-03c
  - INTEG-05a
  - INTEG-05b

must_haves:
  truths:
    - "actor_speak() 的错误检测逻辑正确区分错误对话和正常对话"
    - "所有 actor_speak() 调用复用共享 httpx.AsyncClient，无重复创建"
    - "actor_speak() 连接失败时自动重启演员并重试"
    - "连续崩溃 3 次后放弃自动重启，返回结构化错误"
    - "每次自动重启记录到 restart_log"
  artifacts:
    - path: "app/tools.py"
      provides: "shared AsyncClient + crash recovery + actor_speak fix + error messages"
      contains: "get_shared_client"
    - path: "app/tools.py"
      provides: "crash recovery"
      contains: "_restart_actor"
    - path: "tests/unit/test_tools_phase12.py"
      provides: "unit tests for Phase 12 tools changes"
      min_lines: 80
  key_links:
    - from: "app/tools.py::actor_speak"
      to: "app/tools.py::_restart_actor"
      via: "crash detection and auto-restart"
      pattern: "_restart_actor"
    - from: "app/tools.py::_call_a2a_sdk"
      to: "app/tools.py::get_shared_client"
      via: "shared httpx.AsyncClient"
      pattern: "get_shared_client"
---

<objective>
修复 actor_speak() 已知 bug、引入共享 AsyncClient 连接池、实现演员崩溃被动检测与自动重启。

Purpose: actor_speak() 是 Director 调用演员的唯一路径——bug 修复确保错误检测正确，共享 AsyncClient 减少 TCP 冷启动开销，崩溃恢复增强系统韧性。三者均在 tools.py 和 actor_service.py 中实现。

Output: 修复后的 tools.py + actor_service.py 重启逻辑 + 单元测试
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-integration-polish/12-CONTEXT.md
@.planning/phases/12-integration-polish/12-RESEARCH.md
@.planning/phases/12-integration-polish/12-01-SUMMARY.md

<interfaces>
<!-- Executor 需要理解的关键接口 -->

From app/tools.py (current):
```python
# Line 214-343: actor_speak() — bug 修复 + 崩溃恢复注入点
async def actor_speak(actor_name, situation, tool_context) -> dict: ...

# Line 284-294: 当前错误处理（需审查优先级 bug）
try:
    actor_dialogue = await _call_a2a_sdk(card_file, prompt, actor_name, port)
except Exception as e:
    err_type = type(e).__name__
    msg = str(e).lower()
    if "connect" in err_type or "refused" in msg or "connection" in msg:
        actor_dialogue = f"[{actor_name}连接失败(端口:{port})。请用create_actor重启服务。]"
    elif "timeout" in err_type or "timeout" in msg or "timed out" in msg:
        actor_dialogue = f"[{actor_name}响应超时。LLM推理可能较慢，稍后重试。]"
    else:
        actor_dialogue = f"[{actor_name}调用失败({err_type}): {e}]"

# Line 297: 算符优先级检测条件
if not (actor_dialogue.startswith("[") and ("失败" in actor_dialogue or "超时" in actor_dialogue)):

# Line 346-440: _call_a2a_sdk() — 共享 AsyncClient 改造目标
async def _call_a2a_sdk(card_file, prompt, actor_name, port) -> str:
    httpx_client = httpx.AsyncClient(timeout=httpx.Timeout(timeout=120))  # 每次新建
    # ... use ...
    await httpx_client.aclose()  # 每次关闭
```

From app/actor_service.py (current):
```python
# Line 215-274: create_actor_service() — 崩溃重启复用
def create_actor_service(actor_name, role, personality, background, knowledge_scope,
                         other_actors=None, memory_entries=None) -> dict: ...

# Line 275+: stop_actor_service() — 重启前需先停止旧进程
def stop_actor_service(actor_name: str) -> dict: ...
```

From app/state_manager.py (after Plan 01):
```python
# Plan 01 新增：归档函数
def archive_old_scenes(state: dict) -> dict: ...
# Plan 01 改造：conversation_log 在 state 中
# state["conversation_log"] — 对话记录在 state 中
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: actor_speak bug fix + shared AsyncClient + scene archival integration</name>
  <files>app/tools.py, tests/unit/test_tools_phase12.py</files>
  <read_first>
    - app/tools.py (重点 line 214-440: actor_speak + _call_a2a_sdk)
    - app/state_manager.py (Plan 01 改造后的 archive_old_scenes)
    - tests/unit/conftest.py (mock_tool_context fixture)
  </read_first>
  <behavior>
    - Test: 错误检测 — "[朱棣连接失败(端口:1234)]" 被正确识别为错误对话
    - Test: 错误检测 — "[朱棣响应超时]" 被正确识别为错误对话
    - Test: 错误检测 — "我觉得应该这样做" 被正确识别为正常对话
    - Test: 错误检测 — "[朱棣调用失败(ConnectionError): connection refused]" 被正确识别为错误
    - Test: 共享 AsyncClient — get_shared_client() 返回同一实例
    - Test: 共享 AsyncClient — get_shared_client() 在 client 关闭后自动重建
    - Test: close_shared_client() 关闭并置 None
    - Test: _call_a2a_sdk 使用 get_shared_client() 而非新建 httpx.AsyncClient
  </behavior>
  <action>
**Part A: actor_speak 算符优先级 bug 修复 (D-05/D-08)**

1. 阅读 `actor_speak()` 当前代码（line 284-343），精确定位错误检测逻辑
2. 当前 line 297 的检测条件是：
   ```python
   if not (actor_dialogue.startswith("[") and ("失败" in actor_dialogue or "超时" in actor_dialogue)):
   ```
   这个逻辑**在当前代码中看起来是正确的**（已有括号）。但需检查：
   - 是否存在其他位置的类似检测但缺少括号
   - 错误消息中是否使用了不含"失败"或"超时"的中文描述
   - `_call_a2a_sdk()` 中无文本内容时的返回 `"[{actor_name}已响应但无文本内容]"` 是否被误判
3. 改进检测逻辑：将简单的 `"失败" in actor_dialogue` 替换为更明确的错误标记模式：
   - 在 `_call_a2a_sdk()` 的 except 块中，统一错误格式为 `"[ERROR:{type}] {details}"`
   - 在 actor_speak() 中，使用 `actor_dialogue.startswith("[ERROR:")` 检测错误
   - 这消除了"失败"/"超时"字符串匹配的脆弱性
4. 如果确认 line 297 逻辑正确无 bug，则仅做上述格式统一改进（将隐式字符串匹配改为显式标记）

**Part B: 共享 AsyncClient (D-11/D-12)**

1. 在 `app/tools.py` 顶部（import 区附近）新增：
   ```python
   _shared_httpx_client: httpx.AsyncClient | None = None

   def get_shared_client() -> httpx.AsyncClient:
       """Get or lazily create the shared httpx.AsyncClient (D-11/D-12)."""
       global _shared_httpx_client
       if _shared_httpx_client is None or _shared_httpx_client.is_closed:
           _shared_httpx_client = httpx.AsyncClient(
               timeout=httpx.Timeout(timeout=120.0),
               limits=httpx.Limits(max_connections=20, max_keepalive_connections=10),
           )
       return _shared_httpx_client

   async def close_shared_client():
       """Close the shared httpx.AsyncClient (D-12)."""
       global _shared_httpx_client
       if _shared_httpx_client is not None and not _shared_httpx_client.is_closed:
           await _shared_httpx_client.aclose()
           _shared_httpx_client = None
   ```

2. 改造 `_call_a2a_sdk()`：
   - 删除 `httpx_client = httpx.AsyncClient(timeout=httpx.Timeout(timeout=120))`
   - 改为 `httpx_client = get_shared_client()`
   - 删除 `await httpx_client.aclose()` — 生命周期由 start_drama/end_drama 管理
   - ClientConfig 改为 `ClientConfig(httpx_client=httpx_client, streaming=False, polling=False)`

3. 在 `start_drama()` 中调用 `get_shared_client()` 初始化（懒初始化，确保第一次调用时创建）
4. 在 `end_drama()` 中调用 `await close_shared_client()` 关闭

**Part C: next_scene 中触发场景归档**

1. 在 `next_scene()` 函数末尾 `_set_state(state, tool_context)` 之前，添加：
   ```python
   from .state_manager import archive_old_scenes
   state = archive_old_scenes(state)
   ```

**测试：** 创建 `tests/unit/test_tools_phase12.py`，包含上述 behavior 测试。
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && uv run pytest tests/unit/test_tools_phase12.py -x -q</automated>
  </verify>
  <done>
    - actor_speak 错误检测逻辑改进（显式标记替代隐式字符串匹配）
    - get_shared_client() 返回单例 AsyncClient
    - close_shared_client() 正确关闭和置 None
    - _call_a2a_sdk 使用共享 client，不再每次新建
    - end_drama() 调用 close_shared_client()
    - next_scene() 触发 archive_old_scenes()
    - 所有新增测试通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Actor crash recovery (passive detection + auto-restart)</name>
  <files>app/tools.py, app/actor_service.py, tests/unit/test_tools_phase12.py</files>
  <read_first>
    - app/tools.py (重点 actor_speak line 284-343 的 except 块)
    - app/actor_service.py (重点 create_actor_service, stop_actor_service)
    - tests/unit/test_tools_phase12.py (Task 1 已添加的测试)
    - tests/unit/conftest.py (mock_tool_context fixture)
  </read_first>
  <behavior>
    - Test: actor_speak 连接异常触发 _restart_actor
    - Test: _restart_actor 调用 stop_actor_service + create_actor_service
    - Test: _restart_actor 更新 crash_count 和 restart_log
    - Test: crash_count 达到 3 时返回错误，不再重启
    - Test: 成功调用后 crash_count 重置为 0
    - Test: restart_log 包含时间戳和原因
  </behavior>
  <action>
1. 在 `app/tools.py` 中新增常量和函数：
   ```python
   MAX_CRASH_COUNT = 3

   async def _restart_actor(actor_name: str, tool_context) -> dict:
       """Restart a crashed actor service (D-17/D-18/D-19)."""
       from .actor_service import stop_actor_service, create_actor_service
       from datetime import datetime

       state = tool_context.state.get("drama", {}) if tool_context else {}
       actor_data = state.get("actors", {}).get(actor_name, {})
       crash_count = actor_data.get("crash_count", 0) + 1

       if crash_count >= MAX_CRASH_COUNT:
           return {
               "status": "error",
               "message": f"角色「{actor_name}」连续崩溃 {crash_count} 次，请用 /cast 查看状态后手动重建",
           }

       # Stop old process if any
       stop_actor_service(actor_name)

       # Extract memory for context restoration
       memory_entries = []
       working = actor_data.get("working_memory", [])
       if working:
           memory_entries = [e.get("entry", str(e)) for e in working]
       critical = actor_data.get("critical_memories", [])
       if critical:
           memory_entries = [f"[关键] {m.get('entry', str(m))}" for m in critical] + memory_entries

       # Restart with original config + memory
       svc_result = create_actor_service(
           actor_name=actor_name,
           role=actor_data.get("role", ""),
           personality=actor_data.get("personality", ""),
           background=actor_data.get("background", ""),
           knowledge_scope=actor_data.get("knowledge_scope", ""),
           memory_entries=memory_entries,
       )

       # Update state: increment crash count + log
       if "actors" in state and actor_name in state.get("actors", {}):
           state["actors"][actor_name]["crash_count"] = crash_count
           state["actors"][actor_name].setdefault("restart_log", []).append({
               "time": datetime.now().isoformat(),
               "reason": "auto_restart_after_crash",
           })
           _set_state(state, tool_context)

       return svc_result
   ```

2. 改造 `actor_speak()` 的 except 块——注入崩溃恢复逻辑：
   ```python
   try:
       actor_dialogue = await _call_a2a_sdk(card_file, prompt, actor_name, port)
   except Exception as e:
       err_type = type(e).__name__
       msg = str(e).lower()
       if "connect" in err_type or "refused" in msg or "connection" in msg:
           # D-16: Passive crash detection → attempt auto-restart
           restart_result = await _restart_actor(actor_name, tool_context)
           if restart_result.get("status") == "success":
               # D-17: Retry once after restart
               try:
                   actor_dialogue = await _call_a2a_sdk(card_file, prompt, actor_name, port)
               except Exception:
                   actor_dialogue = f"[ERROR:connection] {actor_name}重启后仍无法连接(端口:{port})"
           else:
               actor_dialogue = f"[ERROR:connection] {actor_name}重启失败: {restart_result.get('message', '未知错误')}"
       elif "timeout" in err_type or "timeout" in msg or "timed out" in msg:
           actor_dialogue = f"[ERROR:timeout] {actor_name}响应超时"
       else:
           actor_dialogue = f"[ERROR:{err_type}] {actor_name}调用失败: {e}"
   ```

3. 更新错误检测逻辑（line 297/307/315 区域）：
   - 将 `actor_dialogue.startswith("[") and ("失败" in actor_dialogue or "超时" in actor_dialogue)`
   - 统一改为 `actor_dialogue.startswith("[ERROR:")`
   - 这同时修复了潜在算符优先级问题（D-08）并使检测更健壮

4. 在 actor_speak 成功调用后重置 crash_count：
   ```python
   # 在正常对话记录之后
   state = _get_state(tool_context)
   if actor_name in state.get("actors", {}):
       state["actors"][actor_name]["crash_count"] = 0
       _set_state(state, tool_context)
   ```

5. 测试：在 `tests/unit/test_tools_phase12.py` 中新增上述 behavior 测试。mock `_call_a2a_sdk`、`stop_actor_service`、`create_actor_service`。
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && uv run pytest tests/unit/test_tools_phase12.py -x -q</automated>
  </verify>
  <done>
    - _restart_actor() 函数存在，调用 stop + create
    - crash_count 在重启时 +1，成功时重置为 0
    - crash_count >= 3 时返回错误不再重启
    - restart_log 记录时间戳和原因
    - actor_speak() 连接异常时触发 _restart_actor
    - 错误检测统一使用 [ERROR:xxx] 标记
    - 所有新增测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Director → Actor A2A | 网络调用，连接可能失败/超时 |
| shared AsyncClient lifecycle | 单例在 start/end 间共享 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Denial of Service | actor_speak crash recovery | mitigate | MAX_CRASH_COUNT=3 限制无限重启；exponential backoff 可在后续迭代中添加 |
| T-12-05 | Tampering | shared AsyncClient | accept | 单用户模式，Director 本地调用，无中间人风险 |
| T-12-06 | Information Disclosure | restart_log in state | accept | 无 PII，仅含时间戳和原因类型 |
</threat_model>

<verification>
```bash
cd /workspace/director-actor-drama && uv run pytest tests/unit/test_tools_phase12.py -x -q

# 共享 AsyncClient 函数存在
grep -c "get_shared_client\|close_shared_client" app/tools.py

# 崩溃恢复函数存在
grep -c "_restart_actor\|MAX_CRASH_COUNT" app/tools.py

# 错误标记统一
grep "\[ERROR:" app/tools.py | head -5
```
</verification>

<success_criteria>
1. get_shared_client() 提供单例 AsyncClient，_call_a2a_sdk 复用
2. close_shared_client() 在 end_drama 时关闭
3. _restart_actor() 实现 passive detection + auto-restart
4. crash_count 达到 3 后放弃重启
5. restart_log 记录每次重启
6. 错误检测统一使用 [ERROR:xxx] 标记
7. 所有新增单元测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/12-integration-polish/12-02-SUMMARY.md`
</output>
