---
phase: 12-integration-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/state_manager.py
  - app/context_builder.py
  - tests/unit/test_state_manager.py
autonomous: true
requirements:
  - INTEG-02b
  - INTEG-03a
  - INTEG-03b

must_haves:
  truths:
    - "每次 _set_state() 后不立即写盘，5秒后才实际保存"
    - "退出时强制 flush，无数据丢失"
    - "_conversation_log 不再是模块级全局变量，对话记录存在 state['conversation_log']"
    - "跨 drama 污染消失：切换 drama 后对话记录是各自独立的"
    - "场景数超过20时，旧场景归档到独立文件，state.json 体积受控"
    - "归档场景可通过 load_archived_scene() 按需加载完整数据"
  artifacts:
    - path: "app/state_manager.py"
      provides: "debounce + conversation_log migration + scene archival"
      contains: "DEBOUNCE_SECONDS"
    - path: "app/state_manager.py"
      provides: "conversation_log in state"
      contains: "conversation_log"
    - path: "app/state_manager.py"
      provides: "scene archival"
      contains: "archive_old_scenes"
    - path: "tests/unit/test_state_manager.py"
      provides: "unit tests for debounce, conversation_log migration, archival"
      min_lines: 80
  key_links:
    - from: "app/state_manager.py"
      to: "app/context_builder.py"
      via: "archive_old_scenes + load_archived_scene"
      pattern: "archive_old_scenes|load_archived_scene"
    - from: "app/state_manager.py::_set_state"
      to: "threading.Timer"
      via: "debounce timer callback"
      pattern: "threading\\.Timer"
---

<objective>
重构 state_manager.py 核心持久化层：引入 debounce 机制减少 I/O 次数，将 _conversation_log 从模块级全局变量迁入 state 消除竞态，实现场景归档控制 state.json 体积。

Purpose: 这三项改造是 Phase 12 的基础——debounce 降低高频 I/O 开销、conversation_log 迁移消除跨 drama 污染（全局状态竞态是实际 bug 根因）、场景归档确保 50+ 场戏后 state.json 不会无限膨胀。三者耦合在 state_manager.py，必须在同一 plan 中完成。

Output: 改造后的 state_manager.py + context_builder.py 归档适配 + 单元测试
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-integration-polish/12-CONTEXT.md
@.planning/phases/12-integration-polish/12-RESEARCH.md

<interfaces>
<!-- Executor 需要理解的关键接口 — 从源码提取 -->

From app/state_manager.py (current):
```python
# Line 18: 需要删除的全局变量
_conversation_log: list[dict] = []

# Line 74-79: 实际写盘函数，debounce 后仍调用此函数
def _save_state_to_file(theme: str, state: dict): ...

# Line 87-96: 需要废弃的独立保存函数
def _save_conversations(theme: str): ...

# Line 1118-1125: debounce 改造目标
def _set_state(state: dict, tool_context):
    if tool_context is not None:
        tool_context.state["drama"] = state
        theme = state.get("theme")
        if theme:
            _save_state_to_file(theme, state)

# Line 338-412: init_drama_state() — 新增 conversation_log 初始化
def init_drama_state(theme: str, tool_context=None) -> dict: ...

# Line 488-630: load_progress() — 旧存档兼容
def load_progress(save_name: str, tool_context=None) -> dict: ...

# Line 99-134: add_conversation() — 改为读写 state
def add_conversation(speaker, content, conversation_type, tool_context) -> dict: ...

# Line 204-231: get_conversation_log() — 改为从 state 读取
def get_conversation_log(scene, tool_context) -> dict: ...

# Line 234-316: export_conversations() — 改为从 state 读取
def export_conversations(format, tool_context) -> dict: ...

# Line 320-328: clear_conversation_log() — 改为操作 state
def clear_conversation_log(tool_context) -> dict: ...
```

From app/context_builder.py (relevant for archival):
```python
# 需要确认的函数：归档场景如何影响上下文构建
# _build_recent_scenes_section() — 只取最近 N 场
# _extract_scene_transition() — 只取 scenes[-1]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Debounce state saving + conversation_log migration</name>
  <files>app/state_manager.py, tests/unit/test_state_manager.py</files>
  <read_first>
    - app/state_manager.py (全文，重点 line 1-20, 74-134, 204-328, 488-630, 1118-1125)
    - tests/unit/conftest.py (mock_tool_context fixture)
  </read_first>
  <behavior>
    - Test: debounce — _set_state() 调用后不立即写盘，_save_state_to_file 未被调用
    - Test: debounce — flush_state_sync() 被调用后，_save_state_to_file 被调用一次
    - Test: debounce — 连续多次 _set_state() 只触发一次实际写盘（5秒内）
    - Test: conversation_log — add_conversation() 写入 state["conversation_log"]，不再使用全局 _conversation_log
    - Test: conversation_log — get_conversation_log() 从 state["conversation_log"] 读取
    - Test: conversation_log — clear_conversation_log() 清空 state["conversation_log"]
    - Test: conversation_log — export_conversations() 从 state["conversation_log"] 读取
    - Test: init_drama_state — state 包含 conversation_log: [] 初始值
    - Test: load_progress — 旧存档中 conversations/conversation_log.json 文件内容被合并到 state["conversation_log"]
    - Test: load_progress — 新存档（已有 conversation_log）不读取旧文件
  </behavior>
  <action>
**Part A: Debounce (D-09)**

1. 在 `app/state_manager.py` 顶部 import 区新增 `import threading`
2. 在 `_conversation_log` 变量附近新增模块级变量：
   ```python
   _save_dirty: bool = False
   _save_timer: threading.Timer | None = None
   _latest_theme: str = ""
   _latest_state_ref: dict = {}
   DEBOUNCE_SECONDS = 5
   ```
3. 新增 `_flush_state()` 函数：
   - 从 `_latest_theme` 和 `_latest_state_ref` 读取当前状态
   - 如果 `_save_dirty` 为 True，调用 `_save_state_to_file(_latest_theme, _latest_state_ref)`
   - 重置 `_save_dirty = False`，`_save_timer = None`
4. 新增 `flush_state_sync()` 公共函数：
   - 取消 pending timer（`_save_timer.cancel()`）
   - 立即调用 `_save_state_to_file` 写盘
   - 重置 `_save_dirty` 和 `_save_timer`
5. 改造 `_set_state()`：
   - 保留 `tool_context.state["drama"] = state` 不变
   - 设置 `_latest_theme = state.get("theme", "")`，`_latest_state_ref = state`
   - 设置 `_save_dirty = True`
   - 取消已有 timer，创建新 `threading.Timer(DEBOUNCE_SECONDS, _flush_state)`
   - Timer 设为 `daemon=True`
   - **删除**原有的立即 `_save_state_to_file(theme, state)` 调用
6. 在 `save_progress()` 中调用 `flush_state_sync()` 确保保存时强制写盘
7. 在 `init_drama_state()` 中：保留末尾的 `_save_state_to_file(theme, state)` 直接调用（初始化应立即持久化），但也在 `_set_state()` 后不再二次写盘——改为只在 `flush_state_sync()` 或显式 `_save_state_to_file()` 时写盘
8. 注册 `atexit`：在模块底部添加 `atexit.register(flush_state_sync)`

**Part B: conversation_log 迁移 (D-06)**

1. `init_drama_state()` 新增 `state["conversation_log"] = []` 初始化
2. 改造 `add_conversation()`：
   - 删除 `global _conversation_log`
   - 删除 `_conversation_log.append(entry)`
   - 改为 `state = _get_state(tool_context)`，`state.setdefault("conversation_log", []).append(entry)`
   - 删除 `_save_conversations(theme)` 调用（统一走 `_set_state()` debounce）
   - 在函数末尾调用 `_set_state(state, tool_context)` 触发 debounce 保存
3. 改造 `get_conversation_log()`：
   - 删除 `global _conversation_log`
   - 删除从磁盘加载 conversation_log.json 的逻辑
   - 改为 `state = _get_state(tool_context)`，`log = state.get("conversation_log", [])`
   - 如果 `scene is not None`，过滤 `entries = [e for e in log if e.get("scene") == scene]`
   - 否则 `entries = log.copy()`
4. 改造 `export_conversations()`：
   - 删除 `global _conversation_log`
   - 改为从 `state = _get_state(tool_context)` 的 `state.get("conversation_log", [])` 读取
   - 删除从磁盘加载的逻辑
5. 改造 `clear_conversation_log()`：
   - 删除 `global _conversation_log` 和 `_conversation_log = []`
   - 改为 `state = _get_state(tool_context)`，`state["conversation_log"] = []`，`_set_state(state, tool_context)`
6. `load_progress()` 兼容旧存档：
   - 删除 `_conversation_log = []` 全局清空逻辑
   - 在 `state.update(save_data)` 之后新增：
     ```python
     # 兼容旧存档：conversation_log 不在 state 中
     if "conversation_log" not in state:
         conv_file = os.path.join(_get_conversations_dir(theme), "conversation_log.json")
         if os.path.exists(conv_file):
             with open(conv_file, "r", encoding="utf-8") as f:
                 state["conversation_log"] = json.load(f)
         else:
             state["conversation_log"] = []
     ```
7. 删除模块级 `_conversation_log: list[dict] = []` 变量（或注释标记 deprecated）
8. 删除 `_save_conversations()` 函数（或标记 deprecated）
9. 删除 `_get_conversations_dir()` 函数（不再需要独立目录）

**注意：** 不迁移 `_current_drama_folder`（D-07），在变量声明旁添加 `# TODO: Phase 12+ — migrate _current_drama_folder to ToolContext.state`

**测试：** 在 `tests/unit/test_state_manager.py` 中新增上述 behavior 对应的测试用例。使用 `mock_tool_context` fixture，mock `_save_state_to_file` 验证 debounce 行为。
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && uv run pytest tests/unit/test_state_manager.py -x -q</automated>
  </verify>
  <done>
    - _set_state() 不立即写盘，5秒 debounce
    - flush_state_sync() 强制写盘
    - _conversation_log 全局变量已删除，对话记录存在 state["conversation_log"]
    - add_conversation/get_conversation_log/export_conversations/clear_conversation_log 全部改为读写 state
    - load_progress() 兼容旧存档的 conversation_log.json
    - _current_drama_folder 保留但有 TODO 注释
    - 所有新增测试通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scene archival (20-scene threshold)</name>
  <files>app/state_manager.py, app/context_builder.py, tests/unit/test_state_manager.py</files>
  <read_first>
    - app/state_manager.py (重点 _set_state, init_drama_state, load_progress, _ensure_drama_dirs)
    - app/context_builder.py (重点 _build_recent_scenes_section, _extract_scene_transition)
    - tests/unit/test_state_manager.py (Task 1 已添加的测试)
  </read_first>
  <behavior>
    - Test: archive_old_scenes() 在场景数 <= 20 时不做任何修改
    - Test: archive_old_scenes() 在场景数 = 25 时，前 5 场被归档，保留最近 20 场完整数据
    - Test: 归档场景在 state["scenes"] 中只有索引元数据（scene_number, title, time_label, archived=True）
    - Test: 归档场景的完整数据写入 scenes/scene_{num:04d}.json 文件
    - Test: load_archived_scene() 能正确读取归档的场景数据
    - Test: load_archived_scene() 对不存在的场景返回 None
  </behavior>
  <action>
1. 在 `app/state_manager.py` 中新增常量和函数：
   ```python
   SCENE_ARCHIVE_THRESHOLD = 20

   def archive_old_scenes(state: dict) -> dict:
       """Archive scenes beyond threshold to reduce state.json size (D-10)."""
       scenes = state.get("scenes", [])
       if len(scenes) <= SCENE_ARCHIVE_THRESHOLD:
           return state

       to_archive = scenes[:-SCENE_ARCHIVE_THRESHOLD]
       keep = scenes[-SCENE_ARCHIVE_THRESHOLD:]

       theme = state.get("theme", "")
       if theme:
           drama_folder = _get_drama_folder(theme)
           scenes_dir = os.path.join(drama_folder, "scenes")
           os.makedirs(scenes_dir, exist_ok=True)
           for scene in to_archive:
               scene_num = scene.get("scene_number", 0)
               archive_path = os.path.join(scenes_dir, f"scene_{scene_num:04d}.json")
               with open(archive_path, "w", encoding="utf-8") as f:
                   json.dump(scene, f, ensure_ascii=False, indent=2)

       # Replace archived scenes with index metadata only
       archived_indices = [
           {
               "scene_number": s.get("scene_number"),
               "title": s.get("title", ""),
               "time_label": s.get("time_label", ""),
               "archived": True,
           }
           for s in to_archive
       ]
       state["scenes"] = archived_indices + keep
       return state

   def load_archived_scene(theme: str, scene_num: int) -> dict | None:
       """Load a single archived scene from disk."""
       archive_path = os.path.join(
           _get_drama_folder(theme), "scenes", f"scene_{scene_num:04d}.json"
       )
       if os.path.exists(archive_path):
           with open(archive_path, "r", encoding="utf-8") as f:
               return json.load(f)
       return None
   ```

2. 在 `write_scene()` 流程中触发归档：找到 `app/tools.py` 中 `write_scene()` 函数末尾 `_set_state(state, tool_context)` 之前，插入：
   ```python
   from .state_manager import archive_old_scenes
   state = archive_old_scenes(state)
   ```
   （注意：这需要 Plan 02 修改 tools.py 时确认位置，但归档函数本身在 state_manager.py 中定义，可独立测试）

   **替代方案：** 在 `_set_state()` 中自动触发归档——每次 `_set_state()` 时检查场景数并调用 `archive_old_scenes()`。但这样会使 debounce 逻辑更复杂。
   **最终选择：** 在 `next_scene()` 工具函数中（场景推进后）调用归档，因为归档只在场景增加时需要触发。在 `app/tools.py` 的 `next_scene()` 末尾添加归档调用。但 tools.py 属于 Plan 02 修改范围。
   **折中方案：** 在 `state_manager.py` 中 `_set_state()` 改造时添加：如果 `state.get("scenes")` 长度超过阈值，自动归档。但这引入副作用到 `_set_state()`。
   **最佳方案：** 在 `next_scene()` 中调用——在 Plan 02 的 Task 1 中处理。此处只实现核心函数和测试。

3. 验证 `context_builder.py` 无需改动：
   - `_build_recent_scenes_section()` 通常只取最近 5-10 场场景
   - 归档的是前 N-20 场，最近 20 场始终完整
   - `_extract_scene_transition()` 取 `scenes[-1]`，始终在保留区
   - 因此 context_builder.py **无需修改**（确认后可跳过）
   - 但如果 context_builder 使用了 `len(state["scenes"])` 做计算且依赖完整数据，需检查。grep 确认。

4. 测试：在 `tests/unit/test_state_manager.py` 中新增上述 behavior 测试用例。使用 `tmp_path` fixture 创建临时目录，mock `_get_drama_folder` 返回临时路径。
  </action>
  <verify>
    <automated>cd /workspace/director-actor-drama && uv run pytest tests/unit/test_state_manager.py -x -q</automated>
  </verify>
  <done>
    - archive_old_scenes() 函数存在，20 场阈值触发归档
    - load_archived_scene() 函数存在，按需读取归档场景
    - 归档场景保留索引元数据（scene_number, title, time_label, archived: True）
    - 最近 20 场场景保持完整数据不变
    - context_builder.py 无需适配（最近场景始终在保留区）
    - 所有新增测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| timer thread → state dict | _flush_state() 在独立线程读写 _latest_state_ref |
| main thread → state dict | _set_state() 在主线程读写 tool_context.state["drama"] |
| disk ← state_manager | state.json 写盘，归档文件写盘 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Denial of Service | _flush_state timer thread | accept | 最多丢 5 秒状态（D-09 已接受风险），daemon=True 不阻塞退出 |
| T-12-02 | Tampering | state.json on disk | accept | 单用户模式，本地文件，低风险 |
| T-12-03 | Information Disclosure | archived scene files | accept | 无 PII，dramas/ 已 gitignore |
</threat_model>

<verification>
```bash
# 单元测试全通过
cd /workspace/director-actor-drama && uv run pytest tests/unit/test_state_manager.py -x -q

# debounce 关键函数存在
grep -c "DEBOUNCE_SECONDS\|flush_state_sync\|_save_dirty" app/state_manager.py

# conversation_log 迁移确认
grep -c "conversation_log" app/state_manager.py
grep "_conversation_log" app/state_manager.py | head -5

# 归档函数存在
grep -c "archive_old_scenes\|load_archived_scene" app/state_manager.py
```
</verification>

<success_criteria>
1. `_set_state()` 不立即调用 `_save_state_to_file()`，改为 5 秒 debounce
2. `flush_state_sync()` 可强制写盘
3. `_conversation_log` 模块级变量已删除，对话记录通过 `state["conversation_log"]` 读写
4. `load_progress()` 兼容旧存档的 `conversations/conversation_log.json`
5. `archive_old_scenes()` 在场景 > 20 时归档到独立文件
6. 所有新增单元测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/12-integration-polish/12-01-SUMMARY.md`
</output>
